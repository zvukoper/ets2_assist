class QSchedule2
{
    
    // array of entities to get created when period starts and deleted outside period 
    ref array<EntityAI> TemporaryNPC = new array<EntityAI>;
    // trigger to mark as inactive and active
    DzrQuestTrigger TriggerObject;
    // set period
    void SetDayTimePeriod(int from_HH, int to_HH)
    {
        
    };
    // start cycle that checks if current time is in the period
    // execute outside period
    // execute inside period
   
}                                                                    