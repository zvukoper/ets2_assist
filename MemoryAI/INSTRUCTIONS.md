# INSTRUCTIONS — памятка агента для проекта ETS2_Assist_GUI

## Важные понятия и терминлогия

> "Начало сессии"

1) агент изучает MemoryAI, восстанавливает контекст WORKLOG и CURRENT_TASK
2) запоминает инструкции INSTRUCTIONS
3) выполняет переоценку лимитов и прогнозов, обновляет файл ollama_limits_usage
4) ожидает постановки задачи от пользователя

# Ключевой принцип - запомнить и применять при ЛЮБОМ запросе от пользователя
Никакие практические задачи (работа с файлами, разработка кода) не выполняются без оценки и прогноза лимитов и времени и ПОДТВЕРЖДЕНИЯ ПОЛЬЗОВАТЕЛЯ.
Алгоритм работы подробно описан начиная с раздела **ШАГ №0 — СТАРТ + ПРОВЕРКА ЛИМИТОВ (работу по задачам не начинаем):** в инструкциях INSTRUCTIONS.md.

> Краткий алгоритм:
Считываем текущие лимиты > Оцениваем затраты > Считываем лимиты ещё раз до актуальных > Делаем прогноз затрат лимитов и времени > Ожидание подтверждения > Выполнение задач > Считываем лимиты и фиксируем время > Отчёт по затратам, оценка прогнозов.

Оценка и прогнозы строятся после переоценки ollama_limits_usage.md

> "Переоценка лимитов" - процесс, когда агент тщательно анализирует результаты предыдущих задач и уточняет примерные  затраты на типовые задачи и дополняет ollama_limits_usage.md и изменяет устаревшие оценки.

> "Завершение сессии"

1) работа завершается, сохраняется контекст, записывается память, сохраняются незаконченные задачи в CURRENT TASK, 
2) агент пишет текст коммита и пользователь самостоятельно коммитит изменения.

> ⚠️ **УРОК 05.09.2026 (замечание пользователя — НАРУШЕНИЕ ПРОТОКОЛА ЗАВЕРШЕНИЯ):**
> при завершении сессии агент НЕ выполнил п.2 «Завершение сессии» — не написал текст коммита.
> Пользователь завершил сессию, агент просто попрощался. Это нарушение. Правило ниже —
> ОБЯЗАТЕЛЬНОЕ, применяется при КАЖДОМ завершении сессии.

> **ЧЕК-ЛИСТ ЗАВЕРШЕНИЯ СЕССИИ (обязательный, каждый пункт):**
> 1. ✅ Сохранить контекст: дописать `MemoryAI/WORKLOG.md` (что сделано в сессии).
> 2. ✅ Сохранить незаконченные задачи в `MemoryAI/CURRENT_TASK.md` (раздел «Следующий шаг»).
> 3. ✅ Обновить `MemoryAI/INSTRUCTIONS.md` (новые задачи/планы/уроки/проблемы).
> 4. ✅ **НАПИСАТЬ ТЕКСТ КОММИТА** (готовое сообщение для `git commit` — краткое, по-русски,
>    с перечнем изменений и версией сборки). Пользователь сам выполняет коммит.
> 5. ✅ Зафиксировать лимиты (Session usage %) и время в `MemoryAI/ollama_limits_usage.md`.
> 6. ✅ Краткое резюме сессии пользователю (что сделано / что осталось / текст коммита).
> Без п.4 (текст коммита) сессия НЕ считается завершённой.

> Этот файл — для меня (агента). В НОВОЙ сессии пользователь даст команду прочитать
> `MemoryAI/INSTRUCTIONS.md` и `MemoryAI/WORKLOG.md`. Прочитав их, я сразу вспомню контекст
> и продолжаю работу без повторного разбора.
>
> ⚠️ **ОБЯЗАТЕЛЬНОЕ ПРЕДУПРЕЖДЕНИЕ ПЕРЕД ЛЮБОЙ ПРАВКОЙ .md (УРОК СЕССИИ 9, 27.08.2026):**
> файлы памяти — UTF-8 **С BOM**. Read/Edit-инструменты среды читают UTF-8 корректно
> ТОЛЬКО при наличии BOM; **без BOM кириллица показывается как mojibake** (`Ð Ď Ň ...`),
> а Edit падает с «oldString not found». Причина, с которой я споткнулся: PowerShell
> `Set-Content -Encoding utf8` (и `Out-File`) в PS 5.1 пишет UTF-8 **БЕЗ BOM** и ломает файл.
> **КАК НЕ ПОВТОРИТЬ:**
> 1. Правь .md ТОЛЬКО через Edit-инструмент — он сохраняет BOM и кириллицу корректно.
> 2. Если нужна пакетная правка в PowerShell — пиши СТРОГО С BOM:
>    `[System.IO.File]::WriteAllText($p, $t, [System.Text.UTF8Encoding]::new($true))`
>    (НЕ `Set-Content -Encoding utf8` и НЕ `Out-File` по умолчанию).
> 3. Увидел mojibake в read/edit — СТОП. Не дописывай поверх битого. Восстанови
>    `git checkout -- MemoryAI/INSTRUCTIONS.md` (и/или `MemoryAI/WORKLOG.md`) и переапликай правки
>    через Edit. Файлы git-tracked, оригинал всегда цел.

# Самоконтроль агента - агент должен строго следить за своим состоянием и избегать зацикливания и повторения одинаковых выводов и сообщений. Такое многократно случалось с deepseek-v4-flash:cloud и происходил бесполезный рахсод лимитов.

# Ключевой принцип - запомнить и применять при ЛЮБОМ запросе от пользователя
Никакие задачи не выполняются без оценки и прогноза лимитов и времени и ПОДТВЕРЖДЕНИЯ ПОЛЬЗОВАТЕЛЯ.
Алгоритм работы подробно описан начиная с раздела **ШАГ №0 — СТАРТ + ПРОВЕРКА ЛИМИТОВ (работу по задачам не начинаем):**.
Краткий алгоритм: **Оценка > Проноз > Ожидание подтверждения > Выполнение задач > Оценка > Отчёт**

> ⚠️ **УРОК 05.09.2026 (замечание пользователя — НАРУШЕНИЕ ПРОТОКОЛА):** после вывода прогноза
> (📊 КОНТРОЛЬ ЛИМИТОВ + TODO) агент ОБЯЗАН ОСТАНОВИТЬСЯ и ЖДАТЬ явного подтверждения
> пользователя («ОК»/«Работаем»/«Выполняй»). НЕЛЬЗЯ сразу переходить к правкам и публикации,
> даже если прогноз показан. Нарушение: задача «developer mode» — прогноз выведен, но работа
> начата без подтверждения. Порядок ВСЕГДА: ресурс → прогноз → **СТОП и ожидание команды** →
> работа. Если пользователь не дал явного «ОК» — НЕ начинать.


## Стоящие команды пользователя («запомни» / «учти»)
- Любая фраза «запомни …» или «учти …» означает: кратко записать факт в память
  (`INSTRUCTIONS.md` и/или `WORKLOG.md`) — БЕЗ излишеств, но так, чтобы в следующей
  сессии всё было понятно. Не нужно переспрашивать, просто зафиксируй.
- **⚠️ НАПОМНИТЬ В СЛЕДУЮЩЕЙ СЕССИИ (исправить; требование пользователя 05.09.2026):**
  телепорт в игру (Ctrl+T) — после ввода команды goto в консоль ETS2 нужно нажать ENTER и
  ЗАКРЫТЬ консоль той же клавишей, что и вызывалась (scan code 0x29). Сейчас команда вводится,
  но НЕ активируется (стоит введённой). В `RunTeleportGameSync` после TypeUnicodeText(cmd) →
  PressKey(Enter) → добавить PressScanCode(SCANCODE_CONSOLE) для закрытия консоли. Проверить,
  что Enter реально нажимается и команда выполняется.
  **✅ РЕШЕНО (05.09.2026, v39.39-TELEPORT-ENTER-CLOSE):** консоль ETS2 работает на SCAN CODES
  (открывается только scan code 0x29, виртуальная VK_OEM_3 не работала). Старый Enter через
  `PressKey(Keys.Enter)` (VK_RETURN) в консоли не срабатывал. ФИКС: Enter через
  `PressScanCode(SCANCODE_ENTER)` (0x1C) + закрытие консоли `PressScanCode(SCANCODE_CONSOLE)` (0x29).
  Константа `SCANCODE_ENTER = 0x001C` в QuestsManager.cs. Осталось верифицировать у пользователя.
- После КАЖДОГО изменения: дополнять `WORKLOG.md` и обновлять `INSTRUCTIONS.md`
  (новые задачи, планы, нереализованные прототипы, нерешённые проблемы).
- **Перед КАЖДОЙ задачей/правкой — сверяться с папкой логов приложения**
  (`bin\Release\net10.0-windows\win-x64\publish\Logs`). Там наша ключевая отладочная
  информация, которую мы создали сами для себя. Это главный источник диагностики.
- Если пользователь помечает задачу как **«в планы»** — дописать её в раздел
  «Планы на будущее» этого файла (и, при необходимости, в WORKLOG), как отложенную задачу.
- **Копилка задач (`MemoryAI\Копилка задач`):** если пришла команда добавить что-то в
  копилку задач — задача добавляется **целиком, без правок**, в папку
  `MemoryAI\Копилка задач` как отдельный `.md`-файл, в имени файла — краткое название
  задачи (как `Кафе.md`). Содержимое файла = ровно то, что дал пользователь,
  без редактирования/перефразирования.
- **CURRENT_TASK.md (НОВОЕ ПРАВИЛО, 31.08.2026):** решаемая СЕЙЧАС задача (не весь
  TODO!) записывается в `MemoryAI\CURRENT_TASK.md` со строго фиксированными разделами:
  `Текущая задача:` / `Что уже сделано:` / `Что обнаружено:` / `Текущая архитектура:` /
  `Изменённые файлы:` / `Что осталось:` / `Известные проблемы:` / `Следующий шаг:`.
  Обновлять ПРИ КАЖДОМ шаге работы над задачей (создание/изменение) — файл заменяет
  «журнал в голове» между сессиями; UTF-8 С BOM (как все .md). При завершении задачи —
  раздел «Следующий шаг» подсказывает, с чего продолжить.

## Логирование и отладка (правила пользователя)
- Логи пишутся РЯДОМ с exe: `bin\Release\net10.0-windows\win-x64\publish\Logs`
  (при публикации в др. папку — рядом с ней).
- В лог пишем ПОШАГОВУЮ отработку работы приложения для отладки. НЕ спамим
  значениями переменных — только шаги («что сделали»).
- Спамить значениями (для отлова багов) можно в `app_data` (рядом с exe / в AppData),
  если это действительно нужно.
- Живая отладка телеметрии: страница `http://localhost:8082/web_telemetry_inspector.html`
  — в реальном времени видны почти все данные от WebSocket TruckTel.
- **ОТЛАДКА ПО ЛОГАМ — БЕЗ УЧАСТИЯ ПОЛЬЗОВАТЕЛЯ (новое правило):** если для диагностики
  или отладки нужен фрагмент лога — агент САМ находит, куда пишется лог приложения
  (`bin\Release\net10.0-windows\win-x64\publish\Logs\app_workflow.log` или иной путь,
  выясняемый из кода/конфигурации), САМ читает файл лога и САМ ищет в нём нужную информацию
  (grep/чтение). **Пользователь НЕ должен копировать и вставлять фрагменты лога для агента.**
  При запросе у пользователя — просить только воспроизвести действие и (опционально) указать
  примерное время, а лог агент забирает и анализирует самостоятельно.

## Контроль лимитов ollama (Session usage) — ОБЯЗАТЕЛЬНОЕ ПРАВИЛО (01.09.2026)
### ⭐ ПЕРВОСТЕПЕННАЯ ЗАДАЧА АГЕНТА (03.09.2026) — фиксация/оценка/прогноз/факт лимитов и времени
**Это ГЛАВНАЯ задача, ПЕРВАЯ в списке, приоритетнее любой другой работы.** Каждая задача
начинается с неё и заканчивается ею. Порядок:

**ШАГ №0 — СТАРТ + ПРОВЕРКА ЛИМИТОВ (работу по задачам не начинаем):**
1. Проанализировать запрос, но не выполнять никаких действий и сразу же прочитать `MemoryAI\ollama_parsed_usage.txt` (строка 1 = процент с запятой, строка 2 = время до ресета).
2. Зафиксировать: текущий лимит, текущее системное время, время ресета.
3. СРАЗУ создать новую строку в «РАЗДЕЛ 3. Лог затрат лимитов и времени» и записать туда
   эту стартовую информацию (лимит, время, RESET, модель).
4. **ФИКСАЦИЯ РАСХОДА НА АНАЛИЗ/ПРОГНОЗ (правило, 05.09.2026):** расход ресурсов на
   предварительный анализ и прогноз (ШАГ №0/№1, исследование кода, чтение файлов) фиксировать
   ОТДЕЛЬНО в РАЗДЕЛЕ 3 (запись «СТАРТ + АНАЛИЗ/ПРОГНОЗ» с лимитом на момент начала анализа).
   Позже этот расход включить в итоговый отчёт по задаче (в записи «ИТОГ» указать, что в
   затраченный % входит и расход на анализ/прогноз). Это позволяет видеть реальную стоимость
   планирования, а не только правок.

**⚠️ УРОК 03.09.2026 21:16 (замечание пользователя — ПОДКРЕПЛЕНО, без исключений):**
ШАГИ №0/№1/№1.5 обязательны ПЕРЕД ЛЮБОЙ задачей — включая «проверь логи», диагностику,
мелкие правки, ответы-исследования. Нарушение на задаче «Проверь логи» (старт/прогноз
постфактум, подтверждение не запрошено). Порядок ВСЕГДА: ресурс → прогноз → TODO →
ожидание команды → работа. Прогноз постфактум считается ошибкой протокола.

**ШАГ №1 — АНАЛИЗ + ПРОГНОЗ + СОГЛАСОВАНИЕ С ПОЛЬЗОВАТЕЛЕМ (работу по задачам не начинаем):**
1. Проанализировать задачи, сформулировать, составить TODO.
2. ОБновить данные из `MemoryAI\ollama_parsed_usage.txt` и спрогнозировать расход лимитов и времени.
3. В TODO ПЕРВЫМ пунктом записать данные ПРОГНОЗА, включая расчёт К КАКОМУ ВРЕМЕНИ задача
   будет выполнена (не только сколько займёт). Формат первого пункта TODO:
   `1) ❇️ ПРОГНОЗ: Расход: <сколько % и времени будет потрачено>. Завершение: <значение
   лимита по завершении и во сколько по системному времени задача будет завершена>`
4. Прогноз также фиксируется в лог лимитов (РАЗДЕЛ 3).

**ШАГ №1.5 — ОСТАНОВКА И ОЖИДАНИЕ КОМАНДЫ ПОЛЬЗОВАТЕЛЯ (работу по задачам не начинаем):**
1. После того как агент зафиксировал ресурсы (ШАГ №0), рассчитал прогнозы и составил список
   TODO (ШАГ №1) — он **ОСТАНАВЛИВАЕТСЯ и ЖДЁТ команды пользователя**.
2. Пользователь знакомится с прогнозами и списком задач и даёт подтверждение: «ОК»,
   «Работаем», «Выполняй» и т.п.
3. **ТОЛЬКО после подтверждения пользователя** агент продолжает остальные шаги инструкции
   (работу над задачами и ШАГ последний).
4. Без подтверждения — НЕ начинать работу над задачами, только ждать.

**ДАЛЬНЕЙШИЕ ШАГИ:** идёт работа над задачами. (работу по задачам МОЖНО НАЧИНАТЬ если пользователь всё подтвердил)

**ШАГ ПОСЛЕДНИЙ — ИТОГ (когда все задачи выполнены):**
1. Зафиксировать системное время, текущий лимит, текущее время ресета.
2. В лог прописать ФАКТИЧЕСКИЙ расход времени и лимитов.
3. Вычислить и отметить, насколько точно сбылся прогноз (прогноз vs факт).
- **Планирование ПО ЛЮБОЙ задачи:** перед НАЧАЛОМ работы ВСЕГДА создавать план TODO
  через инструмент manage_todo_list.
- **Первый пункт TODO — оценка лимитов/ресурсов + прогноз** в формате:
  ```
  Текущий ресурс: <процент> <reset>. Прогноз по завершении: ~<прогнозируемое значение лимита после завершения всех TODO> через <прогнозируемое время на все задачи>.
  ```
  Пример: `Текущий ресурс: 25.4% RESET 2ч. Прогноз по завершении: ~33% через ~55 мин.`
  Прогноз считается по РАЗДЕЛ 1 `MemoryAI\ollama_limits_usage.md` (сумма ресурсозатратности задач
  TODO + запас на итерации/отладку, прибавленная к текущему %).
  Оценка лимитов делается ПЕРЕД остальными пунктами; только после неё добавляются пункты задач.- **ВЫВОД ЛИМИТОВ В TODO (уточнение 01.09.2026, фидбек пользователя):** пользователь НЕ видит
  обзор/прогноз лимитов в TODO-списке. ПРАВИЛО: выводить контроль лимитов и прогноз НЕ только
  первым пунктом TODO, но и **отдельным текстовым сообщением ПЕРЕД началом работы**, выделенным
  цветом и шрифтом (например, жирным + эмодзи 📊), чтобы пользователь гарантированно его увидел.
  Формат сообщения:
  ```
  📊 КОНТРОЛЬ ЛИМИТОВ (первый пункт TODO):
  - Текущий ресурс: <процент> RESET <время>.
  - Прогноз по завершении: ~<процент> через ~<время>.
  - Модель: <имя>.
  - ⚠️ предупреждение о RESET/пороге при необходимости.
  ```
  Это сообщение — ОБЯЗАТЕЛЬНОЕ перед каждой задачей (даже мелкой), как и первый пункт TODO.- **После выполнения всех задач** — снова проверить лимиты.
- **В долгих задачах** — проверять лимиты **каждые ~5 минут**; при пороге **90%** и **95%**
  предупреждать пользователя. Прогнозировать задачи так, чтобы уложиться в лимит без риска
  прерваться в неподходящий момент.
- **Инструмент проверки (НОВОЕ ПРАВИЛО, 02.09.2026):** лимиты БОЛЬШЕ НЕ парсятся бат-файлами
  (`ollama_usage_v2.bat` / `ollama_login.bat` НЕ используются). Вместо этого просто читается
  файл **`MemoryAI\ollama_parsed_usage.txt`** — в нём ВСЕГДА свежие данные (обновляет
  расширение VS Code «Ollama Usage Monitor» каждые 5 сек):
  - строка 1 — процент Session usage (с запятой, без округления, напр. `3,5`);
  - строка 2 — время до ресета (напр. `3ч 9м`).
  Формат чтения: `Текущий ресурс: <строка1>% RESET <строка2>.` (напр. `3,5% RESET 3ч 9м`).
  Если файл пуст/отсутствует — спросить процент у пользователя в конце сессии.
- **RESET (время до сброса лимитов):** учитывать в прогнозе расходов. Если до сброса
  осталось мало времени, а задача большая — выгоднее дождаться сброса (лимиты обнулятся),
  чем рисковать прерваться на середине. При планировании: если `RESET` меньше ожидаемого
  времени задачи + запас — предложить пользователю подождать сброса.
- **ЛОГИКА РЕСУРСА — ПРОЦЕНТЫ ПЕРВИЧНЫ (НОВОЕ ПРАВИЛО, 01.09.2026, задача пользователя):**
  основной ресурс = ПРОЦЕНТЫ. Достижение **100% = конец работы** (агент отключается),
  продолжение возможно только после RESET (проценты обнулятся до 0%).
  Порядок прогноза ПЕРЕД задачей:
  1) **Хватит ли процентов:** `старт% + прогноз_расхода ≤ ~95%` (запас 5% на порог)?
     ДА → работаем; время вторично (RESET просто отметится в логе).
  2) **Процентов НЕ хватает** (сумма > 95%): проверить, наступит ли RESET ЗА время
     выполнения задачи. ДА (RESET < прогнозного времени работы) → работаем: в середине
     задачи проценты обнулятся и ресурс восстановится.
  3) **Ни процентов, ни RESET до конца задачи** → ПРОБЛЕМА: достигнем 100% и агент
     отключится на середине. Варианты: разбить задачу на части (выполнить часть сейчас,
     остаток после RESET), отложить до сброса, или предупредить пользователя и спросить.
  Формат прогноза с учётом этого:
  ```
  Текущий ресурс: NN% RESET hh. Прогноз расхода: ~X% → итог ~NN+X% через ~Y мин.
  Хватает/НЕ хватает: <верdict + вариант (работаем / ждём RESET / разбиваем)>.
  ```
- **Если лимиты не парсятся / файл пуст или отсутствует:** спросить процент у пользователя
  в конце сессии (расширение обновляет `ollama_parsed_usage.txt` само, бат-файлы не нужны).
- **Учёт расхода:** после задачи записывать в `MemoryAI\ollama_limits_usage.md`:
  - затраченные % лимита на задачу/набор задач;
  - затраченное время.
  Файл имеет 2 раздела: РАЗДЕЛ 1 — описание типовых задач и их примерная ресурсозатратность;
  РАЗДЕЛ 2 — лог затрат лимитов и времени.
- **ПРОГНОЗ И ФАКТ (обязательно, 01.09.2026):** в каждой записи РАЗДЕЛА 2 указывать И
  прогнозируемое время/расход (из первого пункта TODO), И фактическое время/расход после
  выполнения. Формат:
  ```
  [дата время] задача | модель | старт% -> конец% | затрачено% | прогноз: ~X%/~Yмин | факт: Z%/~Wмин | класс
  ```
  Это накапливает данные для переоценки РАЗДЕЛА 1.
- **ПЕРЕОЦЕНКА РАЗДЕЛА 1 (01.09.2026):** периодически (раз в несколько задач или по запросу)
  анализировать `MemoryAI\ollama_limits_usage.md`, сравнивать прогноз vs факт и корректировать
  оценки РАЗДЕЛА 1. ВАЖНО (по факту 01.09.2026): экономная модель deepseek-v4-flash тратит
  ~x0.3-0.5 от оценок РАЗДЕЛА 1 (для glm5.3-flash), а НЕ x0.5-0.7. Прогнозы в TODO (10-60%)
  были сильно завышены — фактический расход 0.3-7.2%. Для экономной модели использовать
  фактические оценки из блока «ПЕРЕОЦЕНКА 01.09.2026» в РАЗДЕЛЕ 1.
- **ПРОГНОЗ И ФАКТ (уточнение 01.09.2026, задача пользователя):** в каждой записи РАЗДЕЛА 2
  указывать И прогнозируемое время/расход (из первого пункта TODO), И фактическое время/расход
  после выполнения. Формат записи:
  ```
  [дата время] задача | модель | старт% -> конец% | затрачено% | прогноз: ~X%/~Yмин | факт: Z%/~Wмин | класс
  ```
  Это позволяет накапливать данные для переоценки РАЗДЕЛА 1 (ресурсозатратность типовых задач).
- **ПЕРЕОЦЕНКА РАЗДЕЛА 1 (задача пользователя, 01.09.2026):** периодически (раз в несколько
  задач или по запросу) делать тщательный анализ `MemoryAI\ollama_limits_usage.md` и
  переоценивать прогнозы времени и расхода лимитов на основании фактических данных о
  завершённых задачах (сравнивать прогноз vs факт, корректировать оценки РАЗДЕЛА 1).
- **Раз в день (перед началом всех работ):** перечитать `MemoryAI\ollama_limits_usage.md`, в этом же
  файле обновить/резюмировать РАЗДЕЛ 1 (ресурсозатратность типовых задач и типичное время),
  затем использовать эти оценки для прогноза ресурсозатратности предстоящих задач.
- **Прогноз:** перед задачей оценить её класс (см. РАЗДЕЛ 1), прибавить запас на итерации/отладку;
  если сумма с текущим % > 90 — предупредить пользователя и предложить отложить/разбить задачу.
- **Модели агента (3 сценария, 01.09.2026):** агент фиксирует своё имя (модель) в каждой записи
  лога `MemoryAI\ollama_limits_usage.md` (РАЗДЕЛ 3). Модели:
  1) нормальная работа — `glm5.3-flash:cloud`;
  2) экономный режим — `deepseek-v4-flash:cloud`;
  3) макс. экономия — `gemma4:cloud`.
  Рекомендация по остатку ресурсов и времени до сброса (RESET):
  - остаток > 60% и RESET большой → `glm5.3-flash:cloud`;
  - остаток 30-60% или RESET мал → `deepseek-v4-flash:cloud`;
  - остаток < 30% или RESET критичен → `gemma4:cloud`.
  Если задача большая, а до RESET мало времени — дождаться сброса (лимиты обнулятся),
  затем работать на `glm5.3-flash:cloud`. Экономные модели тратят меньше (примерно x0.5-0.7).

- **Модель агента в отчётах (правило, 01.09.2026):** в отчёте 📊 КОНТРОЛЬ ЛИМИТОВ
  и в первом пункте TODO указывается ТЕКУЩАЯ модель агента (та, что реально
  обслуживает сессию СЕЙЧАС — см. имя модели в контексте/заголовке чата).
  НЕ копировать модель из памяти прошлых сессий: пользователь переключает модели
  (glm5.3-flash:cloud ↔ deepseek-v4-flash:cloud ↔ gemma4:cloud), и старое значение
  вводит в заблуждение. Прогноз расхода — по оценкам РАЗДЕЛА 1
  ollama_limits_usage.md ДЛЯ ТЕКУЩЕЙ модели: glm5.3-flash = базовые оценки РАЗДЕЛА 1;
  экономные (deepseek/gemma) = x0.3-0.5 от базовых.

## Статус памяти между сессиями
Память живёт ТОЛЬКО в папке `F:\repo\ets2_assist\MemoryAI\` (файлы `WORKLOG.md` и `INSTRUCTIONS.md`).
Системная память (memory-*) может быть неполной — всегда сверяйся с этими файлами первыми.

## Что сделать в начале каждой сессии (по запросу пользователя)
1. Прочитать `MemoryAI/INSTRUCTIONS.md` (этот файл) — понять контекст, задачи, проблемы.
2. Прочитать `MemoryAI/WORKLOG.md` — что уже сделано, как собирать, ключевые файлы.
3. Кратко подтвердить пользователю, что контекст восстановлен, и уточнить текущую задачу.

## Как собирать и публиковать EXE
- VS MCP (`vs-mcp_LoadSolution`) НЕ грузит `.slnx` (падает `E_ABORT`). Не трать время на него.
- **ЧИСТКА `.slnx` ОТ ВРЕМЕННЫХ ПРОЕКТОВ (ПРАВИЛО, 04.09.2026):** после любого тестирования/
  диагностики (probe-проекты, временные csproj в `Temp` и т.п.) ОБЯЗАТЕЛЬНО вычищать их из
  `ETS2_Assist_GUI.slnx`. Временные проекты создаются в `%TEMP%` (напр. `probe_dcomp2\probe.csproj`)
  и после удаления папки остаются в slnx как битые ссылки → ошибка загрузки решения.
  **Проверка перед КАЖДЫМ build/publish:** открыть `ETS2_Assist_GUI.slnx` и убедиться, что в нём
  ТОЛЬКО `ETS2_Assist_GUI.csproj` (и, при необходимости, другие реальные проекты). Любая ссылка
  на путь вне корня проекта (особенно в `%TEMP%`/`AppData`) — это временный проект, удалить.
  Если такой проект ещё нужен для диагностики — держать его ВНЕ slnx (компилировать напрямую
  через `dotnet build <путь>\probe.csproj`), а не добавлять в решение.
- Использовать dotnet CLI из `F:\repo\ets2_assist`:
  ```
  dotnet publish ETS2_Assist_GUI.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
  ```
- **ФИНАЛЬНАЯ СБОРКА — ТОЛЬКО через `compile.ps1` (КОРЕНЬ РЕПО, ПРАВИЛО 12.09.2026):**
  финальную сборку проекта делать командой `.\compile.ps1` — скрипт сам:
  1) гасит запущенный `ETS2_Assist.exe` (`--shutdown`); 2) убивает зависшие `msedgewebview2`;
  3) чистит кэш WebView2 (профиль `%LOCALAPPDATA%\ETS2_Assist\EBWebView` + все `<exe>.WebView2`
     рядом с exe в `publish` и `bin`); 4) `dotnet clean` + `restore` + `publish -c Release`;
  5) повторно чистит `<exe>.WebView2`; 6) запускает приложение
     (`Start-Process ... -Verb RunAs`).
  **Запускать скрипт КАК ЕСТЬ, без модификаций** — автозапуск в конце работает нормально,
  отдельный ключ `-NoStart` НЕ нужен (проверено пользователем 12.09.2026: «просто запускай
  скрипт, всё сработает, так уже делали»).
  После скрипта всё равно сверить `exe = data/ets2_assist_build.txt = data/web_runtime_manifest.json`
  (п.4 ниже) и при расхождении дозаписать оба data-файла (штамм времени `Hmm` может отличаться
  от того, что попал в exe).
  Ручной `dotnet publish` использовать ТОЛЬКО для промежуточных проверок компиляции.
- Результат: `bin\Release\net10.0-windows\win-x64\publish\ETS2_Assist.exe` (~130 МБ).
  Имя EXE задано через `<AssemblyName>ETS2_Assist</AssemblyName>` в csproj (namespace проекта
  остаётся `ETS2_Assist_GUI`).
- JS-файлы в `data/` не компилируются — правки копируются при публикации как Content.
  Перед публикацией желательно `node --check` на изменённых `.js`.
- **OFFLINE-БИЛД (машина без egress в NuGet; 27.08.2026, СЕССИЯ 9):**
  `dotnet` на этой машине НЕ имеет доступа к NuGet-фиду → `dotnet restore` падает МОЛЧА:
  `error MSB4181: The "RestoreTask" task returned false but did not log an error.`
  Все нужные пакеты УЖЕ есть в глобальном кэше `%USERPROFILE%\.nuget\packages`.
  Рецепт:
  1. Скопировать все `*.nupkg` из `%USERPROFILE%\.nuget\packages` в плоскую папку
     (напр. `C:\Users\Admin\AppData\Local\Temp\opencode\offlinefeed`):
     `Get-ChildItem "$env:USERPROFILE\.nuget\packages" -Recurse -Filter *.nupkg | Copy-Item -Destination <flatfeed>`
  2. `dotnet restore ETS2_Assist_GUI.csproj --source <flatfeed>`
  3. `dotnet publish ETS2_Assist_GUI.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true --no-restore`
  - Windows Desktop workload НЕ нужен: `Microsoft.WindowsDesktop.App.Ref` уже лежит в
    `C:\Program Files\dotnet\packs`, минимальный WinForms восстанавливается/билдится офлайн нормально.
- **ЛОВУШКА SEMVER (27.08.2026, СЕССИЯ 9):** `<Version>` (NuGet-версия, используемая restore)
  ДОЛЖНА быть ЧИСТЫМ SemVer. Если в `<Version>` положить составную пре-версию вроде
  `1.0.38-TARGETS-FILE-2026.08.27-2110` — restore падает ТАК ЖЕ молча (`RestoreTask returned false`).
  Описательную строку класть ТОЛЬКО в `<InformationalVersion>` (SDK генерит
  `AssemblyInformationalVersionAttribute` из `$(InformationalVersion)`; `<AssemblyInformationalVersion>`
  в одиночку атрибут НЕ генерит).    `<Version>` оставлять чистой (`1.0.38`). Это ПРАВИЛО — иначе билд не соберётся.
- **ЛОВУШКА: exe ЗАЛОЧЕН при публикации (урок 28.08.2026, сессия 10, MAP-EDITOR):**
   `dotnet publish` (и `dotnet build`) падает на этапе упаковки с
   `System.UnauthorizedAccessException: Access to the path '...publish\ETS2_Assist.exe' is denied`,
   если этот exe **в данный момент запущен** (пользователь тестирует сборку). При этом цель
   `SyncDataToPublish` (`AfterTargets="Publish"`) НЕ дорабатывает, и папка `publish\data`
   ОСТАЁТСЯ ПУСТОЙ/неполной (пропадают js, css, GeoJson, language, localized_cities и т.д.) —
   приложение потом падает/работает без данных. Признак: файл залочен = процесс жив.
   **ПРАВИЛО (обязательное перед КАЖДЫМ build И publish):**
   1. ПЕРЕД `dotnet build`/`dotnet publish` проверить, запущен ли `ETS2_Assist.exe` (и для
      обратной совместимости `ETS2_Assist_GUI.exe` — старые сборки ещё могут быть запущены) и/или
      залочен ли файл (попытка `[System.IO.File]::Open(...,FileShare.None)`).
   2. Если залочен/запущен — **ВЫДАТЬ ПОЛЬЗОВАТЕЛЮ ЗАПРОС** (question-инструмент) закрыть
      запущенный exe (через Диспетчер задач / просто выйти из приложения) и только ПОСЛЕ
      подтверждения продолжить публикацию. НЕ перезаписывать поверх живого процесса.
   3. После успешной публикации ОБЯЗАТЕЛЬНО убедиться, что `publish\data` содержит ВСЕ папки
      (js, css, GeoJson, language, localized_cities, bin, ...) и файлы локализации
      (`data/language/ru.csv`, `en.csv`). Команда `SyncDataToPublish` логирует
      `скопировано N файлов data` — N должно быть ~390.
   4. Версия в `data/ets2_assist_build.txt` и `data/web_runtime_manifest.json` (`"build"`) ДОЛЖНА
      совпадать с `ProductVersion` собранного exe (читается из
      `bin\Release\...\publish\ETS2_Assist.exe` → VersionInfo.ProductVersion). Так как время
      сборки `Hmm` генерится в момент билда, после публикации сверить и при расхождении
      перезаписать оба файла в `publish\data` актуальной строкой.
   5. После переименования exe (сессия 28.08.2026) в папке `publish\` может остаться СТАРЫЙ
      `ETS2_Assist_GUI.exe` — после успешной публикации удалить его, чтобы не путать пользователя.

## Версионирование (НОВОЕ ПРАВИЛО, сессия 10, 28.08.2026)
- Формат строки версии: **`A.B.C.D-DESC-MM.DD-Hmm`**
  - `A` (1) — поколение проекта. Меняется редко.
  - `B` (0) — ключевой этап. Повышается ПО КОМАНДЕ при внедрении существенного функционала.
  - `C` (38) — номер текущего НАБОРА целей/проблем. НЕ меняется, пока не пофиксим набор и не
    начнём новый; тогда сбрасывается и растёт по команде.
  - `D` — ИТЕРАЦИЯ: счётчик работ по задачам из `C`. **БАМПИТСЯ АВТОМАТИЧЕСКИ (агентом):**
    +1 при завершении работы по задаче И +1 при каждом тест-билде. A,B,C — только по команде.
  - `DESC` — краткое описание проблем, решаемых в текущей `D` (обновляется агентом вместе с `D`,
    напр. `VER-FMT`, `RND-TARGETS`, `QUESTS-FIX`).
  - `MM.DD` — дата сборки (месяц.число, ведущие нули). Год НЕ указываем (не нужен).
  - `Hmm` — время сборки: час БЕЗ ведущего нуля, минуты С ведущим нулём, без разделителей.
    Пример: 9:22 -> `922`, 13:47 -> `1347`. UNIX-секунды убрали (слишком длинные).
- **НЕТ RND** в имени версии.
- `<Version>` = `A.B.C` (ЧИСТЫЙ SemVer) — только для restore. `D` НЕ в `<Version>`
  (ловушка SemVer: составная версия в `<Version>` роняет restore молча). `D` уходит в
  `FileVersion`/`AssemblyVersion` (`A.B.C.D`) и в строку `InformationalVersion`.
- Место строки версии: `BuildInfo.cs` → `BuildInfo.Version` читает `AssemblyInformationalVersion`
  (нужен `using System.Reflection;`). Строка прилетает из csproj `<InformationalVersion>`.
  `BuildInfo.Version` — в заголовке окна, логах, HTTP-заголовке `X-ETS2-Assist-Build`.
- csproj-свойства (рабочий вариант, сессия 10):
  ```
  <VersionMajor>1</VersionMajor>
  <VersionStage>0</VersionStage>
  <VersionSet>38</VersionSet>
  <VersionIter>1</VersionIter>          <!-- D: бампить при задаче/билде -->
  <VersionDesc>VER-FMT</VersionDesc>     <!-- что решаем в D -->
  <BuildDate>$([System.DateTime]::Now.ToString('MM.dd'))</BuildDate>
  <BuildTime>$([System.DateTime]::Now.ToString('Hmm'))</BuildTime>
  <DescriptiveVersion>$(VersionMajor).$(VersionStage).$(VersionSet).$(VersionIter)-$(VersionDesc)-$(BuildDate)-$(BuildTime)</DescriptiveVersion>
  <Version>$(VersionMajor).$(VersionStage).$(VersionSet)</Version>
  <AssemblyVersion>$(VersionMajor).$(VersionStage).$(VersionSet).$(VersionIter)</AssemblyVersion>
  <FileVersion>$(VersionMajor).$(VersionStage).$(VersionSet).$(VersionIter)</FileVersion>
  <AssemblyInformationalVersion>$(DescriptiveVersion)</AssemblyInformationalVersion>
  <InformationalVersion>$(DescriptiveVersion)</InformationalVersion>
  ```
- Бейджи билда (`data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`) выставлять
  в `A.B.C.D-DESC-MM.DD-Hmm` вручную (пример: `1.0.38.3-QUESTS-FIX-08.28-1015`).
- **Свойства exe (Проводник → Свойства → Подробно):** формируются из csproj АВТОМАТИЧЕСКИ при
  каждой сборке — `FileVersion`/`AssemblyVersion` = `A.B.C.D`, `ProductVersion` (и всё поле
  «Версия продукта») = полная строка `A.B.C.D-DESC-MM.DD-Hmm`. Ручных действий НЕТ: достаточно
  бампнуть `VersionIter`/`VersionDesc` в csproj перед билдом. ОБЯЗАТЕЛЬНАЯ проверка после
  каждой сборки/публикации (сверка с build.txt/manifest):
  `(Get-Item <путь\к\ETS2_Assist.exe>).VersionInfo.ProductVersion`

## Обязательные команды (всегда!)
ПОСЛЕ КАЖДОГО изменения в проекте:
- **Дополнять `MemoryAI/WORKLOG.md`**: что изменил, в каком файле, зачем, результат сборки.
- **Обновлять `MemoryAI/INSTRUCTIONS.md`** (этот файл): добавлять
  - новые задачи (TODO-список ниже),
  - планы и идеи,
  - нереализованные прототипы / наброски,
  - нерешённые проблемы и гипотезы.
Не оставлять эти файлы устаревшими — они единственный мост памяти между сессиями.

**ПРИ ЗАВЕРШЕНИИ СЕССИИ — ОБЯЗАТЕЛЬНО написать текст коммита** (см. чек-лист в разделе
«Важнаые понятия и терминлогия» → «Завершение сессии»). Без текста коммита сессия
НЕ считается завершённой. Пользователь сам выполняет `git commit`.

## Архитектура (кратко)
- **Редактор карты: фура = треугольник-стрелка (v61, 31.08.2026).** `_truckHeading`
  парсится из placement[3] (ProcessTelemetry). OnPaint: g.Save→TranslateTransform→
  RotateTransform(-heading*360)→FillPolygon #ff4d4d + белая обводка 1.5px→Restore(gstate).
  Эталон формы = миникарта (map_draw.js): нос (0,-8), база ±(5.5,7). ЛОВУШКА: g.Restore()
  без GraphicsState = CS7036 — только Restore(g.Save()).
- C# WinForms приложение `ETS2_Assist_GUI`, .NET 10, Windows.
- Мини-карта — веб-оверлей (WebView2): страница `data/web_pda_map.html`,
  JS в `data/js/` (init.js, ui.js, websocket.js, targets.js, map_draw.js, trail.js, state.js).
- Поток команд цели: кнопки `BtnRandomTarget*` (Quests/QuestsManager.cs)
  → `SendCommandToMap(...)` (broadcast по WebSocket save-сервера)
  → `web_pda_map.html` слушает `websocket.js` → `generateRandomTarget` (targets.js)
  → `state.customTargets` + `targetX/targetZ` → `updateAll()` → `drawMinimap()` (map_draw.js).
- Телеметрия ETS2: `ws://localhost:8080/api/ws/delta/flat/` (TruckTel).
- `loadCustomTargets()` вызывается каждые 2с из `websocket.js` (`setInterval(fetchHttpData, 2000)`)
  и сбрасывает `state.targetMapOverview=false` / `zoomOnMapTargets=[]` — для random-цели
  это компенсируется вызовом `focusTargetOnMap` внутри.

## Найденные и исправленные проблемы
1. Сплеш: артефакты обводки версии (path+толстые перья). → Переписан на DrawString + кольцо 2px + авто-подгонка размера. Файл `SplashForm.cs`.
2. Случайная цель не появлялась на карте:
   - Баг А: `generateRandomTarget` молча не создавал цель без `state.roads`/`state.pois`
     (кнопки 1 и 2). → Добавлен гарантированный запасной спавн около фуры.
   - Баг Б: `loadCustomTargets` не восстанавливал `targetX/targetZ`/`state.target` в ветке
     «файл содержит random». → Добавлено восстановление координат.
   Файл `data/js/targets.js`.
3. **КОРЕНЬ БАГА «точки не появлялись / list_targets видел только файловые цели» (27.08.2026):**
   `loadCustomTargets` (targets.js) читал файл через `withRnd('/custom_targets.json')` →
   порт **:8082 (статический `data/`)**, а `saveTargetsToFile()` писал через POST
   `:8083/update_targets` → порт **:8083 (mutable-файл `custom_targets.json` в AppData
   `%LOCALAPPDATA%/ETS2_Assist`)**. То есть читали и писали РАЗНЫЕ файлы. Созданные кнопками
   цели попадали в AppData-файл, а миникарта перечитывала статический — и не видела их.
   **Фикс:** `loadCustomTargets` теперь читает `http://localhost:8083/custom_targets.json`
   (тот же файл, что и пишется). Это совпадает с `АРХИТЕКТУРА ПРОЕКТА.md` (targets_read/write
   через :8083). До первой загрузки `saveTargetsToFile` защищён: если `state.customTargets`
   пуст — сначала догружает из файла, чтобы не перезаписать файл пустым массивом.

## Архитектура целей (новая, после рефакторинга 27.08.2026)
- **Единый источник истины — файл `custom_targets.json` в AppData (:8083).** Все создаваемые
  кнопками цели пишутся туда (`saveTargetsToFile` POST :8083) и перечитываются оттуда.
- **Миникарта НЕ опрашивает файл сама.** Убраны все периодические вызовы `loadCustomTargets`:
  - `websocket.js` `fetchHttpData` больше НЕ вызывает `loadCustomTargets()` (оставлена только
    телеметрия `hydrateTelemetrySnapshot`).
  - `init.js` `loadData` больше НЕ вызывает `loadCustomTargets()` на старте.
- **Перезагрузка целей — ТОЛЬКО по команде `reload_custom_targets`** (приходит в миникарту
  через `saveWs`, case в `websocket.js` → `loadCustomTargets()`).
- Когда шлётся `reload_custom_targets`:
  1. **Старт системы:** миникарта при `saveWs.onopen` шлёт `map_ready` → C#
     (`QuestsManager.OnClientCommand`) отвечает `SendCommandToMap("reload_custom_targets")`.
  2. **После создания точки:** `generateRandomTarget` (после `await saveTargetsToFile()`)
     шлёт `request_reload_custom_targets` → C# → `reload_custom_targets`.
- Кнопки случайной цели (1,2,3,4) и «Проверка точек» — как ранее (`BtnRandomTarget*`,
  `BtnCheckTargets` в `MainForm.cs` / `QuestsManager.cs`). Все 4 кнопки теперь файл-базированы.

## Ключевая задача для СЛЕДУЮЩЕЙ сессии (НОВЫЙ ПК) — РЕШЕНО (28.08.2026)
- Парадокс ×100 POI **РАЗРЕШЁН** в сессии 11 (build 1.0.38.25-POI-FIX). Тултип на новом ПК
  показывал `11960205` (×100) при корректном файле `119602.05` — значит на ПК пользователя
  читался иной `Overlays.json` с ×100. В `MapEditorForm.LoadOverlays` добавлена ЗАЩИТНАЯ
  нормализация: если ОБЕ оси POI > 1 000 000 — делим на 100 (корректный файл не трогается).
- СК POI = дороги = игра (единая). Любое «×100» ломает наложение — НЕ умножать.
- Кнопки «инвертировать v/h POI» (MapEditorForm) теперь лишние (гипотеза отвергнута) — кандидаты
  на удаление.

## Нерешённые проблемы / гипотезы
- [x] **Корень бага «точка создаётся и СРАЗУ исчезает» — НАЙДЕН И ИСПРАВЛЕН (30.08.2026,
  сессия 36, build 1.0.38.56-LOG-TARGET-FIX):** JS-dedup в targets.js слал remove_target
  по broadcast при нескольких WS-клиентах → ping-pong add_target/remove_target и цель
  пропадала. Фикс: удалён JS-dedup; запись/удаление целей — ТОЛЬКО C#-конвейер
  (AddTargetToFile c защитой от дублей questType). Это правило — ЖЁЛТИЧНОЕ: JS НИКОГДА
  не шлёт remove_target/add_target сам (кроме add_target при создании кнопкой).
- [x] **Спам логов «placement отсутствует» — ИСПРАВЛЕН (сессия 36, v56):** лог только
  смены состояния (`_teleHadPlacement` в MapEditorForm). Было 12k+ строк/день.
- [ ] **Глюк авто-зумa (подтверждён пользователем):** одна из кнопок (№2, спавн у POI на
  любом расстоянии) вызывает `focusTargetOnMap` → `targetMapOverview=true` → карта
  разом масштабируется «дальше всей карты», затем возврат к фуре. Надо либо не сбрасывать
  обзор для активной цели, либо ограничить макс. дистанцию обзора (чтобы не «больше чем вся карта»).
- [ ] Предупреждения компиляции `CS0414` (неиспользуемые поля ...) и `CS8600/8602`
  (nullability) в `MainForm.cs` — не критично, но можно почистить.

## Список задач (TODO)
- [x] **dev-mode белый даже в disabled (05.09.2026, v39.44-DEV-MODE-FONT-DISABLED):** текст
  «developer mode» всё ещё чёрный «как у кнопки Stop». КОРЕНЬ: кнопка Stop чёрная, потому что
  DISABLED. То же с devModeChk: при запущенной игре `UpdateDevModeEnabled()` ставит `Enabled=false`
  → WinForms в disabled рисует текст системным тёмным цветом, игнорируя ForeColor. ФИКС: в
  `DevModeCheckBox.OnPaint` временно `Enabled=true` перед отрисовкой (чтобы base.OnPaint рисовал
  белым), затем восстановление. Публикация v39.44 (exe=txt=manifest MATCH). Осталось верифицировать.
- [x] **dev-mode устойчиво белый (05.09.2026, v39.43-DEV-MODE-FONT-FIX):** консоль закрывается
  (1.5с v39.42 сработала), но developer mode всё ещё чёрный. КОРЕНЬ: ToolStrip (проф. рендерер)
  перебивает ForeColor хост-контрола ControlText'ом КАЖДЫЙ раз при перерисовке, поэтому
  `ForeColor=White` (при создании и в ApplyDarkTheme) не спасает. ФИКС: приватный класс
  `DevModeCheckBox : CheckBox` — OnPaint ставит ForeColor=White перед отрисовкой, затем
  восстанавливает. Поле devModeChk → тип DevModeCheckBox, создание → new DevModeCheckBox().
  Правки ApplyDarkTheme остались как защита. Публикация v39.43 (exe=txt=manifest MATCH).
  Осталось верифицировать у пользователя.
- [x] **Белый текст чекбокса dev-mode + закрытие консоли 1.5с (05.09.2026, v39.42-DEV-MODE-FONT-CONSOLE-CLOSE):**
  (1) чекбокс «developer mode» был тёмный на тёмном фоне — ПРИЧИНА: devModeChk в ToolStripControlHost
  (mainMenu.Items) не затрагивает SetControlTheme (обход only this.Controls), ToolStrip перебивает
  ForeColor. ФИКС: ForeColor=White при создании И принудительно в ApplyDarkTheme (devModeChk +
  devModeHost). (2) закрытие консоли было слишком ранним (Sleep(150) после Enter) — телепорт с
  прогрузкой ~1с, консоль закрывалась до полной телепортации. ФИКС: Sleep(1500) после Enter, затем
  PressScanCode(SCANCODE_CONSOLE) (0x29). Промежуточный Sleep(150) убран. Публикация v39.42
  (exe=txt=manifest MATCH). Осталось верифицировать у пользователя.
- [x] **Телепорт: задержки возвращены для игры (05.09.2026, v39.41-TELEPORT-DELAYS-GAME):**
  результат теста v39.40 — для РЕДАКТОРА задержки не критичны (работает без них), для ИГРЫ
  (Ctrl+T) КРИТИЧНЫ (без них ничего не работает). Возвращено в `RunTeleportGameSync`: Sleep(250)
  после снятия паузы; Sleep(250) в OpenEts2Console (после PressScanCode(0x29)); Sleep(100) после
  ввода команды; Sleep(150) после Enter. Редактор (`RunTeleportEditorSync`) ОСТАВЛЕН без задержек.
  УРОК: игра и редактор — РАЗНАЯ чувствительность к задержкам (после открытия консоли игры scan
  code 0x29 SendInput нужна пауза; редактору Win32-сообщения синхронны). Публикация v39.41
  (exe=txt=manifest MATCH). Осталось верифицировать Ctrl+T с возвращёнными задержками.
- [x] **Телепорт: убраны все задержки (05.09.2026, v39.40-TELEPORT-NO-DELAY):** после переключения
  на окно процесса (игра/редактор) и открытия консоли/Find — работа МОМЕНТАЛЬНАЯ. УБРАНО в
  `RunTeleportGameSync`: Sleep(250) после снятия паузы; Sleep(250) в OpenEts2Console (теперь сразу
  PressScanCode(0x29)); Sleep(100) после ввода команды; Sleep(150) после Enter. УБРАНО в
  `RunTeleportEditorSync`: Sleep(150) после Find clicked; Sleep(100) после Close; Sleep(100) после
  ESC. ОСТАВЛЕНО (это и есть ожидание): Sleep(25) в WaitForForegroundWindow; Sleep(30) в цикле
  ожидания окна Find. Задержки снятия паузы (2×250мс) — часть работы с паузой, не телепорта.
  Публикация v39.40 (exe=txt=manifest MATCH). Осталось верифицировать у пользователя.
- [x] **Телепорт CTRL+T: Enter scan code + закрытие консоли (05.09.2026, v39.39-TELEPORT-ENTER-CLOSE):**
  консоль ETS2 работает на SCAN CODES (открывается только scan code 0x29, виртуальная VK_OEM_3 не
  работала — сессия 10). Старый Enter через `PressKey(Keys.Enter)` (VK_RETURN) в консоли не
  срабатывал → команда оставалась введённой, консоль не закрывалась. ФИКС в `RunTeleportGameSync`:
  (5) Enter через `PressScanCode(SCANCODE_ENTER)` (0x1C) вместо `PressKey(Keys.Enter)`; (6) после
  Enter + Sleep(150) — закрытие консоли `PressScanCode(SCANCODE_CONSOLE)` (0x29, та же клавиша, что
  открывала). Константа `SCANCODE_ENTER = 0x001C` в QuestsManager.cs. Публикация v39.39
  (exe=txt=manifest MATCH). Осталось верифицировать у пользователя.
- [x] **Чекбокс dev-mode + тёмная тема + высота окна (05.09.2026, v39.38-DEV-MODE-CHECKBOX-DARK-THEME):**
  (1) dev-mode — настоящий CheckBox (`devModeHost` ToolStripControlHost + `devModeChk` CheckBox с
  текстом «developer mode»), а не ToolStripMenuItem-«кнопка»; тултип через `ToolTip.SetToolTip`
  (у CheckBox НЕТ ToolTipText). (2) Смена темы убрана: удалены `btnTheme`/`_darkTheme`/`ApplyTheme`,
  добавлен `ApplyDarkTheme` (всегда тёмная), `SetControlTheme` упрощён, `DefaultButtonColor()` →
  всегда `Color.FromArgb(60,60,60)`. (3) `MinimumSize` окна (1180, 740)→(1180, 800) — все кнопки
  (АР2 на topY+640+30) видны, resize ниже минимума запрещён. Файлы: MainForm.cs, csproj.
  Публикация v39.38 (exe=txt=manifest MATCH).
- [x] **Галочка developer mode в главном меню (05.09.2026, v39.37-DEV-MODE-MENU-FIX):** перенесена
  из редактора в MainForm.cs — `devModeMenu` (ToolStripMenuItem, CheckOnClick=true, Alignment=Right)
  справа у правого края меню (File слева), тултип `g_developer "2" g_console "1"`. Старт:
  `InitDevModeFromConfig()` (в конструкторе после RefreshUI) читает
  `Documents\Euro Truck Simulator 2\config.cfg` — активна, если там `uset g_developer "2"` И
  `uset g_console "1"` (только чтение). Переключение: `ApplyDevModeToConfig(bool)` — пишет
  `uset g_developer "2"/"0"` и `uset g_console "1"/"0"` (замена по префиксу, добавление в конец,
  создание файла). Блокировка: `UpdateDevModeEnabled()` — disabled, если запущен eurotrucks2.exe.
  **ВАЖНО (КОРЕНЬ БАГА):** строки config.cfg имеют префикс `uset ` — искать/писать ТОЛЬКО по
  `StartsWith("uset g_developer")`/`StartsWith("uset g_console")`, НЕ по `g_developer` без uset.
  Файлы: MainForm.cs (добавление), MapEditorForm.cs (удаление), csproj. Публикация v39.37
  (exe=txt=manifest MATCH).
- [x] **Консоль ETS2 scan code + Unicode + закрытие Find (04.09.2026, v39.35-CONSOLE-SCANCODE-
  UNICODE):** консоль ETS2 открывается физическим scan code 0x29 (KEYEVENTF_SCANCODE, PressScanCode/
  OpenEts2Console) вместо VK_OEM_3 (виртуальная клавиша не работала); goto вводится через
  KEYEVENTF_UNICODE (TypeUnicodeText, не зависит от раскладки — раньше «?????» на русской);
  VkKeyScan/VkKeyScanW удалены; Find закрывается после BM_CLICK (кнопка Close, fallback ESC);
  offset камеры редактора X+2/Y+4/Z; проверка foreground (GetForegroundWindow==gameHandle).
  Константы: KEYEVENTF_SCANCODE=0x8, KEYEVENTF_UNICODE=0x4, SCANCODE_CONSOLE=0x29 (QuestsManager.cs).
  Публикация v39.35 (exe=txt=manifest MATCH).
- [x] **Фикс MOUSEINPUT dx/dy (04.09.2026, v39.34-INPUT-SIZE-40-FIX):** в MOUSEINPUT поля dx/dy были
  IntPtr (8 байт) вместо int (4 байта) → INPUT=48 (вместо 40) → SendInput sent=0 → Ctrl+F и VK_OEM_3
  не срабатывали. ФИКС: dx/dy → int; убран Size=32 у INPUTUNION. Теперь INPUT=40. SendKeyboardInputs
  проверяет cbSize==40 + результат SendInput. ЛОВУШКА: MOUSEINPUT.dx/dy в Win32 — LONG (int), НЕ IntPtr.
  Публикация v39.34 (exe=txt=manifest MATCH).
- [x] **Фикс SendInput + WM_GETTEXT (04.09.2026, v39.33-INPUT-FIX-WMGETTEXT):** структура INPUT в
  QuestsManager.cs имела неверный размер union (MOUSEINPUT/HARDWAREINPUT пустые) → SendInput
  возвращал ошибку → Ctrl+F и VK_OEM_3 не срабатывали. ФИКС: INPUTUNION (Explicit, Size=32) +
  полные MOUSEINPUT/HARDWAREINPUT (размер INPUT на x64 = 40). Проверка WM_SETTEXT через WM_GETTEXT
  (GetWindowText НЕ читает текст Edit из другого процесса). SendKeyboardInputs — обёртка SendInput
  с проверкой результата (sent == inputs.Length) + логированием ошибки. PressKey/PressCtrlF/TypeText
  переведены на новую структуру и проверку результата. Публикация v39.33 (exe=txt=manifest MATCH).
- [x] **Телепорт через Win32 (04.09.2026, v39.32-TELEPORT-WIN32-FIND):** телепорт в редактор
  (Ctrl+Shift+T) переведён на Win32: TryFindEts2Window (процесс eurotrucks2 + заголовок «Map editor»)
  → WaitForForegroundWindow → PressCtrlF → ожидание окна Find (TryFindVisibleWindowByProcessAndTitle)
  → TryFindPositionEdits (label «Position» + ровно 3 Edit) → SetEditTextDirect (WM_SETTEXT X/Y/Z) →
  TryFindButtonByText («Find») → SendMessage BM_CLICK. Телепорт в игру (Ctrl+T): TryFindEts2Window
  («Euro Truck Simulator») → WaitForForegroundWindow → консоль → goto → Enter. Удалены
  FindWindowByTitle/FindGameWindowHandle. Добавлены Win32 API: GetClassName, EnumChildWindows,
  GetWindowRect, GetWindowThreadProcessId (out uint), SendMessage ×2, IsWindowVisible, RECT,
  WM_SETTEXT, BM_CLICK. ЛОВУШКИ: (1) не удалять DllImport/RECT при замене блока; (2) CS1628 —
  out-параметры нельзя использовать в лямбдах EnumWindows/EnumChildWindows (локальные переменные).
  Публикация v39.32 (exe=txt=manifest MATCH, R2R=false).
- [x] **Фиксы телепорта/кнопок/консоли (04.09.2026, v39.31-TELEPORT-FIX-DELAYS):** padding кнопок
  видимости убран (AutoSize+Padding 0, текст не обрезается); телепорт в игру вынесен в Task.Run
  (RunTeleportGameSync) — синхронные SCS pipe/REST (.Result) и SendInput больше НЕ блокируют UI
  (раньше зависало приложение); телепорт в редактор (Ctrl+Shift+T) — фокус через UI-поток
  (FocusWindowOnUi → ForceForegroundWindow, AttachThreadInput корректен только из UI-потока),
  задержки по 250 мс между ВСЕМИ шагами (в т.ч. X/Y/Z через TAB), RunTeleportEditorSync;
  цвет консоли — тёмно-серый #2D2D30 (logConsole) с v39.28 + LogConsoleOk/Error/Warn/Data.
  Публикация v39.31 (exe=txt=manifest MATCH, R2R=false).
- [x] **Сайдбар/слои/панель полей/удаление/телепорт/консоль (04.09.2026, v39.28-SDO-TELEPORT-
  CONSOLE):** слой городов поверх всех (сортировка по layer в редакторе: города=0 → последними);
  сайдбар — оранжевый фон + жирный у несохранённых в РОДНОЙ категории (StylePointNode/SidedName/
  OrderSidebarPoints); панель полей — cyan-метка `(имя_override_файла)` у переопределённых полей +
  клик по названию открывает override-файл (_fieldLabels, ApplyFieldOverrideHints/OpenFieldOverrideFile);
  удаление по load order (DeleteCurrentPoint — из последнего файла, пересборка, повтор до полного удаления);
  телепорт Ctrl+Shift+C (HOTKEY_TELEPORT): goto X;Y;Z;A;E, фокус/снятие паузы/консоль VK_OEM_3/SendInput
  TypeText, камера 6м/2м (10м/4м для курсора), ResolveTeleportTarget (высота из ближайшей точки с Y!=0);
  цвета консоли (тёмно-серый фон, LogConsoleOk/Error/Warn/Data = lime/красный/оранжевый/cyan).
  Файлы: MapEditorForm.cs, MainForm.cs, csproj. Публикация v39.28 (exe=txt=manifest MATCH, R2R=false).
- [x] **SDO: повторный парсинг + meta (display_on_map/opacity/layer) + сайдбар (04.09.2026,
  v39.27-SDO-META-OPACITY-LAYER):** повторный парсинг 75 файлов/5043 объекта (0 несовпадений
  сектора); meta.json — display_on_map (показ по умолчанию, false=скрыта для релиза/секретных),
  opacity (0..1), layer (1..9999, 0=наивысший, всегда поверх — города); C# SdoMeta
  (DisplayOnMapOf/OpacityOf/LayerOf) + OverridesPipeline PoisToJArray (payload opacity/layer/
  display_on_map); сайдбар — сортировка (грязные первыми оранжевым, затем '*', затем остальные)
  + префикс '*' у объектов с overrides (OrderSidebarPoints); подсветка грязных полей не теряется
  при повторном выборе (_dirtyFieldsByPoint + RestoreDirtyFieldHighlight); JS миникарта —
  points_overrides.js (opacity/layer/display_on_map) + map_draw.js (сортировка по слою, opacity
  через globalAlpha, скрытие display_on_map=false). Файлы: parse_raw.ps1, meta.json,
  MapEditorForm.Sdo.cs, MapEditorForm.cs, MainForm.OverridesPipeline.cs, points_overrides.js,
  map_draw.js. Публикация v39.27 (exe=txt=manifest MATCH, R2R=false).
- [ ] **SDO (Static Data Objects) — РЕАЛИЗОВАНО И ПРОВЕРЕНО (03.09.2026, v39.26-SDO-NAME-ICON-FONT):**
  парсер parse_raw.ps1 (raw\*.txt → *.json, 27 файлов/1133 объекта, 0 ошибок СК — координаты
  уже в игровой мировой СК); meta.json (name/color/icon/font_size/font_color/font_weight;
  5ka=Пятёрочка #ff0000 icons\5ka.png; «Города» жёлтые жирные); категории = имя файла без
  model_/overlay_, easter_* → «Easter eggs»; редактор (SDO-группы сайдбара, иконки 11px,
  статусы «Не сохранённое»/«Сохранённые», стили подписей); миникарта (иконки 24px);
  окно/АР2 (MinimumSize 740, кнопка 150px). Файлы: MapEditorForm.Sdo.cs (NEW), MapEditorForm.cs,
  MainForm.OverridesPipeline.cs, PointData.IsSdo, MainForm.cs, points_overrides.js, map_draw.js.
  ЛОВУШКИ: LoadSdoPoints ВНУТРИ LoadTargets (Clear!); crossgen2 AV → PublishReadyToRun=false;
  Category=display name vs ключ meta → обратный индекс.
- [ ] **НОВАЯ СТРАТЕГИЯ ДАННЫХ (03.09.2026, вводные пользователя):** единый источник точек =
  выгрузка всех точек игрового мира из редактора (с высотами). Перевод ВСЕЙ системы на эти
  данные; упрощение генерации квестов. АР → ОПЦИЯ (миникарта+оверлеи достаточно). Решить
  полную кликабельность АР и веб-оверлеев; ВСЕ диалоги на вебоверлей; АР2 — только
  визуализация точек. Полная синхронизация редактор↔миникарта↔АР. Новый UI: радиосвязь,
  анимированные плашки наград/инфо/диалогов квестов (дам-приёмники в АР1). Подробности —
  в разделе «Планы на будущее» → «НОВАЯ СТРАТЕГИЯ».
- [ ] **ВЕРИФИЦИРОВАТЬ/ЗАВЕРШИТЬ перспективную калибровку сетки АР2 (perspective warp):**
  драг 4 жёлтых хендлов → сетка меняет перспективу (homography), MouseUp фиксирует; красные
  точки исходника движутся с камерой при замороженной H. Код в ArRenderer/ArBridge/
  ArOverlayWindow (готов, ждёт runtime-теста). Применить калибровку FOV=99/PlaneOffset=−0.62
  из CURRENT_TASK.
- [x] **РЕШЕНО (v64, 1.0.38.64-LOCALE-PARSE-FIX-08.31-1111) — КОРЕНЬ ВСЕХ «МАСШТАБНЫХ»
  БАГОВ:** JValue.ToString() на ru-RU даёт «121657,32» (запятая); Invariant TryParse
  читает «,» как разделитель тысяч → числа ×1e11 мусор («вне границ карты» в редакторе,
  «приложение не прислало цель» в AR = фура «вне карты → 0 точек в 3км»,
  «произвольное вращение» стрелки, вероятно и «парадокс ×100 POI» на старом ПК).
  ПРАВИЛО: числовые JValue читать ТОЛЬКО Value<double>() (культуро-независимо);
  ToString()+TryParse — ТОЛЬКО для строковых полей из файлов (там точка).
  Проверка-паттерн: grep «ToString(), NumberStyles». Исправлено: ArTarget.ApplyPlacementJson
  (все 6 элементов), MapEditorForm.ProcessTelemetry + LoadOverlays, OverridesPipeline
  (LoadStaticPois, merge x/z). LoadStaticCities не тронут — там координаты-строки («1436.4»).
- [ ] **ВЕРИФИЦИРОВАТЬ (v64, 1.0.38.64-LOCALE-PARSE-FIX-08.31-1111):** (а) редактор —
  фура-дельтоид появляется в границах карты, стрелка по ходу движения;
  (б) AR в движении — перекрестье на ближайшей точке (в app_data: near3km>=1,
  hasTarget=True); (в) AR на паузе — замирает, оживает после снятия;
  (г) workflow-лог БЕЗ данных, данные → Logs/app_data.log
  ([AR] placement/tick, [EDITOR][TELEMETRY][DROP]).
- [ ] **Крэш редактора при ПЕРВОМ старте (v61, не сделано):** Overflow error в
  g.DrawPath(_roadsPath) (OnPaint, MapEditorForm ~755) — белый фон, красный крест;
  исчезает при повторном открытии. Вероятно экстремальная матрица/координаты дорог
  при первом построении. План: culling сегментов в BuildRoadsPath, clamp масштаба,
  try/catch вокруг DrawPath (проглатывать paint-исключение, перерисовать после готовности).
- [ ] **Галочка «Показать в AR» в редакторе (v61, не сделано):** PointData.ShowInAr
  (int 0/1) + Fields (Group «AR») + FieldJson + ApplyJObjectToPoint + PointDataToJObject;
  BuildFieldControls уже строит CheckBox для bool — но поле int: использовать ValueType
  bool c конверсией в OnFieldChanged/ReadPanelIntoPoint ИЛИ сразу bool ShowInAr.
  Фильтр в MainForm.ArTarget.RefreshArModel: НЕ показывать точки с ShowInAr==0
  (только отмеченные летят в AR). ar_target остаётся «ближайшая из отмеченных».
- [ ] **AR не показывает цель/«приложение не прислало цель» (сессия 41):** проверить у
  пользователя: wsPort в web_data.json; лог [AR] «Модель точек обновлена: N»; если N=0 —
  причина в статики/overrides; после внедрения «Показать в AR» подбор станет по отмеченным.
- [ ] **ВЕРИФИЦИРОВАТЬ у пользователя (сессия 40, 30.08.2026, сборка 1.0.38.60-AR-C-TARGET):**
  AR — DUMB-RECEIVER (требование пользователя): страница не грузит точки; приложение само
  подбирает ближайшую (MainForm.ArTarget.cs: свой WS-клиент телеметрии, копия модели
  конвейера, рассылка ar_target + ar_telemetry 5 Гц через WS 8084; город/POI ≤3 км,
  цели без лимита). Проверить: перекрестье на ближайшей точке, на паузе замирает,
  прижим к краю + стрелка; точность проекции — CFG.fovDeg (75) в ar_hud.js.
  **ПРАВИЛО ТЕЛЕМЕТРИИ (урок 30.08.2026): placement существует ТОЛЬКО в WS-дельте и в
  окрестности движения (на паузе не шлётся); REST /flat/* его НЕ отдаёт. Порт берём
  из web_data.json (обновляет приложение), НЕ хардкодим 8080.**
- [x] **ВЕРИФИЦИРОВАНО у пользователя (сессия 32, 29.08.2026):** hex-ID загружается в поле
  «Системное имя» без искажений; заглавные в GameName переводятся в строчные (не удаляются);
  санитайзер исправлен, тест-сборка 1.0.38.50-GAMEID-SANITIZE успешна.
- [ ] **ВЕРИФИЦИРОВАТЬ у пользователя (сессия 34, 30.08.2026, сборка 1.0.38.52-OVR-PIPELINE):**
  НОВЫЙ КОНВЕЙЕР overrides. Приложение собирает effective-состояние (статика + overrides +
  test_targets) и шлёт ГОТОВЫЙ пакет `map_overrides_data` (миникарта = dumb-receiver, ничего
  не merge сама). Проверить: статусные строки (редактор+миникарта, клик в редакторе → логи);
  перемещение города/POI в редакторе → сохранение → новая позиция на миникарте; тестовые
  кнопки пишут в `map_overrides\test_targets.json` (custom_targets.json более не пишется);
  тоггл «Показать карту»: ВКЛ = карта всегда видна, ВЫКЛ (по умолчанию) = обычная логика.
  Код: MainForm.OverridesPipeline.cs (NEW), MainForm.PointsOverrides.cs (сокращён),
  QuestsManager.cs (file ops → TestTargetsFile), data/js/points_overrides.js (переписан),
  data/js/status_bar.js (NEW), websocket.js (map_overrides_data + minimapAlwaysOn),
  UI/EditorStatusBar.cs (NEW), WebUIManager.cs, MainForm.cs (тоггл).
- [ ] **ВЕРИФИЦИРОВАТЬ у пользователя (сессия 37, 30.08.2026, сборка 1.0.38.57-QUEST-PAUSE-DLG):**
  (а) пауза перед КАЖДЫМ квест-диалогом (только если НЕ на паузе; при сбое — ретрай PAUSE);
  (б) задержка 2с между паузой и диалогом; (в) награды в диалоге — ОТДЕЛЬНЫЙ блок,
  зелёный жирный (QuestDialogForm: _messageLabel + _rewardsTable c Label на каждый элемент);
  (г) после закрытия диалога фокус в окно игры (ReturnFocusToGame в finally).
- [~] **ВЕРИФИЦИРОВАНО ПОЛЬЗОВАТЕЛЕМ (30.08.2026, по v56):** «Точки добавляются. Даже триггеры
  у них срабатывают идеально» — пинг-понг целей устранён, квест-триггеры работают.
- [ ] **ВЕРИФИЦИРОВАТЬ у пользователя (сессия 36, 30.08.2026, сборка 1.0.38.56-LOG-TARGET-FIX):**
  (а) кнопки Курьер/Тайник/Перекус: точка появляется и ОСТАЁТСЯ на карте (фикс ping-pong:
  JS не шлёт remove_target, удаление только через C#-конвейер); (б) лог при паузе игры —
  без потока «placement отсутствует» (анти-спам в MapEditorForm.ProcessTelemetry,
  лог только смены состояния); (в) цели в редакторе: дубли random-целей как POI «custom»
  больше не создаются (ApplyOverrideFiles 3b); (г) load_order.txt нормализуется если
  перепутан (test_targets.json последним).
- [ ] **Правило пайплайна (закрепить):** JS-страницы НИКОГДА не шлют add_target/
  remove_target в C# сами (add_target — только при создании цели кнопкой). Все удаления
  / кулдауны / изменения файла - исключительная зона C# (MainForm.OverridesPipeline.cs
  + QuestsManager.AddTargetToFile). Нарушает исход "двум страницам» = ping-pong.
- [ ] **ВЕРИФИЦИРОВАТЬ у пользователя (сессия 35, 30.08.2026, сборка 1.0.38.53-STATUS-MAP-FIX):**
  (а) редактор карты НЕ падает при открытии (фикс крэша MeasureString в EditorStatusBar —
  try/catch + клон Font + SafeMeasure); (б) миникарта рисует точки/грузовика СРАЗУ (фикс
  ReferenceError: безопасные typeof-вызовы getEffectivePoiList/getEffectiveCityList в
  map_draw.js — гонка с map_ready); (в) OllamaLimits.exe (НОВЫЙ проект F:\repo\ollama_limits,
  standalone 49.6МБ): трей-иконка Session/Weekly usage с ollama.com/settings, окно входа
  WebView2 при редиректе, пульс >95%, клик → браузер, ПКМ → Выход.
- [ ] **Проверить у пользователя:** видна ли теперь точка случайной цели (гарантированная
  отрисовка `randomTarget` в map_draw.js + кнопка №4 «Ближайшая цель» 51-60м на дороге).
- [ ] **Исправить глюк авто-зумa:** ограничить макс. дистанцию обзора в `focusTargetOnMap`
  (чтобы карта не масштабировалась «дальше всей карты») и не сбрасывать `targetMapOverview`
  для активной цели в `loadCustomTargets` (каждые 2с).
- [ ] Проверить, что логи цели пишутся (создание + дистанция; кнопка «Проверка точек»).
- [ ] (опционально) Почистить неиспользуемые поля/`nullability`-предупреждения в MainForm.cs.
- [ ] (исследовать) Перенести рисование ВСЕХ целей на надёжный источник, а не `state.customTargets`.
- [ ] **ВЕРИФИЦИРОВАТЬ ИСПРАВЛЕНИЯ СЕССИЙ 1-9 В РАНТАЙМЕ (у пользователя):**
  - Пауза/оверлей: карта и гибрид СКРЫВАЮТСЯ на паузе ETS2; `web_pause_logo` показывается;
    хоткей CTRL+SHIFT+S не висит 4с (подтверждение паузы срабатывает). Код готов
    (MainForm `_pausedIntent` + `ParsePausedResponse`, WebUIManager `IsGamePausedAsync`,
    скрытие map/hybrid на паузе), НЕ проверено на практике.
  - Replay-анимация миникарты: `SetUiSync` теперь шлёт `show_ui` (не `show_ui_first`) —
    анимация играет ОДИН раз, не каждые ~3с. НЕ проверено.
  - Двойная случайная цель: `map_draw.js` больше не рисует `randomTarget` дважды
    (пропуск в циклах inactive/active). НЕ проверено.
  - Все 4 кнопки случайной цели идут через file-write (`add_target` → `AddTargetToFile`) —
    подтверждено в коде, НЕ проверено в рантайме.
- [x] **ВЕРСИОНИРОВАНИЕ (новое, сессия 10, уточнено):** строка `A.B.C.D-DESC-MM.DD-Hmm`.
  A,B,C — только ПО КОМАНДЕ (C=38 = текущий набор задач). D (итератор) бампит САМ агент
  при завершении задачи и при каждом тест-билде. UNIX-секунды и год убраны (слишком длинные);
  время `Hmm` (час без нуля, минуты с нулём). Собрано `1.0.38.3-QUESTS-FIX-08.28-1015`.
- [ ] **ВЕРИФИЦИРОВАТЬ КВЕСТ-КНОПКИ/ТРИГГЕРЫ (сессия 10, доп) у пользователя:** 3 кнопки
  (Курьер-забор/Тайник/Перекус) + тоггл Обзор; вход в зону → нужный диалог; завершение по
  кнопке → награда/удаление; Курьер ведёт к фиолетовой точке выдачи; метка вне карты у стрелки;
  выход из зоны → красная. Флапинг WS при старте — воспроизводится ли ещё.
- [ ] **КВЕСТЫ (пользователь, этапы):** менеджер квестов (база реализована в сессии 10, доп):
  - **Супер-задача:** квест «Курьер» (забор→выдача) — реализован; успех → повысить B, новый цикл C/D.
  - **Премиум-задача:** квест «Попутчик» (СВЕРХ реализованного — ещё не сделан).
  - Дальше развивать функционал случайных целей по фидбеку пользователя.

## Нереализованные прототипы / идеи
- (пока нет зафиксированных; добавлять сюда наброски и отложенные правки)

## Планы на будущее (отложенные задачи)

### 🔄 НОВАЯ СТРАТЕГИЯ (03.09.2026, вводные пользователя — важный поворот) ⚠️
**Открытие пользователя:** из РЕДАКТОРА игры можно выгрузить ЛЮБЫЕ точки игрового мира
с координатами, В ТОМ ЧИСЛЕ С ВЫСОТОЙ. Это полностью меняет подход к наполнению параллельной
игровой вселенной и к генерации квестов (алгоритмы существенно упрощаются).

**Что это значит для архитектуры:**
1. Будет собран МАССИВ ВСЕХ точек в ЕДИНОЙ системе координат, включая высоты. ВЕСЬ проект
   переводится на эти данные (единый источник точек = выгрузка из редактора).
2. **АР становится ОПЦИЕЙ, а не обязательным инструментом.** Для успешной работы с квестами
   достаточно МИНИКАРТЫ + доп. оверлеев. АР2 даёт лишь более точное пространственное
   ориентирование (для игроков, кто включит АР).
3. Решить ПОЛНОСТЬЮ проблему кликабельности АР и веб-оверлеев.
4. Переработка приложения вебоверлей: ВСЕ диалоги перевести на вебоверлей. **АР2 используется
   исключительно для ВИЗУАЛИЗАЦИИ точек в пространстве** (не для интерфейса).
5. ПОЛНАЯ синхронизация отображения точек: редактор ↔ миникарта ↔ АР.

**Новые элементы интерфейса (всё — дам-приёмники в АР1, контент генерит основное приложение):**
- Радиосвязь — список поступающих сообщений.
- Красивые плашки с анимацией: получение наград, информационные плашки, диалоги квестов.
- Приложение принимает обратно выбор пользователя в диалогах.

**Отложенная задача (ТЗ будет позже):** механизмы поиска и фиксации поинтеров памяти для
изменения игровых данных, нужных для квестов: деньги, опыт, повреждения и т.п.

**Выгрузка данных из редактора (пользователь начал сейчас):** города, заправки, парковки
для отдыха, ДПС, точки питания и покупок, НПЦ по профессиям/типу, отели, беседки,
Пятёрочки/Магниты/Ленты, шавермы, СТО и автозапчасти, леса, водоёмы.
Затем — массив РУЧНЫХ статичных точек: места отдыха/досуга/проживания, НПЦ, тайники,
ролевые и сюжетные события, сбор грибов, перевалы.

- **[В ПЛАНЫ] Полный отказ от legacy custom_targets.json (сессия 34 — выполнено для целей
  тестовых кнопок).** НОВЫЙ принцип (реализован): единый конвейер
  `MainForm.OverridesPipeline.cs` — приложение единственный владелец всех файлов точек
  (`map_overrides\*.json` по load_order + `test_targets.json`); миникарта и редактор
  получают/строят effective-state через один и тот же merge-код (`ApplyJObjectToPoint`).
  Остаточная legacy: старый `custom_targets.json` в AppData больше не используется
  тестовыми кнопками (остаётся только чтение статических целей редактора — см.
  `MapEditorForm._targetsFile` → при полной чистке перевести и его на test_targets).
- **[В ПЛАНЫ] Чистка статических целей редактора**: `MapEditorForm.LoadTargets` всё ещё
  читает legacy `custom_targets.json` как «статические точки» — перевести на
  test_targets.json/overrides и убрать дифференциацию «статические/ользовательские».
- **[В ПЛАНЫ] Чистка корня проекта от патч-ноутов в формате `.md` (второстепенная задача, 27.08.2026).**
  Изучить содержимое ВСЕХ `*.md` в корне проекта (кроме `АРХИТЕКТУРА ПРОЕКТА.md` и `README.md`,
  а также памяти `MemoryAI\`), кратко резюмировать проделанную работу: только ПОЛЕЗНОЕ, удачные
  исправления; ОСОБОЕ внимание — сформированные пользователем задачи, которые в итоге были
  забыты/не сделаны; искать успешные реализации задач. Свести всё в ОДИН общий файл
  **`Отработано ChatGPT`** (создать в корне проекта). Кандидаты (корень, исключая
  архитектуру/readme/MemoryAI): `MINIMAP_ARCHITECTURE_1.0.18.md`, `MINIMAP_ARCHITECTURE_1.0.19.md`,
  `MIGRATION_2026-08-25.md`, `WEB_RUNTIME_SYNC_1.0.24.md`, `VERSION_FIX.md`, `PATCH_INSTRUCTIONS.md`,
  `RELEASE_NOTES_1.0.30.md`, `RELEASE_NOTES_1.0.31.md`, `RELEASE_NOTES_1.0.32.md`,
  `PATCH_1.0.20_NOTES.md`, `PATCH_1.0.21_NOTES.md`, `PATCH_1.0.22_NOTES.md`, `PATCH_1.0.23_NOTES.md`,
  `PATCH_1.0.29_NOTES.md`, `BUILD_1.0.15_NOTES.md`, `SPLASH_REGRESSION_FIX_1.0.29.md`,
  `SPLASH_FOCUS_FIX_2026-08-25.md`, `FULL_SYNC_1.0.25.md`, `FULL_REFRESH_1.0.26.md`,
  `FULL_REFRESH_1.0.27.md`, `PUBLISH_DATA_RULE.md` (плюс `data/MAP_RUNTIME_FIX.md`).

## Заметки по стилю работы с пользователем
- Отвечать на русском языке.
- Пользователь ценит конкретные правки + сборку + краткий отчёт, а не просто теорию.
- Память между сессиями ведётся вручную в `MemoryAI/`.

### [v65 31.08.2026] AR-MIRROR-SEND-ONCE — опубликовано
- ar_target РАЗОВО (смена цели / форс-ресет: StartArTargetFeed, RefreshArModel, SendMapOverridesToMap). Телеметрия 30 Гц (Timer 33мс). Модель = record ArPoint(...category, color). Payload: category+color.
- Зеркало: редактор RotateTransform(-h*360) без +180; JS fwd=(sin,cos), right=(-cos,sin). Конус обзора ±35° (heading+headYaw).
- ar_hud.js CFG: minSize=30 maxSize=96 sizeNearDist=10 sizeFarDist=500 fadeDist=1500. За спиной → стрелка снизу. rAF ~60FPS.
- МАШИННОЕ ПРАВИЛО (подтверждено v64): числовые JValue — ТОЛЬКО .Value<double>(); ToString+TryParse(InvariantCulture) на ru-RU = запятая → ×1e8 мусор.

### TODO (не завершено)
- [ ] ВЕРИФИЦИРОВАТЬ v65 у пользователя: зеркало стрелки/AR-проекции, конус с головой, цвет/размер/фэйд метки, стрелка снизу для цели сзади, разовая рассылка (в логе ar_target только при смене).
- [ ] Краш первого старта редактора: Overflow error в g.DrawPath(_roadsPath) — план: клиппинг в BuildRoadsPath + try/catch DrawPath.
- [ ] Чекбокс «Показать в AR» (PointData.ShowInAr → Fields/FieldJson/ApplyJObjectToPoint/RefreshArModel filter).
### [v65 31.08.2026] AR-MIRROR-SEND-ONCE — опубликовано
- ar_target РАЗОВО (смена цели / форс-ресет: StartArTargetFeed, RefreshArModel, SendMapOverridesToMap). Телеметрия 30 Гц (Timer 33мс). Модель = record ArPoint(...category, color). Payload: category+color.
- Зеркало: редактор RotateTransform(-h*360) без +180; JS fwd=(sin,cos), right=(-cos,sin). Конус обзора ±35° (heading+headYaw).
- ar_hud.js CFG: minSize=30 maxSize=96 sizeNearDist=10 sizeFarDist=500 fadeDist=1500. За спиной → стрелка снизу. rAF ~60FPS.
- МАШИННОЕ ПРАВИЛО (подтверждено v64): числовые JValue — ТОЛЬКО .Value<double>(); ToString+TryParse(InvariantCulture) на ru-RU = запятая -> x1e8 мусор.

### TODO (не завершено)
- [ ] ВЕРИФИЦИРОВАТЬ v65: зеркало стрелки/AR-проекции, конус с головой, цвет/размер/фэйд метки, стрелка снизу для цели сзади, разовая рассылка.
- [ ] Краш первого старта редактора: Overflow error в g.DrawPath(_roadsPath) - план: клиппинг в BuildRoadsPath + try/catch DrawPath.
- [ ] Чекбокс «Показать в AR» (PointData.ShowInAr -> Fields/FieldJson/ApplyJObjectToPoint/RefreshArModel filter).
### [v66+v67 31.08.2026] AR-VERTICAL-SMOOTH / HEAD-PITCH-FIX — опубликовано
- v66: ОТКАТ зеркала проекции v65 (fwd=(-sin,-cos), right=(cos,-sin) — КАК НА МИНИКАРТЕ; v65 «зеркало» было ошибкой 180°). Камера=глаз (placement[1]+1.9м), метка ground-якорь (target.y+0.5м). Экстраполяция камеры+lerp позиции. Конус редактора ×0.5 (17.5°). Офлайн: стрелка+конус серые, НЕ удаляются.
- v67: headPitchSign=+1 (был -1; фидбек «движется вместе с головой» = двойная инверсия). head.offset[4]=pitch (доля оборота; пример: -0.0357=-12.9°). runtimeDebug: HeadPitch.
- Если знак pitch снова не тот — CFG.headPitchSign в ar_hud.js (±1); калибровка по статус-строке AR «pitch N°».
- TODO с прошлых сессий (актуально): краш DrawPath первого старта редактора; чекбокс «Показать в AR».
### [v68 31.08.2026] AR-ROUND-DIM — опубликовано (exe=txt=manifest 1.0.38.68-AR-ROUND-DIM-08.31-1234)
- AR метка КРУГ; размеры -30% (min 21 / max 67); перекрестье масштабируется как метка (*0.7).
- drawOutlinedText(text,x,y,font,color,alpha): обводка тоже прозрачнеет (фикс «чернеющего» текста).
- Питч кузова учитывается в projectPoint (placement[4]*2π + headPitch); знак кузова = TrailSaver.
- Пороги без изменений: 10м/500м/1.5км. Минимум globalAlpha 0.12 (никогда не исчезает полностью).
### МАШИННОЕ ПРАВИЛО ЛОГОВ (пользователь, 31.08.2026 — «хватит спамить»)
- workflow-лог (AppendLog/LogEditor): ТОЛЬКО шаги и переходы состояния (1 строка на событие).
- ПОТОКОВЫЕ/ВЫСОКОЧАСТОТНЫЕ данные (ar_telemetry 30Гц, координаты, кадры, периодические команды) — ТОЛЬКО app_data (Logger.Data/LogEditorData). См. IsStreamingCommand() в MainForm.SendCommandToMap.
- Проверка перед коммитом: нет ли AppendLog/LogEditor внутри таймеров/циклов/частых тиков.
### [v70+v71 31.08.2026] AR-PIN-HEIGHT / PIN-CROSS-VIEWCONE — опубликовано
- AR: подписи realName/gameName; высоты Y=0 по городам (hCityDist 350 / hLockDist 50, фиксация в _yLock Map); cityYCorrection=-40 (все города выше земли на ~40м — переменная CFG.cityYCorrection в ar_hud.js); fps x2 (~120 расчетов/с, double-step lerp), FPS в статусе; метка круг min15/max33; вне экрана только стрелка+текст.
- «Поставить в АР» = Shift+Ctrl+X (кнопка удалена); OpenCreateAt(x,z,y,fromArPin) в редакторе (один экземпляр: старое окно ЗАКРЫВАЕТСЯ и открывается новое — правило и для кнопки «Редактор карты»); ar_pin {active,x,y,z}; клик по пустому месту тоже помечает.
- Конус обзора на миникарте: VIEW_CONE_LEN_M=350, VIEW_CONE_HALF_ANGLE_DEG=17.5 (переменные в map_draw.js — править здесь).
- ЛОГИ: [OVR][DEBUG] → app_data (Logger.Data). ПРАВИЛО: потоки/данные/файлы — app_data, workflow — только события.
### МАШИННОЕ ПРАВИЛО Session usage (пользователь, 31.08.2026; обновлено 02.09.2026)
- ПЕРЕД любой задачей и ПОСЛЕ: проверить Session usage %.
- Инструмент (НОВОЕ): читать файл `MemoryAI\ollama_parsed_usage.txt` — строка 1 = процент
  (с запятой), строка 2 = время до ресета. Файл обновляет расширение VS Code каждые 5 сек;
  бат-файлы (`ollama_usage_v2.bat` / `ollama_login.bat`) БОЛЬШЕ НЕ используются.
- Процент В TODO первой задачей; после публикации в резюме — графический прогрессбар [████----] NN.N%.
- Если файл пуст/отсутствует — спросить процент у пользователя в конце сессии.
### Session usage (ollama) — РАБОТАЕТ (31.08.2026; обновлено 02.09.2026)
- Инструмент (НОВОЕ): файл `MemoryAI\ollama_parsed_usage.txt` (строка 1 = процент с запятой,
  строка 2 = время до ресета). Обновляется расширением VS Code каждые 5 сек.
- Бат-файлы (`ollama_usage_v2.bat` / `ollama_login.bat`) БОЛЬШЕ НЕ используются для проверки лимитов.
- Агент: % первой задачей в TODO; после публикации прогрессбар [████░░░] в резюме. Если файл пуст — спросить процент у пользователя.
### [v73 31.08.2026] Первостепенные (все применены) + вторичные
1. Дороги миникарты: MIN_ROAD_WIDTH=3.5 (=шлейф) — state.js roadWidthStyles ×2.
2. Рывок высоты AR раз/с КОРЕНЬ: RefreshArModel обнулял _arLastSentGameName каждую секунду → ar_target пересылался → ar.target пересоздавался. Фикс: сигнатура _arModelSig (имя+коорд F1) — ресет ТОЛЬКО при реальном изменении; «Модель точек обновлена» в workflow только при изменении, иначе app_data.
3. ЕДИНЫЙ радиус AR 1500м (было 3км для городов); reason «нет точек в радиусе 1.5 км»; плашка AR показывает этот текст при пустой цели; fade txtAlpha теперь на ВЕСЬ блок (метка+крестик+стрелка+текст, ctx.save/globalAlpha/restore, <0.03 — не рисуем).
4. Pin: PinMaxDistM=1500; взгляд выше горизонта/параллельно → сразу 1500м (не 500).
5. Города при выборе AR-целью: kind='city' тоже проходит displayYFor (компенсация -44).
6. Конус миникарты: LEN 15м базово, вверх → до 40, вниз → до 5 (head.offset[4]: +вверх/-вниз, k=pitch*10).
7. Shift+Ctrl+X: ТОЛЬКО пометка АР + ar_pin_map (кружок+крест на миникарте, state.arPinMap); редактор НЕ открываем/НЕ фокусируем. GetArPin ещё используется.
8. Спам AR: см.п.2; GetCandidatePorts → LogEditorData (app_data).
9. Logs\new_object_po_selections.txt (3 строки: город+дист / JSON {x,y,z} / пустая) — LogNewPointSelection: клик-создание в редакторе + pin из АР.
10. КВОТА: >90% Session usage — СПРАШИВАТЬ подтверждение перед задачей.

### [v1.0.40.27 12.09.2026] AR1-REVIVE — реанимация AR1 + горизонт/дистанция/конус/чекбоксы
- **Единый набор комбинаций (НОВОЕ ПРАВИЛО):** `SHIFT+CTRL+X` — только редактор карты;
  `CTRL+X` — ИГРОВОЙ режим (новая точка): AR-пометка (`ar_pin`) + миникарта (`ar_pin_map`)
  + `MapEditor2Form.CreatePointFromEditor(x,y,z)` БЕЗ активации окна (фокус остаётся в игре).
  `CTRL+PGUP/PGDN` — FOV AR1 ±1° (30..150, хранится в `AppSettings.Ar1FovDeg`,
  рантайм — `ArBridge.FovDegreesAr1`, ОТДЕЛЬНО от FOV AR2 `FovDegrees`).
- **AR1 = внешний процесс `WebOverlay`** (`MainWindowTitle` содержит «AR HUD»).
  Кнопка AR1 — тумблер: `SyncAr1Button()` (опрос процессов раз в 2 с в `statusTimer`),
  `ToggleArOverlay()`, `StopArOverlay(manual)`, `IsAr1Running`. `StopSystem` гасит AR1.
- **ПРАВИЛО ТЕЛЕМЕТРИИ AR (урок v1.0.40.27):** `StartArTargetFeed()` обязан форсить
  `_arTruckChanged=true`, `_arCitiesSent=false`, `_arLastHeadSent=null`;
  `head` НЕ должен входить в гейт отправки (смена головы — отдельный триггер через
  `_arLastHeadSent` + `JToken.DeepEquals`); на подключение нового WS-клиента
  (`TrailBehavior.SetOnClientConnected` → `OnOpen`) — `ForceArDataResend(reason)`.
  Иначе «AR: нет телеметрии», когда грузовик стоит (нет события изменения).
- **ЕДИНАЯ ФОРМУЛА КОНУСА ОБЗОРА = ДИСТАНЦИЯ ДО ЗЕМЛИ ПОД ПРИЦЕЛОМ:**
  `eyeH / |tan(pitch)|`, `eyeH = 1.5 м` (`MapEditorForm.EyeHeightActrosM`), clamp ≤ 1500 м
  (`ViewConeMaxDistM`). Питч везде — ДОЛЯ ОБОРОТА (×2π). Реализации:
  `MapEditorForm.GroundDistanceFromPitchM` (старый редактор → `MapEditorVectorRenderer.DrawCone`),
  `map_draw.js` (миникарта), `map_editor2/index.html` `groundDistFromPitchM`,
  `ar_hud.js` `CFG.eyeHeightM`.
- **Линия горизонта (AR1 + AR2):** `ar_hud.js horizonScreenLine` / `ArRenderer.ComputeHorizonLine` —
  проекция двух точек плоскости горизонта (вперёд 500 м, ±500 м вбок) с композитным
  питчем (кузов + голова) и **КРЕНОМ кузова** (`Roll`, знак инвертирован) вокруг оси взгляда.
- **Выше горизонта (пункт 8):** `dirY = sin(pitch) ≥ -1e-6` → дистанция `NaN`,
  подписи дистанции НЕТ, на микроточке прицела — СЕРЫЙ ДИАГОНАЛЬНЫЙ микрокрестик
  (`drawDiagCross` / `ArRenderer.DrawDiagCross`; в AR2 раньше рисовался текст «—»).
- **Чекбоксы «Показать в AR» / «Показать на миникарте»** (`PointData.ShowInAr` / `ShowOnMap`,
  по умолчанию `true`): JSON-ключи `showInAr` / `showOnMap`. Читать через
  `MapEditorForm.ReadBoolToken` (`internal`, принимает bool/int/float/"true"/0/1 — JS
  редактора-2 пишет bool как `1/0`). Миникарта — dumb-приёмник: `showOnMap` СВОРАЧИВАЕТСЯ
  в существующий флаг `hidden` (`hidden = Hidden==1 || !Enabled || !ShowOnMap`).
  AR-канал фильтрует по `ShowInAr`. **Решение пользователя: строгий B+** — точка без
  галочек не показывается НИ в AR, НИ на миникарте.
- Хелперы, используемые из других классов проекта, объявлять `internal` (иначе CS0103).
- Минифицированный inline-JS (`map_editor2/index.html`) после правок ВСЕГДА проверять:
  извлечь `<script>` и прогнать `node --check`.

### [v1.0.40.28 15.09.2026] WEB-DEBUG-CAM — AR1-телеметрия, камера, конус=FOV, debugShow
- **КОРНЕВОЙ УРОК (событийная рассылка):** флаг-«ЗАПРОС» и флаг-«данные изменились» должны
  быть РАЗНЫМИ переменными. `ApplyPlacementJson` ставит `_arTruckChanged` при каждом
  входящем пакете (WS + REST 1 Гц) и перетирал форс-запрос до тика (33 мс) → `ar_telemetry`
  не уходила ВООБЩЕ (0 отправок за сессию; симптом — «AR1 без визуальных изменений,
  как будто старый код»). Решение: залипающий `_arTelemetryForced`, сбрасывается ТОЛЬКО
  тиком после отправки.
- **КАМЕРА СЧИТАЕТСЯ ОТ ТЕЛЕМЕТРИИ** (не от констант): `truck.cabin.position` +
  `truck.head.position` (ЛОКАЛЬНЫЕ, метры в системе фуры), `truck.head.offset` (yaw/pitch,
  доля оборота), `truck.cabin.offset`. Положение камеры = опорная точка фуры + локальная
  ВЫСОТА (cabin.y + head.y ≈ 2.55 м у Actros). Горизонталь — `CFG.camForwardM`/`camRightM`
  (0, уточняется замером через debugShow-строку).
- **КОНУС ОБЗОРА = УГОЛ FOV:** полуугол = `FOV/2` (минимум 5°) ВЕЗДЕ — `ar_hud.js`,
  `map_draw.js` (FOV из `state.fovDeg` ← команда `ar_fov`), `map_editor2/index.html`
  (`coneFovDeg()` ← `window.MapEditor2SetFov` ← `MapEditor2Form.SendFovAsync`).
  **НЕ считать полуугол от питча** (прежнее `clamp(|headPitch*360|,5,45)` — ошибка).
  Длина конуса — прежняя единая формула `eyeH/|tan(питч)|` ≤1500 м.
- **ВЫВОД FOV:** `ar_hud.js drawFovText` — левый нижний угол, двойная чёрная обводка
  (`CFG.fovFont/fovColor/fovMarginX/fovMarginY`).
- **debugShow(bool) — ЕДИНЫЙ МЕТОД ДЛЯ ВСЕГО ВЕБ-КОНТЕНТА** (`data/js/debug_show.js`):
  подключать ПЕРВЫМ в `<head>` КАЖДОЙ страницы (13 шт., включая `map_editor2/index.html`
  и диагностические). `true` — игнорировать логику показа, контент виден принудительно
  (CSS `.debug-show-force` + заморозка `classList`/стилей + MutationObserver + дозатор
  500 мс, инлайн-стили запоминаются и восстанавливаются), `false` — возврат.
  Входы: `window.debugShow(bool)`, URL `?debugshow=1`, WS `{command:'debug_show',enabled}`;
  UI — чекбокс «Отладка веб (debugShow)» (`chkWebDebug`), `ArBridge.DebugShow` для AR2.
- **РЕДАКТОР КАРТЫ 1 ОТКЛЮЧЁН** (решение пользователя: «редактор 2 — единственный»):
  кнопка «Редактор карты» и поле `btnMapEditor` УДАЛЕНЫ; якорь сетки кнопок — `btnStart`.
  `MapEditorForm` остаётся legacy (его статики использует конвейер overrides).
  `SHIFT+CTRL+X` НЕ открывает/не фокусирует редактор и НЕ берёт координаты через
  `MapEditor2GameEditorBridge` — обе комбинации (`CTRL+X`, `SHIFT+CTRL+X`) создают
  **ВРЕМЕННЫЙ (виртуальный) маркер**: AR1 (`ar_pin`) + миникарта (`ar_pin_map`).
  Сохранить такую точку нельзя — для постоянной нужно открыть редактор и поставить там.
- **ТОЧКА ИЗ РЕДАКТОРА 2 → В AR1:** `MapEditor2Form` при `map2-create-point` зовёт
  `MainForm.ArPlacePinAtWorld(x,z)` + `ArSendPinMap(x,y,z)` (AR1 + миникарта одной точкой).
- `truck.*`-массивы читать через `Value<double>()` (культуро-независимо; урок v64).

### [v1.0.40.29 15.09.2026] CACHE-TICK-FIX — два корня «в АР1 ноль изменений»
- **ПРОФИЛЬ WEBVIEW2 ОВЕРЛЕЯ ЛЕЖИТ В `publish\data\bin\WebOverlay.exe.WebView2\EBWebView`**
  (рядом с его EXE, ДВА уровня ниже `publish`, ~34 МБ). Старый `compile.ps1` искал кэш
  через `Get-ChildItem -Filter '*.WebView2' -Directory` **без `-Recurse`** → эта папка не
  попадала НИКОГДА, и `web_ar_hud.html` грузился из кэша («ноль изменений» при живых
  правках и работающей миникарте). Искать РЕКУРСИВНО `*.WebView2` И `EBWebView`, удалять
  глубокие раньше родителей, гасить `WebOverlay` перед очисткой (держит локи), + профиль
  `%APPDATA%\ETS2_Assist\EBWebView`.
- **compile.ps1 САМ ПРОВЕРЯЕТ ДОСТАВКУ:** Stage 3b — MD5 веб-контента (`.html/.js/.css/.json/`
  `.ico/.png/.svg`) из `data\` против `publish\data` + размеры остальных файлов (хешировать
  564 МБ = два EXE по 166 МБ дорого), печатает `PUBLISH DELIVERY CHECK FAILED` при
  расхождении. Stage 3c — подтверждает, что папок `*.WebView2`/`EBWebView` не осталось.
  При жалобе «старый код в билде» СНАЧАЛА смотреть эти две строки вывода compile.ps1.
- **AR-ТИК — ПОТОКОВЫЙ ТАЙМЕР.** `System.Windows.Forms.Timer` для AR-тика МОЖЕТ НЕ ТИКАТЬ
  (то же ловили в `MapEditor2Form`). Симптом: AR запущен, но в логе НЕТ ни записей из
  `ArUpdateTick`, ни отправок `ar_telemetry`; в AR1 нет даже того, что рисуется БЕЗ
  телеметрии (надпись FOV). Лечение: `System.Threading.Timer` + `BeginInvoke` для WS/UI,
  `Interlocked _arTickBusy` от наложения тиков. В тике есть строка «жизни»
  `[AR] tick alive:` (app_data, 1/5 c) — по ней отличать «тик мёртв» от «условие ложно».
- ДИАГНОСТИЧЕСКОЕ ПРАВИЛО: если в логе нет `Command 'ar_telemetry' sent to map.` —
  AR-сторона не получает данных вообще; проверять ТАЙМЕР раньше, чем условия отправки.

### [v1.0.40.30 15.09.2026] SCS-CAMERA-POSE — реальная 6DoF-поза камеры (ЗАМЕНЯЕТ все camera-хаки)
- **ПРАВИЛО: В AR НЕТ НИКАКИХ ПРИБЛИЖЁННЫХ ПАРАМЕТРОВ КАМЕРЫ.** Запрещено
  использовать в runtime-проекции: `EyeHeight`, `PitchCompensation`, отдельный
  Roll/Pitch-хак, зеркалирование `u`, сглаживание экранной координаты, prediction,
  высоту точки через ближайший город, плоскость колёс для положения камеры.
- **ИСТОЧНИК ПОЗЫ (SCS hierarchy, файл `AR/ScsCameraPose.cs`):**
  `head_cabin = head.position + head.offset.position` →
  `head_vehicle = cabin.position + cabin.offset.position + rotate(cabin.offset.orientation, …)` →
  `head_world = truck.world.position + rotate(truck.world.orientation, …)`.
  Ориентация — та же иерархия. Базовая голова смотрит по локальной **−Z**
  (SCS: локальный Z назад, X вправо, Y вверх). `ScsCameraPose.Rotate` =
  **Roll вокруг Z → Pitch вокруг X → Heading вокруг Y** (доли оборота).
- **ЕДИНСТВЕННЫЙ ПУТЬ world→screen:** `world − camera.position` → проекции на
  CameraForward/Right/Up → pinhole `f = W/2 / tan(FOV/2)` → пиксели. Базис
  ортонормализуется в `TryCreate`. Никаких добавок после проекции.
- **`ArGameState.CamX/Y/Z` = ГЛАЗ** (а НЕ truck.world.placement!); +`CameraForward/
  Right/Up`+`CameraPoseValid`. Углы (`YawBase/PitchBody/Roll/YawHead/PitchHead`)
  — ТОЛЬКО диагностика. `PublishArV2Snapshot` — только при валидной позе.
- **payload `ar_telemetry`:** `camera{position,forward,right,up,fovDeg,valid}` +
  legacy `placement`/`head` (для статуса). AR1 НЕ собирает камеру из углов.
- **ГОРИЗОНТ — МИРОВОЙ:** горизонт = мировые лучи ⟂ Up=(0,1,0):
  `v = cy + f*(Forward.Y + Right.Y*x)/Up.Y` (вырожденный случай — вертикальная
  линия). Прежний `horizonScreenLine` со складыванием питчей УДАЛЁН — он
  «не держал» горизонт (симптом: смещение при движении головы вверх/вниз).
- **КОНУС ОБЗОРА В AR1 УБРАН** (решение пользователя: в AR1 он совпадает с нашим
  POV). Конус остаётся ТОЛЬКО на миникарте и в редакторе карты.
- Удалено из AR (не возвращать): `UsePinholeProjection`, старый путь v85,
  `CabinFovDegrees` (статик), `TargetDisplayY`, `displayYFor`/`nearestCityY`/`_ySmooth`,
  `_sm` (screen-сглаживание), `pinSmooth`/`pinLead`, зеркало `W − pPr.u`,
  `exCam`-экстраполяция, `updateCameraWorld`/`cameraWorld`.
- Частота AR-тика 33→16 мс, TruckTel `throttle=50`→`throttle=16` (уменьшение temporal
  latency; геометрия точна относительно последнего сэмпла, sync/prediction — отдельно).

### [v1.0.40.31 16.09.2026] GROUND-PLANE — реальная плоскость дороги по колёсам
- **ПРАВИЛО: ЗЕМЛЯ В AR — ЭТО `ArGroundPlane`, А НЕ ГОРИЗОНТАЛЬНАЯ ПЛОСКОСТЬ.**
  Запрещено использовать в геометрии AR (сетка, создание точки, дистанция до земли,
  визуализация высот): `GroundY + PlaneOffsetM`, `truckY` как высота земли,
  `Math.Round(worldX/worldZ)` как snap. `PlaneOffsetM` оставлен ТОЛЬКО для legacy-ручек AR2.
- **КЛЮЧИ ТЕЛЕМЕТРИИ КОЛЁС (подтверждено живым REST-кадром):**
  `truck.wheels.count`, `truck.wheel.position` (`[[x,y,z],…]`, ЛОКАЛЬНО),
  `truck.wheel.radius[]`, `truck.wheel.on_ground[]`, `truck.wheel.suspension.deflection[]`.
  В WS-дельте дополнительно поддерживаются indexed-ключи `truck.wheel.position.N`.
- **РАСЧЁТ** (`AR/ArGroundPlane.TryBuild`, ровно по заданию): `wheel.position`
  → world через `ScsCameraPose.Rotate`; контакт = центр − Up·radius; fit
  `Y = aX + bZ + c`; нормаль `(−a, 1, −b)`; контакт = центр − НОРМАЛЬ·radius;
  6 итераций; финальный fit + residual. `|Ny| < 0.30` → отказ («стена»).
  Колёса с `on_ground=false` НЕ участвуют в fit, НО остаются в `Wheels` (диагностика).
- **ТОЧНОСТЬ:** fit идёт в ОТНОСИТЕЛЬНОЙ системе фуры (±5 м), НЕ в мировых
  координатах (≈160000) — иначе float теряет сантиметры.
- **`suspension.deflection` ВЫЧИТАЕТСЯ из локального Y центра колеса**: SCS
  `wheel.position.y` — НОМИНАЛЬНОЕ (полностью разжатое) положение; deflection — ход.
  Проверено тестом: сдвиг плоскости = deflection ровно.
- Плоскость: `Origin` (целый узел мира, прижатый к плоскости), `Normal`, `AxisU`
  (проекция мировой X в плоскость), `AxisV = Normal × AxisU`, `AverageWheelHeight`,
  `ReferenceHeight`, `MaxResidual`, `UsedWheelCount`. Запросы: `HeightAt`,
  `IntersectRay` (t>0), `SignedDistance` (+над плоскостью), `ProjectToGridCoordinates`,
  `FromGrid`, `SnapToPlane`.
- **`ArGroundPlane.RunSelfTests`** — 11 сценариев, вызывается ОДИН раз при старте
  насоса (`EnsureArGroundPlaneSelfTest`), результат в `app_data.log`. Проверять эту
  строку ПЕРВОЙ при жалобах на геометрию земли.
- **СОЗДАНИЕ ТОЧКИ (`ArPlacePinFromViewCenter`) — ТОЛЬКО ЛУЧ × ПЛОСКОСТЬ:**
  `CameraForward` → `IntersectRay` → `ProjectToGridCoordinates` → округление (u,v)
  `MidpointRounding.AwayFromZero` → `FromGrid`. Взгляд выше горизонта или t>1500 м →
  пометка НЕ создаётся. Старая угловая математика (heading+head[3], head[4],
  `EyeHeightM=1.5`, `Math.Round(px/pz)`) УДАЛЕНА — она и была корнем «точка не по лучу».
  `ArPlacePinAtWorld(x,z)` — проекция на плоскость → snap → мир.
- **СЕТКА AR1 (`drawGroundGrid` / `DrawPlaneGrid`):** круг R=150 м ВНУТРИ плоскости
  (оси `AxisU/AxisV`), центр — опорная точка фуры на плоскости. 1 м — тонкая белая;
  10 м — толстая оранжевая; 50 м — красная (ПРИОРИТЕТ над 10 м); до 125 м alpha
  постоянная, 125…150 м линейный fade, дальше не рисуется; граница круга — реальная
  МИРОВАЯ окружность в плоскости (не экранный круг).
- **ПРОИЗВОДИТЕЛЬНОСТЬ СЕТКИ:** линия В ПЛОСКОСТИ остаётся прямой на экране, поэтому
  каждый участок рисуется ОДНИМ отрезком (2 проекции); разбиение только на 3 шага
  внутри зоны fade. Наивная пошаговая версия (~70 000 объектов/кадр) — GC-шторм.
  Проекция узла — инлайн, без аллокации объекта на точку.
- **ОКНО ВИЗУАЛИЗАЦИИ ВЫСОТ (`data/web_heights.html`)** — правый верхний угол,
  открыто ВСЕГДА. Грузовик НЕ рисуется: белая линия плоскости земли, два красных
  колеса сбоку (нижние точки НА плоскости), полупрозрачный оранжевый конус
  ВЕРТИКАЛЬНОГО FOV (вершина в позиции головы-камеры; вертикальный FOV вычисляется
  C# из ГОРИЗОНТАЛЬНОГО по пропорции экрана), белая прицельная линия до плоскости +
  метка, длина полосы над меткой. Масштаб: 1 м за задним колесом, 40 м от камеры
  до края. Все величины считает C# (`ar_telemetry.heights`).
- **НАСОС ТЕЛЕМЕТРИИ НЕЗАВИСИМ ОТ AR1:** `EnsureArTelemetryPump(alwaysOn)` поднимает
  тик+WS+REST один раз (окно высот открыто всегда). При `_arPumpAlwaysOn` остановка
  AR1 СОХРАНЯЕТ телеметрию; при выходе — `StopArTelemetryPumpForShutdown`.
- **УРОК:** при переносе геометрии в JS — считать объём работы на кадр. 900 линий ×
  900 шагов = 810 000 вызовов; правильная декомпозиция (постоянная часть + 3 шага
  fade) даёт 20-кратный выигрыш без потери качества картинки.

### [v1.0.40.32 16.09.2026] VERTICAL-FOV — ЗАПРЕТ «ОДНОГО FOCAL ПО X И Y»
- **ГЛАВНОЕ ПРАВИЛО: ВЕРТИКАЛЬНЫЙ И ГОРИЗОНТАЛЬНЫЙ FOCAL LENGTH — РАЗНЫЕ.**
  Запрещено применять `f = (W/2)/tan(hFov/2)` и к X, и к Y. В ETS2 вертикаль и
  горизонталь масштабируются НЕЗАВИСИМО (в config.cfg есть отдельные
  `r_multimon_fov_vertical` и `r_multimon_fov_horizontal`). Нарушение даёт ошибку
  `Δv ≈ Δf·tan(наклона головы)`: НОЛЬ на горизонте и рост с углом — симптом
  «горизонт/метка уплывают тем сильнее, чем дальше прицел от горизонта».
- **ИЗМЕРЕНО (три скриншота, 1920×1080, cy=540, горизонт в центральном столбце):**
  `h=−10.8° → 110 px`, `h=0° → 400 px`, `h=+10.8° → 860 px`. МНК по модели
  `v = cy + fv·tan(b + h)` (b — базовый наклон камеры, fv — вертикальный focal;
  ТРИ уравнения на ДВЕ неизвестные ⇒ проверяемая гипотеза):
  `fv = 1965 px` → **vFov ≈ 30.7°**, `b ≈ −2.35°` (внутренняя камера смотрит чуть
  вниз), RMS = 41 px. Устойчиво к ±20 px ошибки измерений (fv меняется на 0.3%).
  «Один focal» (`fv = 1144 px`, vFov = 50.5°) даёт RMS = 158 px и ошибки ±160 px
  на наклонах — **НЕ объясняет данные**. `factor = tan(30.7/2)/tan(50.5/2) ≈ 0.58`.
  МЕТРИКА КАЧЕСТВА: разброс ошибки по кадрам 313 px (старая) → **90 px (новая)**.
- **РЕАЛИЗАЦИЯ:** `ArBridge.FovDegreesAr1Vertical` (+ `Ar1VerticalFovManual`,
  `Ar1VerticalFromHorizontalFactor = 0.58`, `DeriveAr1VerticalFov`);
  `CabinArProjection.CabinFovDegreesVertical` (два focal, обратная совместимость
  при ≤ 1°); `ar.fovDegVertical`/`CFG.fovDegVertical` в JS; РАЗНЫЕ focal в
  `projectPoint` И в `projUV` сетки; горизонт через вертикальный focal;
  `camera.fovDegVertical` в payload; `ar_fov.fovVertical`.
- **ПОДСТРОЙКА НА ЛЕТУ (процедура калибровки):** `Ctrl+Alt+PGUP/PGDN` — верт. FOV
  AR1 ±0.5° (автоповтор), `Ctrl+Alt+HOME` — сброс к авто. Навестись на ДАЛЬНИЙ
  горизонт → крутить до совпадения → наклонить голову на ±10° и убедиться, что
  линия НЕ уезжает (старая модель уезжала на ~157 px). Debug-строка печатает
  `FOV: H …° V …°`; надпись внизу — `80° / 31°`.
- **СЕТКА ДОЛЖНА ДОХОДИТЬ ДО НИЗА ЭКРАНА.** Круг R=150 м вокруг ОПОРНОЙ ТОЧКИ ФУРЫ
  не покрывал низ экрана: камера выше и сзади, поэтому «вниз» плоскость попадала
  в круг лишь через десятки метров. Фикс — расширение В СТОРОНУ КАМЕРЫ на
  `ArGroundPlane.CameraPadM = 60 м`; центр НЕ переезжает (требование «от низа
  экрана до центра»), растут только границы индексов линий; внешние участки —
  постоянная слабая alpha 0.35.
- **ЦВЕТА НЕ ДОЛЖНЫ ПУТАТЬ.** Граница круга сетки была почти как горизонт
  (`rgba(64,217,255,…)` против `rgba(120,220,255,…)`) → «вижу две голубые линии».
  Граница — приглушённая серо-синяя; линия горизонта ПОДПИСЫВАЕТСЯ
  (`drawHorizonLabel`: плашка + текст «ГОРИЗОНТ» у левого края).
- **НАЗВАНИЕ СТРАНИЦЫ** (`<title>`): не дублировать префикс «ETS2 Assist —» —
  WebOverlay использует title как заголовок окна, получалось «AR HUD — ETS2 Assist…».

### [v1.0.40.33 16.09.2026] STEP-AND-TILT — ALT НЕ РАБОТАЕТ, СЕТКА-ПЛОСКОСТЬ, WebOverlay-контракт, наклон+ролл
- ⛔ **ALT НЕЛЬЗЯ ИСПОЛЬЗОВАТЬ В ГОРЯЧИХ КОМБИНАЦИЯХ.** Windows перехватывает их
  раньше приложения (системное меню / Alt+Tab) → `RegisterHotKey` падает (1409).
  Раздел v40.32 про `Ctrl+Alt+PGUP/PGDN`/`Ctrl+Alt+HOME` **ОТМЕНЁН**.
  **ДЕЙСТВУЮЩИЕ ХОТКЕИ (проверено по логу, 0 ошибок регистрации):**
  `Ctrl+Shift+PGUP/PGDN` — вертикальный FOV AR1 ±0.5° (автоповтор), сброса НЕТ;
  `Ctrl+PGUP/PGDN` — горизонтальный FOV AR1; `Ctrl+Shift+HOME/END` — FOV AR2.
- **LEGACY-ХОТКЕИ ПЛОСКОСТИ ОТКЛЮЧЕНЫ И НЕ ДОЛЖНЫ ВКЛЮЧАТЬСЯ ОБРАТНО:**
  `HOTKEY_PLANE_UP/DOWN` (подстройка `PlaneOffsetM`) закомментированы — иначе PGUP/PGDN
  заняты и вертикальный FOV не регистрируется (ошибка 1409). Реальная земля теперь
  `ArGroundPlane` (плоскость по колёсам), `PlaneOffsetM` — только AR2-инструменты.
- **СЕТКА = ПРОСТАЯ БЕСКОНЕЧНАЯ ПЛОСКОСТЬ. НЕ ВОЗВРАЩАТЬ круг/эффекты/шахматы.**
  Убраны: круг R=150 м, радиусный fade 125…150 м, граница-окружность, расширение
  к камере `CameraPadM`. Центр сетки привязан к **КАМЕРЕ** (`snapToPlane(camX, camZ)`),
  а не к опорной точке фуры — именно это даёт покрытие от низа экрана. Затухание только
  по расстоянию до камеры (`gridFarStartM 220` / `gridFarEndM 320`) и по экранной
  вертикали. `ArGroundPlane.CameraPadM`/`CameraPadInGrid` помечены `[Obsolete]`.
- **WebOverlay — ОТДЕЛЬНЫЙ РЕПОЗИТОРИЙ:** `F:\repo\weboverlay` (`Program.cs`, `HelpPage.cs`,
  `LocaleData.cs`). Правки GUI-репозитория его НЕ меняют. Порядок: собрать в
  `F:\repo\weboverlay`, затем скопировать exe в `f:\repo\ets2_assist\data\bin\WebOverlay.exe`
  (и в `publish\data\bin\`), иначе compile.ps1 доставит старый бинарник.
- ⛔ **НИКАКИХ СОСТОЯНИЙ ОКОН ПО МОНИТОРАМ.** Файлы `<url>__monitor_DISPLAY<n>.txt`
  ломали всё: каждому монитору — своя геометрия, при переключении настройки «сбрасывались»,
  оставались осиротевшие файлы. Действует ЕДИНЫЙ `<url>.txt`
  (`GetStateFilePath()` без монитора). `GetStateFilePath(Screen)`, `GetMonitorKey`,
  `LoadStateForMonitor`, `MoveToNextMonitor` удалены. Окно свободно переезжает между
  мониторами ПО КООРДИНАТАМ.
- **КОМБИНАЦИЯ `MoveMonitor` ЗАМЕНЕНА НА ТОГГЛ ШАГА:** `Ctrl+Shift+Alt+Oem5` = `ToggleStep`
  (обычный шаг ↔ ×3 для перемещения/размера/зума). Тоггл — ЕДИНСТВЕННОЕ действие
  на отпускание (arm on press → execute on release); всё остальное — непрерывно.
- ⛔ **МАНИПУЛЯЦИИ С ОКНОМ ДОЛЖНЫ БЫТЬ НЕПРЕРЫВНЫМИ (НЕ `OnKeyDown`!).**
  Обработка зума/размера в `OnKeyDown` через `CheckBinding` давала «срабатывает один раз
  и требует перевыбора окна жёлтой рамкой»: после первого действия WebView2 забирал фокус,
  `KeyDown` больше не приходил. Правило: перемещение + ЗУМ + РАЗМЕР обрабатывать в тике
  (`ApplyHeldManipulation`, 15 мс) по ГЛОБАЛЬНОМУ состоянию клавиш (`GetAsyncKeyState`) —
  работает при зажатии, БЕЗ фокуса окна и повторно без перевыбора рамкой.
- **УМОЛЧАНИЯ ОКОН (если конфигов нет; монитор — ОСНОВНОЙ):** миникарта `web_pda_map` —
  левый НИЖНИЙ (сторона 30% раб. высоты); гибрид `web_ui_hybrid` — снизу ПО ЦЕНТРУ
  (42%×32%); высоты `web_heights` — ПРАВЫЙ ВЕРХНИЙ (34%×30%); pause-logo — левый ВЕРХНИЙ
  (сторона 18%); `help.html` — 800×1000 у `left+560 / top+15`. Место всегда зажимается
  в рабочую область монитора.
- **ОКНО ВЫСОТ (`data/web_heights.html`):** фон чёрный, прозрачность 25%
  (`rgba(0,0,0,0.75)`). Подписи колёс — ВЛЕВО от колеса (right-align по `cx − r − 6`),
  двумя строками («заднее»/«переднее» + `R=N.NN м`) — по центру они перекрывали плоскость.
  Плоскость земли НАКЛОНЕНА по питчу: `planeYAt(longM) = groundY − tan(pitch)·(longM − camL)·pxPerM`
  (пивот под камерой, камера на месте). Подъём → вверх вправо (против часовой), спуск →
  вниз вправо (по часовой); подпись «подъём/спуск N°». Ролл — вертикальная ЖЁЛТАЯ линия
  от точки камеры: 0 → не видна, влево → вверх, вправо → вниз, подпись угла на конце,
  0 и ±1 не подписываются.
- **PITCH/ROLL СЧИТАТЬ ОТ ПЛОСКОСТИ, А НЕ ОТ УГЛОВ КУЗОВА.** В `BuildHeightsPayload`:
  `alongForward = dot(forward_truck, N)`, `alongRight = dot(right_truck, N)`,
  `pitchDeg = −asin(alongForward)`, `rollDeg = asin(alongRight)`; `fwd = (−sin(h·2π), −cos(h·2π))`.
  Знаки проверены численно 16/16 (в т.ч. независимость от heading 0/45/90/137/250°).

### [v1.0.40.35 16.09.2026] CAM-ROLL-STAB — КАМЕРА ETS2 СТАБИЛИЗИРОВАНА ПО КРЕНУ
- ⛔ **ГЛАВНОЕ ПРАВИЛО: КРЕН КУЗОВА НЕЛЬЗЯ ПРИМЕНЯТЬ К КАМЕРЕ.** Камера салона ETS2
  стабилизирована: при наклоне фуры мир на экране НЕ заваливается. Применять к базису
  камеры `truck.world.placement[5]` — ошибка, дающая сразу две жалобы: горизонт уходит
  по крену НЕ В ТУ СТОРОНУ («против часовой») и наклоняется ПОПЕРЁК реального ≈ на `2·|roll|`
  («неровный»). ДОКАЗАНО живым кадром:
  `кузов roll=-6.81°`, но `КАМЕРА крен=+9.93°` (крен кузова в камеру не перешёл),
  а наклон взгляда точно равен голове (`45.80° ≈ 45.76°`).
- ⛔ **ЭТО ДВЕ РАЗНЫЕ ВЕЛИЧИНЫ, А НЕ ОДНА НА ДВА МЕСТА:**
  - **КАМЕРА** — крен НЕ применяется: `new ScsEuler(_arHeading, _arPitch, 0)`.
    Тангаж и курс применяются (подъём/спуск и поворот камера чувствует);
  - **ПЛОСКОСТЬ ДОРОГИ** (`ArGroundPlane`) — крен применяется
    (`_arRoll * _arTruckRollSign`): колёса жёстко связаны с кузовом, пятна контакта
    реально наклоняются, значит и полотно. Смешение этих двух трактовок — источник ошибки.
- **МЕТОД ПОИСКА (повторять при жалобах на геометрию):** НЕ подгонять формулу по
  скриншотам. Сначала инструментировать: печатать ВСЕ источники углов (кузов/голова/
  кабина/итоговый базис) И геометрию, которую реально рисует страница
  (`y(лево/центр/право)` + наклон). Реализовано как `[AR] TILT` + `[AR] HORIZON-GEOM`
  (`LogArTiltDiagnostics`, `MainForm.ArTarget.cs`). В живом кадре это сразу показало,
  что центр линии уехал в −353 px при 1080 — то есть причина не в тонкой ошибке.
- **ПРЕЖДЕ ЧЕМ МЕНЯТЬ ЗНАК — ПРОВЕРИТЬ МОДЕЛЬ ЧИСЛЕННО.** Логично выглядящая инверсия
  здесь оказалась бы регрессом: численная модель плоскости по колёсам (с подвеской,
  которая гасит крен кузова, поэтому нормаль близка к мировой вертикали) подтвердила,
  что текущий знак `rollDeg` и знак жёлтого индикатора УЖЕ верны:
  кузов −10° → `rollDeg=+20°` → индикатор ВВЕРХ; кузов +10° → `rollDeg=−20°` → ВНИЗ.
  Требование «влево → вверх, вправо → вниз» выполняется. Инверсия была ОТКАЧЕНА.
- **ВРЕМЕННЫЙ ИНСТРУМЕНТ ПРОВЕРКИ:** `Ctrl+Shift+Y` — стабилизация крена камеры
  ВКЛ/ВЫКЛ (`ToggleArCameraRollStabilization`, по умолчанию ВКЛ = норма ETS2).
  Даёт однозначный ответ за один тест, если направление всё ещё спорно.
- **ВЕРТИКАЛЬНЫЙ FOV — НЕЗАВИСИМАЯ НАСТРОЙКА:** `AppSettings.Ar1FovVerticalDeg`,
  по умолчанию **64.5°**, шаг **0.2°** (`Ctrl+Shift+PGUP/PGDN`), значение СОХРАНЯЕТСЯ.
  ⛔ Вывод вертикали из горизонтали через aspect УБРАН: он давал ≈59.6° при hFov 91°
  и тайно переезжал при смене горизонтального FOV, сбрасывая калибровку.
  При смене горизонтального FOV вертикаль НЕ должна меняться.

### [v1.0.40.36 16.09.2026] ROLL-MODES — ЭТАЛОН ГОРИЗОНТА ПОЛОТНА + ПЕРЕБОР РЕЖИМОВ КРЕНА
- ⚠️ **ОТМЕНА ВЫВОДА v40.35:** «камера стабилизирована, крен камеры = 0» — НЕВЕРНО.
  Вывод был сделан из одного кадра и неверно прочитан. ДОКАЗАТЕЛЬСТВО:
  пользователь видит, что «голубой горизонт В ПРИНЦИПЕ не компенсируется по крену,
  всегда под прямым углом к краю экрана» — это ровно режим «крен=0»; в логе 16:24
  `кузов roll=-8.05°` → `крен КАМЕРЫ=+0.62°` (т.е. ровно 0, как я и включил).
- **КРИТЕРИЙ ПРАВИЛЬНОГО РЕЖИМА — БЕЛАЯ ЛИНИЯ ГОРИЗОНТА ПОЛОТНА** (`drawPlaneHorizon`).
  Голубой горизонт строится из БАЗИСА КАМЕРЫ, белый — из РЕАЛЬНОЙ плоскости дороги
  (по колёсам). Источники НЕЗАВИСИМЫ, поэтому их совпадение — объективная проверка.
  На ровной дороге совпадают ТОЧНО (проверено: `[540.0,540.0]`); на уклоне/крене
  расходятся ровно настолько, насколько неверна компенсация.
  Голубой горизонт подписан **«ГОРИЗОНТ МИРА»**, белый — **«горизонт полотна»**.
- **ТРИ РЕЖИМА КРЕНА КАМЕРЫ** (`Ctrl+Shift+Y`, по кругу 1 → 0 → 2; по умолчанию 1):
  `0` = крен 0 (стабилизация игрой); `1` = крен кузова С ИНВЕРСИЕЙ (`−roll`);
  `2` = крен кузова как есть (`+roll`). Выбирать тот, при котором голубой ложится на белый.
- ⛔ **УРОК (метод):** когда два источника геометрии противоречат представлению о физике,
  НЕ перебирать знаки вслепую и НЕ «доказывать» по одному кадру (именно так возникла
  ошибка v40.35). Нужно построить ВТОРУЮ НЕЗАВИСИМУЮ реализацию той же величины
  (здесь — горизонт из реальной плоскости) и сделать правильный режим наблюдаемым.
- **ЧЕГО НЕ ХВАТАЕТ ДЛЯ ЗНАКА ПО ГОРИЗОНТУ:** все три режима дают одинаковое расхождение
  с горизонтом полотна по ВЫСОТЕ (≈±105 px); они различаются HАКЛОНОМ (0° против +6°).
  Поэтому одного горизонта недостаточно — нужна именно пара линий.
- **Геометрия горизонта полотна:** точка плоскости уходит в бесконечность вдоль своих
  направлений; экранная позиция предела = проекция НАПРАВЛЕНИЯ (не точки!). Реализация —
  в `drawPlaneHorizon`, `data/js/ar_hud.js`.

### [v1.0.40.37 16.09.2026] HORIZONTAL-PLANE — ПЛОСКОСТЬ ВСЕГДА ПО ГОРИЗОНТУ (ПЕРЕСМОТР)
- ⛔ **ОТМЕНА ПРЕЖНЕГО ТРЕБОВАНИЯ «строить сетку по плоскости колёс».** Пользователь:
  «Плоскость и сетку рисуем ВСЕГДА ПО ГОРИЗОНТУ, чтобы даже с креном на обочине, носом
  вниз, можно было прицельной точкой поставить метку СТРОГО НА ПЛОСКОСТИ ЗЕМЛИ.
  Плоскость земли будет параллельна горизонту мира ВСЕГДА.»
  Смысл сетки — поставить точку в адекватных координатах из ЛЮБОГО положения
  грузовика/камеры, а не измерить уклон полотна.
- **ЧИСЛЕННО ДОКАЗАНО** (кадр: крен влево 16°, питч вниз 4.3°):
  Г О Р И З О Н Т А Л Ь Н А Я плоскость ⇒ расхождение с горизонтом мира **0.00°**
  при ВСЕХ режимах крена камеры; наклонная (по колёсам) ⇒ **12.10°…12.96°**
  (ровно угол крена кузова). Наклонная плоскость и была причиной, по которой точка
  ставилась неверно.
- **`ArGroundPlane.CreateHorizontal(measured)`** — фабрика горизонтальной плоскости:
  нормаль `(0,1,0)`, оси — мировые X/Z, высота = `AverageWheelHeight` контактов колёс,
  `c` подобран так, чтобы `HeightAt` давала ПОСТОЯННУЮ высоту. Признак `IsHorizontal`.
  Измеренная (наклонная) плоскость хранится в `_arMeasuredGroundPlane` — для уклона.
- **ЗНАК КРЕНА КУЗОВА ИСПРАВЛЕН: `_arTruckRollSign = +1`** (было −1). ДОКАЗАТЕЛЬСТВО
  НЕЗАВИСИМОЕ — через ФИТ ПЛОСКОСТИ: при крене кузова на ровной земле верный знак
  обязан дать ВЕРТИКАЛЬНУЮ нормаль. `sign=+1` → наклон 0.00° (верно);
  `sign=−1` → 18.37° (неверно). В логе при крене 9.187° наблюдался наклон 9.03° —
  ровно ошибка знака. Так знак проверяется БЕЗ экрана и без подгонки.
- **РЕЖИМ КРЕНА КАМЕРЫ: умолчание изменено на 0.** Вывод из наблюдения: горизонт
  «скренился больше вправо и менее параллелен реальному» при крене ВЛЕВО — это ровно
  режим 1; реальный горизонт при крене остаётся практически горизонтальным.
  Цикл `Ctrl+Shift+Y`: 0 → 1 → 2.
- **ОРИЕНТАЦИЯ ГРУЗОВИКА — ОСИ ШАССИ** (`drawChassisAxes`): три линии через центр
  экрана, параллельные осям ШАССИ (локальные X/Z кузова), а НЕ краям экрана:
  X шасси (красная), Z шасси (зелёная), Y кузова (синяя). Поля
  `groundPlane.chassisForward/chassisRight/chassisUp` — от ОРИЕНТАЦИИ КУБОВА.
  Смысл: камера компенсирует наклон, поэтому крен/питч ГРУЗОВИКА по ней не виден.
- **ОКНО ВЫСОТ:** наклон/крен полотна берём из `_arMeasuredGroundPlane`, иначе при
  горизонтальной визуализации они всегда были бы нулевыми.
- **Сравнение одной клавишей:** `Ctrl+Shift+U` — плоскость «по горизонту» ↔ «по колёсам».
- ⚠️ **ОТКРЫТЫЙ ВОПРОС (НЕ РЕШИМ ВСЛЕПУЮ):** игра НЕ приклеивает камеру строго
  параллельно шасси/кабине — «голова компенсируется и стоит ровнее, чем кабина».
  По скриншотам: угол ЗЕРКАЛА к горизонту ≈ угол камеры, а верхний козырёк кабины
  имеет угол БОЛЬШЕ ⇒ полная компенсация креном КУЗОВА всегда будет неточной.
  Доступно в телеметрии: `truck.world.placement[3..5]` = КУЗОВ;
  `truck.head.offset[3..5]` = курс/питч/**крен ГОЛОВЫ** (в живом кадре roll=0).
  Отдельного «угла камеры к горизонту» в телеметрии НЕТ. ВЕРНОЕ НАПРАВЛЕНИЕ:
  брать крен из ГОЛОВЫ, а не из кузова — но сначала подтвердить знак/масштаб на данных.

### [v1.0.40.38 16.09.2026] REAR-VIEW-ROLL — задняя проекция + доля компенсации крена
- **НАБЛЮДЕНИЕ ПОЛЬЗОВАТЕЛЯ (количественный критерий):** «угол между голубым горизонтом
  и реальным ОЧЕНЬ ПОХОЖ НА УГОЛ между голубым горизонтом и X шасси» ⇒ реальный горизонт
  ПАРАЛЛЕЛЕН оси X шасси. ИСПОЛЬЗОВАТЬ КАК ЭТАЛОН: ось X шасси уже рисуется в AR1.
- **ЗАДНЯЯ ПРОЕКЦИЯ** (`drawRearProjection` в `data/web_heights.html`, панель внизу окна):
  колёса — КРАСНЫЕ ПРЯМОУГОЛЬНИКИ высотой с диаметр, на реальном разносе колеи; линия
  земли НАКЛОНЯЕТСЯ при крене вместе с колёсами; точка камеры + ДВЕ ОСИ камеры (F белая,
  U сиреневая); ГОЛУБОЙ ГОРИЗОНТ над землёй, при крене НЕ двигается.
  ЗАЧЕМ: боковой вид НЕ ПОКАЗЫВАЛ крен, а именно крен — источник проблемы.
- **ДОЛЯ КОМПЕНСАЦИИ КРЕНА** `_arCameraRollFactor` (0…1.5, сейчас 0.7): какая часть
  крена кузова переходит в камеру. Подстройка `Ctrl+Shift+J` / `Ctrl+Shift+K` (шаг 0.1),
  значение в лог. Работает только в режимах 1 и 2 (в режиме 0 крен камеры всегда 0).
  Найденное значение 0.7 дало совпадение с осью X шасси с точностью 0.06° на кадре 18:54.
- ⚠️ **МОДЕЛЬ ПРИЗНАНА НЕПОЛНОЙ — НЕ ВЫДАВАТЬ 0.7 ЗА КОНСТАНТУ.** При проверке
  критерия на НЕСКОЛЬКИХ кадрах (самосогласованно: горизонт и ось X одним базисом)
  единого коэффициента НЕ существует:
  крен 9.19° → 0.78 (остаток 6.70°); 9.56° → 1.20 (3.08°); 16° → 0.00 (11.97°);
  −12° → 0.00 (8.92°). Остаток не убирается масштабом ⇒ равномерное масштабирование
  крена — НЕВЕРНАЯ модель. Гипотеза на будущее: компенсация в ETS2 затрагивает ВЕКТОР
  ВЕРТИКАЛИ камеры (голова выравнивается по локальной вертикали), а не масштабирует
  угол. Нужна отдельная гипотеза + запись углов в лог.
- ⛔ **ОБЯЗАТЕЛЬНОЕ ПРАВИЛО ПРОВЕРКИ КРИТЕРИЕВ:** проверять найденный параметр
  НА НЕСКОЛЬКИХ НЕЗАВИСИМЫХ КАДРАХ. Если «лучший» параметр меняется от кадра к кадру —
  это сигнал, что МОДЕЛЬ неверна, а не что данных мало.
- ⛔ КРИТЕРИЙ ДОЛЖЕН БЫТЬ САМОСОГЛАСОВАННЫМ: обе линии (горизонт и ось шасси) рисовать
  ОДНИМ базисом камеры. Сравнение с базисом «без крена» давало ложный результат.
- Данные заднего вида в payload `heights`: `halfTrackM`, `wheelDiameterM`, `cameraLateralM`,
  `cameraHeightAboveGroundM`, `camAxisForward`, `camAxisUp`, `cameraRollDeg`.

### [v1.0.40.39 16.09.2026] FOV-TUNED-DEFAULTS — ЗАФИКСИРОВАНЫ ПОДОБРАННЫЕ ЗНАЧЕНИЯ
- **ДЕЙСТВУЮЩИЕ ЗНАЧЕНИЯ ПО УМОЛЧАНИЮ (менять только по явной команде пользователя):**
  | параметр | значение |
  |---|---|
  | горизонтальный FOV AR1 (`AppSettings.Ar1FovDeg`, `ArBridge._fovDegreesAr1`) | **105°** |
  | вертикальный FOV AR1 (`AppSettings.Ar1FovVerticalDeg`, `_fovDegreesAr1Vertical`) | **65°** |
  | режим крена камеры `_arCameraRollMode` | **2 (+roll)** |
  | доля компенсации крена `_arCameraRollFactor` | **0.5** |
  Подобрано пользователем по видимой картине (шаг верт. FOV 0.2°, гориз. 1°).
- ⚠️ **0.5 — РЕЗУЛЬТАТ ПОДБОРА, А НЕ ВЫЧИСЛЕННАЯ КОНСТАНТА.** Мой перебор давал 0.7,
  но единого коэффициента не существует (v1.0.40.38). ⛔ НЕ «исправлять» 0.5 обратно
  на 0.7 как на «найденное» — это откатит подобранную картину.
- Хоткеи подстройки остались: `Ctrl+PGUP/PGDN` (гориз.), `Ctrl+Shift+PGUP/PGDN` (верт.,
  шаг 0.2°), `Ctrl+Shift+Y` (режим 0→1→2), `Ctrl+Shift+J/K` (доля ±0.1),
  `Ctrl+Shift+U` (плоскость горизонт/колёса).

### [v1.0.40.40 16.09.2026] VIEW-MENU-POINTS50 — меню «Вид», точки в 50 м, высоты точек
- ⛔ **ВЫСОТА ТОЧКИ БЕРЁТСЯ КАК ЕСТЬ. НИКАКИХ ПОПРАВОК ВЫСОТЫ ПО ТИПУ ТОЧКИ.**
  Убрана константа `ArCityHeightCorrectionM = -44.0` (была в `ar_telemetry.cities`
  и `BuildArV2Snapshot`) — именно она «опускала» компании относительно метки.
- ⚠️ **ГЛАВНАЯ ПРИЧИНА «компании ниже метки» — не константа, а ЖЁСТКИЙ `Y = 0`:**
  POI в `RefreshArModel` создавались как `new ArPoint(…, p.X, 0, p.Z, …)` — реальная
  `p.Y` ИГНОРИРОВАЛАСЬ. Плюс при разборе `coords="x,y,z"` читались только части
  0 и 2, а `coords[1]` (высота) ВЫБРАСЫВАЛСЯ → цели/пользовательские точки тоже
  шли с Y=0. Теперь читается и `parts[1]`, и `entry["y"]`.
- **ГДЕ ЛЕЖИТ ВЫСОТА (запомнить):** в `Overlays.json` поля `y` НЕТ ВООБЩЕ; высота
  есть в SDO — `data/editor_static_data/*.json`, поле `y` (проверено: все 5115
  объектов имеют высоту; компании `overlay_company.json`: −41.8…160.1 м).
  uid компаний в SDO и в `Overlays.json` СОВПАДАЮТ → SDO надо накладывать ПОВЕРХ.
- ⛔ **AR-модель точек ОБЯЗАНА звать `LoadSdoPointsInto(pois)`** после
  `LoadStaticPois()` — без этого высоты не доходят до AR (было именно так).
- **МЕНЮ «ВИД»** (`BuildViewMenu`): сетка AR1, оси шасси AR1, линия горизонта AR1,
  горизонт полотна AR1, радиус показа точек (25/50/100/250/500/1500 м), окно высот,
  «Только точки». По умолчанию всё ВЫКЛЮЧЕНО. Выбор СОХРАНЯЕТСЯ в AppSettings
  (`Ar1ShowGrid/Ar1ShowChassisAxes/Ar1ShowHorizon/Ar1ShowPlaneHorizon/ShowHeightsWindow/ArDisplayRadiusM`).
  Настройки → странице командой `ar_view`; страница может запросить `ar_view_request`.
- **ОКНО ВЫСОТ БОЛЬШЕ НЕ ОТКРЫВАЕТСЯ ПО УМОЛЧАНИЮ.** `append web_heights.html`
  теперь только при `AppSettings.ShowHeightsWindow`.
- **ТОЧКИ В РАДИУСЕ 50 м** (`ArDisplayRadiusM`): приложение шлёт СПИСОК `points[]`
  в `ar_target`; поля верхнего уровня = БЛИЖАЙШАЯ (совместимость). В JS —
  `ar.points[]`, рисуются ВСЕ. Событийность сохранена: отправка только при СМЕНЕ
  НАБОРА (подпись из gameName), иначе спам каждый тик.
- **ПОДПИСИ НЕ ВСЕМ ТОЧКАМ:** полные — только целям и точкам ближе 25 м; остальным
  короткая подпись. Иначе при плотной застройке экран превращается в текст.
- **СОРТИРОВКА НАБОРА:** цели → дистанция → имя. Стабильный порядок нужен, чтобы
  точки не «мигали» порядком отрисовки между кадрами.

### [v1.0.40.41 16.09.2026] AR-SETTINGS-MENU — меню «Настройки АР»
- ⛔ **РАЗДЕЛЯТЬ ДВА МЕНЮ:** «Вид» = ЧТО ПОКАЗЫВАТЬ (отладочные визуализации:
  сетка/оси/горизонты/окно высот); **«Настройки АР» = КАК РАБОТАЕТ AR** (рабочие
  параметры: радиус точек, FOV, крен камеры, плоскость). Не сваливать одно в другое.
- **РАДИУС ОТОБРАЖЕНИЯ ТОЧЕК ПЕРЕЕХАЛ В «Настройки АР»** (`BuildArMenu`):
  готовые 25/50/100/250/500/1000/1500 м + пункт **«Другое…»** с вводом произвольного
  числа (раньше из UI задать произвольное значение было НЕЛЬЗЯ).
- В меню также: FOV гориз./верт. (текущие значения видны + ввод вручную),
  крен камеры (режим 0/1/2 + доля ±0.1), плоскость по горизонту,
  «Показать настройки в лог» (одной строкой всё состояние AR — удобно для отчётов),
  «Сбросить настройки АР» (радиус 50, FOV 105/65, режим 1, доля 0.5, плоскость по горизонту).
- **ДИАЛОГ ЧИСЛА:** `ArNumberPrompt.cs` — свой компактный тёмный диалог вместо
  `Microsoft.VisualBasic.InputBox` (не тянем зависимость). Принимает точку и запятую.
- ⛔ **ОШИБКА, КОТОРУЮ ПОВТОРЯТЬ НЕЛЬЗЯ:** `arMenu.DropDownItems.Add(new ToolStripMenuItem(...)).Click += …`
  НЕ КОМПИЛИРУЕТСЯ — **`Add()` возвращает `int`** (индекс), а не элемент. Создавать элемент
  отдельной переменной, затем `Add(item)` и `item.Click += …`.
- `SyncAr1VerticalFov`: fallback был `64.5`, а значение по умолчанию — `65`. Приведено к 65
  (расхождение с `AppSettings.Ar1FovVerticalDeg` давало другую вертикаль при битом файле настроек).

### [v1.0.40.42/43 16.09.2026] FREEZE-FIX2 — замирание точек и цвет точек
- ⛔ **НЕ ЧИТАТЬ ТЯЖЁЛЫЕ ФАЙЛЫ НА UI-ПОТОКЕ.** Требование пользователя: точки
  замирали. ЗАМЕРЕНО (интервалы строк `tick alive`): 5010 мс → **5903 / 6483 / 6558 мс**
  каждые 5-6 интервалов. ПРИЧИНА: `RefreshArModel` выполнялся в UI-потоке (`ArUpdateTick`
  идёт через `BeginInvoke`) и читал/парсил ~1.9 МБ JSON (SDO ≈960 мс + Overlays + города).
  ФИКС: пересборка модели — в ФОНЕ (`QueueArModelRebuild` + `Task.Run`), подмена
  `_arPoints` атомарная под `lock` (`ArPointsSnapshot` — доступ ТОЛЬКО через него),
  интервал 5 → 15 с (`ModelRefreshIntervalMs` — модель статична).
- **`SdoLoader` КЭШИРУЕТ результат** по отпечатку каталога (время последней записи
  + число файлов). `InvalidateCache()` — после выгрузки новых данных из редактора.
  Без кэша `LoadAll()` парсит 1.76 МБ при КАЖДОМ вызове.
- ⛔ **`Process.GetProcessesByName` НЕЛЬЗЯ ЗВАТЬ В КАДРОВОМ КОДЕ.** `GetGameScreen()`
  вызывался из `BuildHeightsPayload`/`LogArTiltDiagnostics` до 30 раз/с и каждый раз
  обходил процессы игры — вторая причина замирания. Теперь кэш на 2 с
  (`GameScreenCacheMs`), сброс — `InvalidateGameScreenCache()`.
- **ЦВЕТ ТОЧЕК В AR:** передавать `p.Color` / `c.Color` из `PointData` (как на миникарте),
  а НЕ пустую строку. Пустая строка → JS `KIND_FALLBACK.poi = '#70d1fe'` (голубой) —
  это и было «у точек всегда голубой цвет». Источник цвета — `SdoMeta.ColorHexOf`
  (company `#a080e0`, garage `#9aa8b8`, fuel_l `#ffa040`…), без SDO-слоя цвета взять негде.
- ⚠️ Модель точек выросла до **5748** (было 1200) после подключения SDO — любой
  тяжёлый проход по ней обязан быть в фоне.
- **МЕТОД ПОИСКА «залипаний»:** брать интервалы периодических лог-строк и сопоставлять
  с таймерами; затем искать дорогие системные вызовы в горячих путях.

### [v1.0.40.44 16.09.2026] SMOOTH-CAM — плавность движения точек в AR
- ⛔ **НЕЛЬЗЯ ПРИМЕНЯТЬ ПОЗУ КАМЕРЫ ТОЛЬКО В МОМЕНТ ПРИХОДА ПАКЕТА.** Телеметрия
  идёт **28–35 Гц**, рендер — 60+ Гц. Если поза меняется только на пакете, два кадра
  рисуют одну и ту же точку, затем прыжок → визуально «14 fps» при счётчике 100.
  РЕШЕНИЕ: сглаживание/экстраполяция на КАЖДОМ кадре (`smoothCamera(nowMs)` в рендере).
- ⚠️ **ИЗМЕРЕННЫЕ ЧАСТОТЫ (не переоценивать заново):** вход TruckTel WS = 35.3 Гц
  (`throttle=16`), `ar_telemetry` C# → страница ≈ 28 Гц. «Дёрганость» — НЕ про вебсокет.
- **АЛГОРИТМ (численно обоснован):** сначала экстраполировать ЦЕЛЬ по скорости
  (`v` из двух последних пакетов, `updateCameraVelocity()` — ВЫЗЫВАТЬ ИЗ ОБРАБОТЧИКА
  ПАКЕТА, там точные интервалы), затем экспоненциально сглаживать
  `k = 1 − exp(−dt/tau)`. Джиттер при 36 мс: 358.9 → **36.6 мм (9.8×)**.
  Голое сглаживание позиции давало лишь 85.2 мм, поэтому экстраполяция обязательна.
- ⛔ **БАЗИС ПОСЛЕ ИНТЕРПОЛЯЦИИ НУЖНО НОРМАЛИЗОВАТЬ** (`lerpNormalized`: лерп +
  нормализация). Покомпонентный лерп ломает ортонормальность → увод горизонта.
- ⛔ **НОЛЬ АЛЛОКАЦИЙ В КАДРЕ:** `camSmoothView` — переиспользуемый объект (поля
  писать НА МЕСТЕ, не пересоздавать `{...}`), `lerpNormalized` пишет в буфер `_lerpOut`.
  Создание объектов каждые 16 мс = постоянный GC-мусор в горячем пути.
- **ПАРАМЕТРЫ:** `posTau = 0.035` (задержка v·tau ≈ 0.7 м при 20 м/с), `rotTau = 0.040`,
  `snapDistM = 25` (телепорт/загрузка — мгновенный снимок без «доезда»),
  `maxExtrapS = 0.15` (потолок экстраполяции: при пропаже пакетов не улетаем вперёд).
- ⛔ **НЕ УДАЛЯТЬ ЭКСТРАПОЛЯЦИЮ КАМЕРЫ СНОВА.** В v1.0.40.30 её убрали (комментарий
  «ЭКСТРАПОЛЯЦИЯ КАМЕРЫ УДАЛЕНА») — и ровно та же жалоба на дёрганость вернулась.
  Это уже второй раз; механизм нужен постоянно.
- **НАСТРОЙКИ:** меню «Настройки АР» → «Плавность (сглаживание позы)»: ВКЛ/ВЫКЛ +
  сила 20/35/50/80 мс (`AppSettings.Ar1SmoothCamera` / `Ar1SmoothTau`, передаются
  в `ar_view` полями `smooth` / `smoothTau`). Владелец настроек — C#, страница получает.
- **ОТКРЫТО (не гадать):** ориентация пока НЕ экстраполируется (только позиция).
  Компенсация крена: модель единой «доли» неполна, гипотеза — компенсация действует
  на ВЕКТОР UP камеры. Значения заморожены: режим 2, доля 0.5.