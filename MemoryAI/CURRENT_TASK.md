# Текущая задача:
⚠️ НОВАЯ СТРАТЕГИЯ (вводные 03.09.2026) — ПРОЕКТ ПЕРЕХОДИТ НА ВЫГРУЗКУ ВСЕХ ТОЧЕК ИЗ
РЕДАКТОРА ИГРЫ (массив всех точек в единой СК, включая высоты). АР становится ОПЦИЕЙ
(миникарта + оверлеи достаточно для квестов). АР2 — только визуализация точек. Всё —
в INSTRUCTIONS.md → «НОВАЯ СТРАТЕГИЯ». Пользователь сейчас выгружает данные из редактора
(города, заправки, ДПС, НПЦ, магазины, шавермы, СТО, леса, водоёмы + ручные статичные точки).

# СЕССИЯ 05.09.2026 (13) — СЕССИЯ ЗАВЕРШЕНА. НОВЫЙ БОЛЬШОЙ ЗАПРОС НЕ НАЧАТ
- **СТАТУС:** viewports всех 77 категорий сгенерированы (40°/10м) и подтверждены пользователем.
  Пользователь завершил сессию. НИ ОДНА часть нового большого запроса НЕ реализована.
- **НОВЫЙ БОЛЬШОЙ ЗАПРОС (следующая сессия, приоритет):**
  1. **Выравнивание кнопок** «скрыть/показать/инверсия» — накладываются друг на друга; применить
     grid/cell выравнивание, автоширину по тексту. Кнопки перекрывают список сайдбара и скрывают
     первую категорию.
  2. **Авторазмер сайдбара** (панель редактирования + панель категорий) — без горизонтального
     скролла; расширять сайдбар, если контент не влезает.
  3. **Цвета категорий** в meta.json — уникальные осмысленные цвета. НЕ жёлтый (резерв для
     городов), НЕ слишком тёмный/чёрный (должен быть светлее фона карты), НЕ белый (резерв).
     В сайдбаре фон под текстом категории и именами точек = цвет категории.
  4. **Тултипы** — текст-описание ко всем кнопкам, чекбоксам и полям.
  5. **Контекстное меню (ПКМ)** для каждой категории и объекта:
     - «Сгенерировать viewport» — файл в `\Euro Truck Simulator 2\editor\storages` из ТЕКУЩИХ
       данных редактора (с оверрайдами). Точка → файл по имени точки+uid. Категория → все точки.
       `viewport_colors[i]` = цвет категории (формат `viewport_colors[0]: 16777215`), заполнить
       `viewport_names[i]`.
     - Категория «Обзор» — скрыть все остальные категории, центрировать карту, масштаб чтобы все
       точки влезли.
     - «Показать в AR» (точка+категория) — все точки категории или одна точка в AR, рисуются в
       оверлее (без изменений файлов/свойств).
     - «Показать на миникарте» — то же, но точки на миникарте с указателями на офф-мап точки.
     - AR и миникарта нужны dumb-receivers для этих данных.
     - Точка «Создать тайник» — форма создания тайника (пустой стаб, без сохранения): поля =
       Имя(Text), Описание(Textarea), Радиус указателя, Радиус обнаружения, Чекбокс «Только в поле
       зрения», Радиус поиска, Кулдаун, Радиус релокации, Оффсет релокации, Кол-во активаций,
       Текст подсказки, Подсказывать ближайший город, Радиус пеленгатора, Файл текстовых
       вариантов, Диапазон награды деньги, Диапазон награды опыт, Список предметов, Только одна
       награда, Покупка награды за опыт или деньги, Журнал геокешинга, Генерировать записи НПЦ шт,
       Генерировать запись НПЦ каждые минут.
     - Точка «Добавить в маршрут».
  6. **Редактор маршрутов** — кнопка «Маршруты» в верхнем меню редактора карты. Маршруты в папке
     `Routes` как json. Маршрут = последовательность точек (достиг первой → открывается вторая и
     т.д.). Кнопка открывает редактор маршрутов: добавленные точки, переупорядочивание. Кнопки:
     Сохранить, Очистить. Клик по точке маршрута двигает карту к ней. Только сохранение в json,
     без обработки.
- **ФАЙЛЫ ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ:** MapEditorForm.cs (сайдбар/кнопки/контекстные меню),
  data\editor_static_data\meta.json (цвета), AR/minimap web-оверлеи (dumb-receivers).

# СЕССИЯ 05.09.2026 (12) — ВСЕ КАТЕГОРИИ VIEWPORTS (40°, 10м)
- **ФИДБЕК: тестовая категория shashlik СРАБОТАЛА** (камера смотрит на точку сверху под углом,
  координаты/кватернион верные).
- **ПАРАМЕТРЫ ИЗМЕНЕНЫ:** угол возвышения 30° → **40°**, дистанция 4м → **10м**.
- **СДЕЛАНО:** скрипт `generate_viewports_all.ps1` перебирает ВСЕ `*.json` в
  `data\editor_static_data` (кроме meta.json) и генерирует `viewports_<имя>.sii` для каждой
  категории в `E:\Users\Docs\Euro Truck Simulator 2\editor\storages\`. Сгенерировано **77 категорий**
  (easter_*, model_*, overlay_*). Камера: к югу от точки (+Z), поднята на 6.428м, смещена на
  7.660м (итого 10м под 40°). Кватернион поворота от (0,0,-1) к направлению на точку.
- **СЛЕДУЮЩИЙ ШАГ:** пользователь тестирует. Если всё ок — можно собрать единый default.sii
  (объединить все viewports в один файл) или оставить пофайлово.

# СЕССИЯ 05.09.2026 (11) — ГЕНЕРАТОР VIEWPORTS РЕДАКТОРА (тестовая категория Шашлык)
- **ЗАДАЧА:** создать default.sii (viewport_placements редактора) для каждой категории сайдбара
  со всеми точками. Камера смотрит на точку сверху под 30° с расстояния 4 м.
- **ФОРМАТ default.sii:** `viewport_placements[i]: (&X, &Y, &Z) (&W; &X, &Y, &Z)` — позиция камеры
  (XYZ, hex float big-endian) + Rotation = КВАРТЕРНИОН (w; x, y, z), НЕ pitch/yaw/roll.
- **СДЕЛАНО:** скрипт `generate_viewports.ps1 -Category <ключ>` читает
  `data\editor_static_data\model_<ключ>.json` (x/y/z в мировых координатах) и генерирует
  `viewports_<ключ>.sii` В ПАПКУ `E:\Users\Docs\Euro Truck Simulator 2\editor\storages\`
  (та же, где лежит исходный default.sii). Камера: к югу от точки (+Z), поднята на 2м, смещена
  на 3.464м (итого 4м под 30°). Кватернион поворота от (0,0,-1) к направлению на точку.
- **ТЕСТОВАЯ КАТЕГОРИЯ:** `viewports_shashlik.sii` (24 точки, категория shashlik = «Шашлык»).
  Пользователь тестирует: скопировать содержимое в
  `E:\Users\Docs\Euro Truck Simulator 2\editor\storages\default.sii`.
- **СЛЕДУЮЩИЙ ШАГ:** после подтверждения пользователя — сгенерировать остальные категории
  (все model_*.json / overlay_*.json / easter_*.json) и собрать единый default.sii.

# СЕССИЯ 05.09.2026 (10) — DEV-MODE БЕЛЫЙ ДАЖЕ В DISABLED (v39.44)
- **v39.44-DEV-MODE-FONT-DISABLED-09.05** (exe=txt=manifest MATCH). Лимиты 3,1% → ~4%,
  RESET 3ч8м. Модель deepseek-v4-flash:cloud.
- **ФИДБЕК: текст «developer mode» всё ещё чёрный, «как у кнопки Stop».** КОРЕНЬ: кнопка Stop
  чёрная, потому что DISABLED (неактивна). То же с devModeChk: когда игра запущена (тест Ctrl+T),
  `UpdateDevModeEnabled()` ставит галочку в `Enabled=false` → WinForms в disabled-состоянии рисует
  текст системным тёмным цветом, ИГНОРИРУЯ ForeColor. Простая установка ForeColor=White (и даже
  в OnPaint) не помогала.
- **ФИКС (v39.44):** в `DevModeCheckBox.OnPaint` временно включаем контрол (`Enabled=true`),
  чтобы `base.OnPaint` использовал белый ForeColor, затем восстанавливаем `Enabled` и `ForeColor`.
  Так текст белый даже в disabled-состоянии (при запущенной игре).
- **СЛЕДУЮЩИЙ ШАГ:** верифицировать, что «developer mode» белый и при запущенной игре (disabled).

# СЕССИЯ 05.09.2026 (9) — DEV-MODE УСТОЙЧИВО БЕЛЫЙ + КОНСОЛЬ ЗАКРЫВАЕТСЯ (v39.43)
- **v39.43-DEV-MODE-FONT-FIX-09.05** (exe=txt=manifest MATCH). Лимиты 0,7% → ~1,5%, RESET 3ч53м.
  Модель deepseek-v4-flash:cloud.
- **ФИДБЕК: консоль теперь закрывается (1.5с задержка v39.42 сработала), НО developer mode всё
  ещё ЧЁРНЫЙ.** Простая установка ForeColor=White не помогла: WinForms ToolStrip
  (профессиональный рендерер) при отрисовке ToolStripControlHost перебивает ForeColor хост-контрола
  значением из своей цветовой таблицы (ControlText = тёмный) КАЖДЫЙ раз при перерисовке.
- **ФИКС (v39.43):** добавлен приватный класс `DevModeCheckBox : CheckBox` — в `OnPaint` ставит
  `ForeColor = Color.White` переД отрисовкой, затем восстанавливает. Так цвет текста НЕ перебивается
  ToolStrip-рендерером. Поле `devModeChk` → тип `DevModeCheckBox`, создание → `new DevModeCheckBox()`.
  Правки ApplyDarkTheme (devModeChk/devModeHost.ForeColor=White) остаются как доп. защита.
- **СЛЕДУЮЩИЙ ШАГ:** верифицировать, что текст «developer mode» теперь стабильно белый.

# СЕССИЯ 05.09.2026 (8) — ЧЕКБОКС БЕЛЫЙ ТЕКСТ + ЗАКРЫТИЕ КОНСОЛИ 1.5С (v39.42)
- **v39.42-DEV-MODE-FONT-CONSOLE-CLOSE-09.05** (exe=txt=manifest MATCH).
  Лимиты 0% (RESET 4ч54м), модель deepseek-v4-flash:cloud.
- **(1) БЕЛЫЙ ТЕКСТ ЧЕКБОКСА «developer mode»:** был чёрный на тёмном фоне. Причина: devModeChk
  живёт в ToolStripControlHost (mainMenu.Items), который НЕ затрагивает SetControlTheme —
  ToolStrip при отрисовке перебивал ForeColor на тёмный. ФИКС: ForeColor=White в конструкторе
  И принудительно в ApplyDarkTheme (devModeChk.ForeColor + devModeHost.ForeColor).
- **(2) ЗАКРЫТИЕ КОНСОЛИ через 1.5с:** телепортация с прогрузкой длится ~1с, консоль закрывалась
  слишком рано (Sleep(150) после Enter). ФИКС: Sleep(1500) после Enter, затем
  PressScanCode(SCANCODE_CONSOLE) (0x29, та же клавиша, что открывала). Убран промежуточный
  Sleep(150) — осталась одна задержка 1500мс к закрытию.

# СЕССИЯ 05.09.2026 (7) — ВОЗВРАЩЕНЫ ЗАДЕРЖКИ ДЛЯ ИГРЫ (v39.41)
- **v39.41-TELEPORT-DELAYS-GAME-09.05-1105** (exe=txt=manifest MATCH). Лимиты 5,9% → ~6%, RESET 2м.
  Модель deepseek-v4-flash:cloud.
- **РЕЗУЛЬТАТ ТЕСТА v39.40 (фидбек):** редактор без задержек работает; игра Ctrl+T БЕЗ задержек
  НЕ работает.
- **ВОЗВРАЩЕНО в `RunTeleportGameSync`:** Sleep(250) после снятия паузы; Sleep(250) в
  OpenEts2Console (после PressScanCode(0x29)); Sleep(100) после ввода команды; Sleep(150) после
  Enter. Редактор (`RunTeleportEditorSync`) — БЕЗ задержек (работает).
- **УРОК:** игра и редактор — РАЗНАЯ чувствительность к задержкам. После открытия консоли игры
  (scan code 0x29) SendInput нужна пауза; редактору (Win32 сообщения) задержки не нужны.
- **СЛЕДУЮЩИЙ ШАГ:** верифицировать Ctrl+T с возвращёнными задержками (должен снова работать).

# СЕССИЯ 05.09.2026 (6) — ТЕЛЕПОРТ CTRL+T: ENTER SCAN CODE + ЗАКРЫТИЕ КОНСОЛИ (v39.39)
- **v39.39-TELEPORT-ENTER-CLOSE-09.05-1034** (exe=txt=manifest MATCH, data 12 папок).
  Лимиты 4,9% → 5,1%, RESET 24м. Модель deepseek-v4-flash:cloud.
- **КОРЕНЬ БАГА:** консоль ETS2 открывается ТОЛЬКО физическим scan code 0x29 (виртуальная
  клавиша VK_OEM_3 не работала — сессия 10). Значит консоль работает на SCAN CODES, а не на
  виртуальных клавишах. Старый код жал Enter через `PressKey((byte)Keys.Enter)` (VK_RETURN) —
  в консоли ETS2 не срабатывал → команда оставалась введённой, консоль не закрывалась.
- **ФИКС (MainForm.cs `RunTeleportGameSync`):** (5) Enter теперь через `PressScanCode(SCANCODE_ENTER)`
  (scan code 0x1C) вместо `PressKey(Keys.Enter)`; (6) после Enter + Sleep(150) — закрытие консоли
  `PressScanCode(SCANCODE_CONSOLE)` (0x29, та же клавиша, что открывала). Константа
  `SCANCODE_ENTER = 0x001C` добавлена в QuestsManager.cs.
- **СЛЕДУЮЩИЙ ШАГ:** верифицировать у пользователя: Ctrl+T — консоль открывается, goto вводится,
  Enter срабатывает (команда выполняется), консоль закрывается. Если Enter всё ещё не срабатывает —
  проверить, что консоль ETS2 принимает scan code Enter (возможно нужен KEYEVENTF_EXTENDEDKEY или
  задержка между вводом и Enter).

# СЕССИЯ 05.09.2026 (5) — СЕССИЯ ЗАВЕРШЕНА (v39.36–v39.38)
- Сессия 05.09.2026 завершена: 3 публикации (v39.36/v39.37/v39.38), все exe=txt=manifest MATCH.
  Лимиты 0,3% → 3,5%, RESET 37м. Модель deepseek-v4-flash:cloud.
- СЛЕДУЮЩИЙ ШАГ (новая сессия): верифицировать v39.38 у пользователя (а) чекбокс dev-mode справа
  в меню с лейблом; (б) кнопка «Тема» исчезла, интерфейс тёмный; (в) все кнопки (в т.ч. АР2) видны,
  окно нельзя уменьшить ниже минимума. Затем — напоминание про телепорт (Ctrl+T: Enter + закрытие
  консоли scan code 0x29).

# СЕССИЯ 05.09.2026 (4) — ЗАВЕРШЕНА (v39.38, чекбокс dev-mode + тёмная тема + высота окна)
- dev-mode теперь настоящий CheckBox (ToolStripControlHost) с лейблом, а не «кнопка». Смена темы
  убрана — тёмная тема по умолчанию (ApplyDarkTheme). MinimumSize окна увеличен до (1180, 800) —
  все кнопки видны, resize ниже минимума запрещён. Публикация v39.38 (exe=txt=manifest MATCH).
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) чекбокс dev-mode справа в меню с лейблом;
  (б) кнопка «Тема» исчезла, интерфейс тёмный; (в) все кнопки (в т.ч. АР2) видны, окно нельзя
  уменьшить ниже минимума.

# СЕССИЯ 05.09.2026 (3) — ЗАВЕРШЕНА (v39.37, developer mode в меню + фикс uset)
- Галочка developer mode перенесена из редактора в главное меню (справа у правого края, File слева).
  Фикс записи config.cfg: строки имеют префикс `uset ` — теперь читаются/пишутся корректно
  (uset g_developer "2"/"0", uset g_console "1"/"0"). Публикация v39.37 (exe=txt=manifest MATCH).
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) галочка справа в меню; (б) при старте отражает
  config.cfg; (в) включение пишет uset g_developer "2" + uset g_console "1", выключение — "0"/"0";
  (г) неактивна при запущенной игре.

# СЕССИЯ 05.09.2026 (2) — ЗАВЕРШЕНА (v39.36, developer mode checkbox)
- Галочка «developer mode» в редакторе карты (после панели overrides): разделитель + CheckBox.
  Старт — чтение config.cfg (g_developer "2" + g_console "1" → активна); переключение — запись
  2/1 или 0/0; блокировка при запущенном eurotrucks2.exe. Публикация v39.36 (exe=txt=manifest MATCH).
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) галочка появляется после overrides;
  (б) при старте отражает config.cfg; (в) переключение пишет/возвращает параметры;
  (г) неактивна при запущенной игре.

# ⚠️ НАПОМНИТЬ ПЕРВОЙ ЗАДАЧЕЙ (05.09.2026, требование пользователя):
- **Телепорт в игру (Ctrl+T): применить команду goto и закрыть консоль.** Сейчас консоль
  появляется, команда вводится, НО не активируется — стоит введённой. Нужно: после ввода команды
  нажать ENTER и закрыть консоль той же клавишей, что и вызывалась (scan code 0x29).
  В `RunTeleportGameSync`: после TypeUnicodeText(cmd) → PressKey(Enter) → добавить ещё раз
  PressScanCode(SCANCODE_CONSOLE) для закрытия консоли. Проверить, что Enter реально нажимается
  и команда выполняется.

# СЕССИЯ 05.09.2026 (1) — ЗАВЕРШЕНА (v39.35 подтверждена: «Всё заработало»)
- Телепорт в игру (scan code 0x29 + Unicode) и редактор (Find/WM_SETTEXT/Close) работают.
  Сессия завершена. Код не менялся (только память). СЛЕДУЮЩИЙ ШАГ: напоминание выше об активации
  команды goto + закрытии консоли.

# СЕССИЯ 04.09.2026 (10) — ЗАВЕРШЕНА (v39.35, тест подтверждён)
- Консоль ETS2 открывается физическим scan code 0x29 (KEYEVENTF_SCANCODE), goto вводится через
  KEYEVENTF_UNICODE (не зависит от раскладки). Find закрывается (Close/ESC). Offset камеры
  редактора X+2/Y+4/Z. Лимиты 20,6%→22,8%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать (а) Ctrl+T — консоль открывается, goto вводится корректно
  (не «?????»); (б) Ctrl+Shift+T — Find открывается, X/Y/Z вписываются, Find нажимается, окно
  закрывается.

# СЕССИЯ 04.09.2026 (9) — ЗАВЕРШЕНА (v39.34, тест подтверждён)
- Исправлен MOUSEINPUT (dx/dy int вместо IntPtr) → INPUT=40, SendInput теперь работает (Ctrl+F,
  VK_OEM_3). SendKeyboardInputs проверяет cbSize==40. Лимиты 19,6%→20,3%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать (а) Ctrl+Shift+T — окно Find открывается, X/Y/Z вписываются,
  Find нажимается; (б) Ctrl+T — консоль открывается, goto отправляется.

# СЕССИЯ 04.09.2026 (8) — ЗАВЕРШЕНА (v39.33, тест подтверждён)
- Исправлен SendInput (размер INPUT: INPUTUNION Size=32, полные MOUSEINPUT/HARDWAREINPUT) —
  Ctrl+F и VK_OEM_3 теперь реально отправляются. Проверка WM_SETTEXT через WM_GETTEXT (не
  GetWindowText). SendKeyboardInputs с проверкой результата. Лимиты 17,8%→19,4%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать (а) Ctrl+Shift+T — окно Find открывается, X/Y/Z вписываются,
  Find нажимается; (б) Ctrl+T — консоль открывается, goto отправляется.

# СЕССИЯ 04.09.2026 (7) — ЗАВЕРШЕНА (v39.32, тест подтверждён)
- Телепорт в редактор (Ctrl+Shift+T) переведён на Win32: окно Find → поля Position (WM_SETTEXT) →
  кнопка Find (BM_CLICK). Телепорт в игру (Ctrl+T) через TryFindEts2Window + WaitForForegroundWindow.
  Удалены FindWindowByTitle/FindGameWindowHandle. Лимиты 15,2%→17,5%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) Ctrl+Shift+T — открывается Find, вписываются
  X/Y/Z, нажимается Find; (б) Ctrl+T — телепорт в игру без зависания.

# СЕССИЯ 04.09.2026 (5) — ЗАВЕРШЕНА (v39.31, тест подтверждён)
- Padding кнопок видимости убран (AutoSize+Padding 0). Телепорт в игру не зависает (вынесено в
  Task.Run, UI не блокируется). Телепорт в редактор: фокус через UI-поток (FocusWindowOnUi),
  задержки 250мс между всеми шагами. Цвет консоли (тёмно-серый #2D2D30) — перепроверить.
  Лимиты 13,5%→14,3%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать (а) кнопки видимости без обрезания текста; (б) CTRL+T не виснет;
  (в) Ctrl+Shift+T открывает Find в редакторе и вписывает координаты; (г) цвет консоли.

# СЕССИЯ 04.09.2026 (4) — ЗАВЕРШЕНА (v39.30, тест подтверждён)
- Кнопки видимости на 35% меньше и НАД списком категорий (контейнер слева). Хоткеи телепорта:
  CTRL+T = в игре (goto), CTRL+SHIFT+T = в редакторе (Find→Position, +3м). Приоритет цели:
  курсор поверх точки → выбранная → курсор. Лимиты 9,2%→13,3%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) кнопки видимости размером/над списком;
  (б) CTRL+T телепорт в игре; (в) CTRL+SHIFT+T телепорт в редакторе (Find), (г) приоритет цели.

# СЕССИЯ 04.09.2026 (3) — ЗАВЕРШЕНА (v39.29, тест подтверждён)
- Фриз окна карты исправлен (слои кэшируются, не читают meta.json в компараторе). Полный экран
  для редактора и приложения. Кнопки видимости (скрыть/показать/инверсия) в левом сайдбаре.
  Ширина stroke названия (PointData.LabelStroke) + удвоенная обводка городов. Лимиты 7,3%→9%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) фриз ушёл; (б) полный экран; (в) кнопки
  видимости; (г) stroke названия + обводка городов.

# СЕССИЯ 04.09.2026 (2) — ЗАВЕРШЕНА (v39.28, тест подтверждён)
- Слой городов поверх всех (сортировка по layer в редакторе, города=0). Сайдбар: оранжевый фон
  + жирный у несохранённых в родной категории. Панель полей: cyan-метка файла override
  (поле+имя файла) + клик по названию → открыть override-файл. Удаление по load order (иерархия).
  Телепорт Ctrl+Shift+C (goto X;Y;Z;A;E, фокус/снятие паузы/консоль/VK_OEM_3, камера 6м/2м в стороне).
  Цвета консоли (тёмно-серый фон, lime/красный/оранжевый/cyan). Лимиты 2,2%→6,9%.
- СЛЕДУЮЩИЙ ШАГ: верифицировать у пользователя (а) телепорт Ctrl+Shift+C — камера показывает
  объект со стороны в 6м/2м, снятие паузы, консоль; (б) cyan-метка полей + клик открывает файл;
  (в) удаление по load order; (г) слой городов поверх; (д) жирный/оранжевый в родной категории.
  (грязные/звёздочки), подсветка грязных полей при повторном выборе; (в) скрытие категорий
  display_on_map=false.

# Текущая задача (до поворота, v39 AR2):
AR v2.0: ПЕРЕХОД НА DirectComposition per-pixel alpha (v100, копилка «Апгрейд
прозрачности рендера.md») — ВЫПОЛНЕНО v39.20 + фикс E_NOINTERFACE v39.21 (02.09.2026). + 
перспективная калибровка сетки (perspective warp, homography, драг жёлтых хендллов).

# КАЛИБРОВКА ПОЛЬЗОВАТЕЛЯ (КОНЕЦ СЕССИИ 01.09.2026, ПРИМЕНИТЬ В СЛЕДУЮЩИХ ШАГАХ):
- **FOV = 99** (вместо 100) — «точки плавают меньше».
- **PlaneOffset = −0.62** (вместо −0.75) — «точки плавают меньше».
- В код НЕ вносить (пользователь: «сейчас код не меняем») — применить в СЛЕДУЮЩЕЙ
  сессии: ArBridge.FovDegrees 100→99, _planeOffsetM −0.75→−0.62 (+ CabinArProjection/
  ArRenderer CabinFovDegrees 100→99 для консистентности), публикация v39.20+1.
- Тест-артефакт «пустые большие квадраты» — ОТЛОЖЕН (возврат по готовности).

# СТАТУС СЕССИИ 01.09.2026 — ЗАВЕРШЕНА (20 итераций v39.12–v39.19; калибровка FOV=99/−0.62 — на следующую сессию):
- **v39.19-V39-AR2-EYE-CALIB-09.01-2148** — последняя публикация (exe=txt=manifest MATCH).
- Ключевые решения сессии: квад-сетка через куб-пайплайн (видимость), Bayer-dither
  альфа (COLORKEY не умеет альфу), круг R=100 м, EyeHeightM=1.5 (Actros), слои
  (сетка первая, текст последним), хоткеи с автоповтором, снэп пина к перекрестьям.
- Лимиты: сессия ~42% (с 0.7% и 2 сбросами); модель glm5.3-flash:cloud.
- ПЕРВЫЙ ШАГ СЛЕДУЮЩЕЙ СЕССИИ: применить калибровку FOV=99/−0.62 + публикация,
  затем тест у пользователя; далее — чекбокс «Показать в AR», CameraModeDetector.

# Что уже сделано (v39.19, 01.09.2026 21:48, 1.0.39.19-V39-AR2-EYE-CALIB):
- RadiusM 30→100 (MaxSegs 16000), BaseA 0.5→0.25 (прозрачность ×2 к v40.6).
- EyeHeightM 1.9→1.5 (глаза Actros 2.25 м от полотна − 0.75 опорная точка) ×4 места.
- Фикс повреждённой строки комментария (CS1513 — пойман сборкой до публикации).
- Публикация v39.19 (exe=txt=manifest MATCH).

# Что уже сделано (v39.18, 01.09.2026 21:33, 1.0.39.18-V39-AR2-DITHER-ALPHA):
- CubeHlsl PMain: 4×4 Bayer-матрица (a → паттерн цвет/чёрный), a>=0.999 — pass.
- DrawPlaneGrid: R=30 м (Ø60), вся сетка шаг 1 м, MaxSegs=4000, BaseA=0.5.
- MainForm.ArTarget.ArPlacePinFromViewCenter: px/pz = Math.Round (перекрестья сетки).
- Публикация v39.18 (exe=txt=manifest MATCH).

# Что уже сделано (v39.17, 01.09.2026 21:16, 1.0.39.17-V39-AR2-GRID-CIRC):
- Сетка-круг Ø200 м (halfLen=√(R²−dk²)), BaseA 0.25, HalfW 1 (2px).
- Хоткеи: шаг ÷2 + автоповтор (repeat=true, без MOD_NOREPEAT).
- Публикация v39.17 (exe=txt=manifest MATCH).

# Что уже сделано (v39.17, 01.09.2026 21:16, 1.0.39.17-V39-AR2-GRID-CIRC):
- ArRenderer.cs DrawPlaneGrid: круг Ø200 м (R=100, halfLen=√(R²−dk²), min InnerM
  для 1-м зоны), BaseA 0.25 (прозрачность ×2), HalfW 1 (линии 2px), шаги 1м/10м.
- MainForm.cs: RegisterHotKeyChecked(..., repeat=true) — без MOD_NOREPEAT для
  FOV/плоскости (автоповтор при удержании); шаги ÷2 (FOV 0.25, плоскость 0.125).
- Публикация v39.17 (exe=txt=manifest MATCH).

# Что уже сделано (v39.16, 01.09.2026 20:59, 1.0.39.16-V39-AR2-GRID-STYLE):
- DrawPlaneGrid: полые квадраты (грани-полосы 4px, BaseA=0.5), VertAlpha
  (1.0 до центр+25%H, →0 к низу), DistFade, WalkLine, MaxSegs=24000.
- RenderFrame: сетка ПЕРВАЯ (дальний слой), прицел/куб/pin/текст поверх.
- Публикация v39.16 (exe=txt=manifest MATCH).

# Что уже сделано (v39.16, 01.09.2026 20:59, 1.0.39.16-V39-AR2-GRID-STYLE):
- ArRenderer.cs DrawPlaneGrid: полые квадраты (грани-полосы 4px, HalfW=2,
  BaseA=0.5), VertAlpha (1.0 до центр+25%H, →0 к низу), DistFade (1−d/400),
  WalkLine (сегменты с отбраковкой, шаг 1м/30м + 10м/400м), MaxSegs=24000,
  snap к мировым координатам, NDC на C# (куб-пайплайн).
- RenderFrame: сетка ПЕРВАЯ (дальний слой), прицел/куб/pin/текст — поверх;
  DrawPinShadow вынесен в отдельный try после куба.
- Публикация v39.16 (exe=txt=manifest MATCH).

# Что уже сделано (v39.15, 01.09.2026 20:39, 1.0.39.15-V39-AR2-GRID-QUADS):
- ArRenderer.cs: DrawPlaneGrid — квады 1×1 м через куб-пайплайн (RadiusM=60,
  MaxQuads=12000, snap, fade). ПЛИТА v39.14 отключена (код сохранён).
- MainForm.cs: FOV = Ctrl+Shift+HOME/END (ALT убран), WM_HOTKEY обновлён.
- Публикация v39.15 (exe=txt=manifest MATCH).

# Что уже сделано (v39.14, 01.09.2026 20:18, 1.0.39.14-V39-AR2-DBG-CUBE):
- MainForm.cs: chkAr2Grid добавлен в Controls.AddRange (КОРЕНЬ), справа от кнопки
  АР2 (leftX+234, topY+646), красная рамка в Paint (отладка), кнопка АР2 230px.
- ArRenderer.cs: DrawGroundDebugCube — яркая НЕпрозрачная плита 40×40×1 м на
  плоскости земли (25 м впереди по yaw), painter's sort, пайплайн куба головы.
- Калибровки: FOV=100 (ArBridge + CabinArProjection + ArRenderer), PlaneOffsetM=−0.75.
- Публикация v39.14: 1.0.39.14-V39-AR2-DBG-CUBE-09.01-2018 (exe=txt=manifest MATCH).

# Что уже сделано (v39.13, 01.09.2026 20:04, 1.0.39.13-V39-AR2-PLANE-PITCH-FIX):
- Галочка «Сетка» слева от кнопки АР2 (в v39.14 переставлена СПРАВА + AddRange-фикс).
- DrawPlaneFillCpu: CPU-заливка плоскости через ProjectPoint→NDC + CubeHlsl + _planeVb,
  оранжевый a=0.38, _tblend. Вызывается первой в DrawPlaneGrid.
- CabinArProjection.Project: bodyPitch *= forwardness² (вперёд=полный, вбок=≈0).
  EnablePitchCompensation=true.
- Публикация v39.13: 1.0.39.13-V39-AR2-PLANE-PITCH-FIX-09.01-2004 (exe=txt=manifest OK).

# Что уже сделано (v39.12, 01.09.2026 19:34, 1.0.39.12-V39-AR2-PLANE-PIN-FIX):
- Удалён CSV-конвейер: EnsureHeadLogFile из Initialize; блок AppendDataLog из RenderFrame;
  поля _lastHeadLogMs/_headLogPath. (Файлы AR/ArRenderer.cs.)
- PublishArV2Snapshot: высота пина = _arTruckY + ArBridge.PlaneOffsetM — каждый кадр,
  X/Z из хранимого пина. Пин «приклеен» к плоскости, движется с ней при её сдвиге.
  (MainForm.ArTarget.cs.)
- Публикация v39.12: 1.0.39.12-V39-AR2-PLANE-PIN-FIX-09.01-1934 (exe=txt=manifest OK).

# Что уже сделано (v98, 01.09.2026 16:20, 1.0.39.6-V39-AR2-CSV-HOTKEY):
- ar_head_ground.csv: создаётся СРАЗУ при старте AR2 (EnsureHeadLogFile в Initialize),
  каждая строка дублируется в app_data.log (Logger.Data).
- ХОТКЕЙ СЕТКИ: CTRL+SHIFT+END (занят системой) → CTRL+SHIFT+* (Numpad, Keys.Multiply).
- ИНСТРУКЦИИ: правило вывода лимитов в TODO отдельным сообщением с цветом/шрифтом (📊).
- Публикация v98: 1.0.39.6-V39-AR2-CSV-HOTKEY-09.01-1620 (exe=txt=manifest OK).

# Что уже сделано (v97, 01.09.2026 16:13, 1.0.39.5-V39-AR2-HOTKEYS-GRID):
- ХОТКЕИ (уточнены пользователем): CTRL+SHIFT+ALT+PGUP/PGDN = FOV ±0.5;
  CTRL+SHIFT+PGUP/PGDN = плоскость земли ±0.25 м; CTRL+SHIFT+END = сетка.
- RegisterHotKeyChecked: каждый хоткей отдельно с проверкой результата — один
  конфликт не роняет все (было «failed to register global hotkeys»).
- FOV-текст: шрифт +30% (8.5f).
- ДИСТАНЦИЯ: текст ПОД крестиком (отступ 2px), шрифт как у FOV (8.5f).
- СЕТКА: оранжевая (1.0,0.55,0.0), полупрозрачная (0.45 к краю), толщина 3px
  (прямоугольники TriangleList — D3D11 не умеет LineWidth).
- Публикация v97: 1.0.39.5-V39-AR2-HOTKEYS-GRID-09.01-1613 (exe=txt=manifest OK).

# Что уже сделано (v96, 01.09.2026 16:00, 1.0.39.4-V39-AR2-GRID-SHADOW):
- КУБ: инверсия yaw (yaw = -s.YawHead*2π) — голова вправо → куб вправо.
- ТЕКСТ: мягкая тень (drop shadow) по умолчанию (3 прохода FillPath со смещением
  вниз-вправо, убывающая альфа) — читаемость на светлом фоне.
- ПРИЦЕЛ: полупрозрачная тень (тёмный квадрат 9×9, альфа 0.35) под микрокрестиком.
- ДИСТАНЦИЯ: шрифт ×2 меньше (6f), текст ВЫШЕ крестика вплотную (отступ 2px).
- FOV-индикатор: шрифт ×2 меньше (6.5f), ниже к краю (отступ 4px), полупрозрачная
  плашка под текстом; в скобках — высота плоскости земли.
- 3D-СЕТКА ПЛОСКОСТИ (Ctrl+Shift+END): DrawPlaneGrid — сетка 200м/шаг 20м на
  плоскости Y=GroundY+PlaneOffsetM, затухание к краю, линии через ProjectPoint
  (та же проекция, что метки → метка «приклеена» к сетке). Новые пайплайны:
  LineHlsl (LineList) + EllipseHlsl (мягкий овал).
- ТЕНЬ ТОЧКИ: DrawPinShadow — если pin.Y ≠ planeY, рисует овальное пятно на
  плоскости в XZ точки (100% на плоскости); если ровно на плоскости — не рисует.
- ХОТКЕИ (v96): Ctrl+Shift+END = сетка; Ctrl+Shift+Alt+PGUP/PGDN = плоскость ±0.25м
  (ArBridge.PlaneOffsetM, clamp ±10м). FOV остался Ctrl+Shift+PGUP/PGDN.
- ПЛОСКОСТЬ влияет ТОЛЬКО на создание новых меток (ArPlacePinFromViewCenter/
  ArPlacePinAtWorld используют planeY = truckY + PlaneOffsetM).
- Публикация v96: 1.0.39.4-V39-AR2-GRID-SHADOW-09.01-1600 (exe=txt=manifest OK).

# Что уже сделано (v87, 01.09.2026 12:11):
- Инверсия: yaw = -(yawBody+yawHead)*2π, bp/hp = -pitch*2π
  (inverse(Rcamera), копилка §4; камера крутится — мир стоит).
- Текст SSAA ×3 (TextSSAA=3): рендер 3× + HighQualityBicubic даунскейл;
  мягкая обводка GraphicsPath (3 кольца Pen, затухание альфы).
- Публикация v87: 1.0.38.87-AR2-AXIS-FIX-HQTEXT-09.01-1211 (exe=txt=manifest OK).

# Что уже сделано (v86, 01.09.2026 11:39):
- ПРОГРЕССБАРЫ (ArRenderer.DrawHeadDiagnosticBars): 2 вертикальные полосы
  14px слева (x=16 и x=40), во всю высоту экрана; yaw — зелёная, pitch —
  голубая; автокалибровка мин/макс (никогда не сужаются — стабильность);
  заливка от дна; при ненабранном окне — метка в середине. state.YawBase
  +YawHead / PitchHead (доля оборота) напрямую.
- CabinArProjection.cs — НОВЫЙ МОДУЛЬ: pinhole-проекция (§4/5/8/9 копилки):
  eye-space перенос → yaw-базис (fwd/right, конвенция TruckTel) → композитный
  питч (кузов→голова) → f=1/tan(FOVh/2), ndcY учитывает aspect → экран через
  ProjectionCenterX/Y (0.5/0.5 конфиг). depth<=0.5 → невалидно.
- ArRenderer.ProjectPoint: A/B-переключатель UsePinholeProjection (static,
  default=true): pinhole-путь (v86) ИЛИ старый путь v85 (копия JS).
  Конфиги — статические поля ArRenderer (CabinFovDegrees=65, Center=0.5/0.5).
- Публикация v86: 1.0.38.86-AR2-PINHOLE-DIAG-09.01-1139 (exe=txt=manifest OK).

# Что обнаружено:
- До v86 старый путь (v85) имел FOV=75 ЗАШИТ и u/v от центра экрана —
  вертикальный FOV не выводился из aspect (копилка §5 нарушалась) →
  «плавание» по краям экрана при повороте головы ожидаемо.
- Прогрессбары покажут: ступеньки = редкие пакеты (проблема частоты
  телеметрии), плавная анимация = проблема в проекции (FOV/оси).

# Текущая архитектура:
- MainForm.ArTarget.cs — канал (WS TruckTel, модель точек, ar_* рассылки, pin,
  PublishArV2Snapshot).
- AR.ArBridge/LatestBuffer — latest-wins снимок → рендер.
- AR.CabinArProjection (v86) — pinhole-модуль: Project(world, camPose, fov, w, h).
- AR.ArRenderer — v86: ProjectPoint = A/B (pinhole v86 | копия JS v85);
  прогрессбары диагностики; маркер/текст/pin/прицел (v85).
- AR v1 JS (data/js/ar_hud.js) — эталон, продолжает работать.

# Изменённые файлы:
- AR/CabinArProjection.cs (НОВЫЙ, v86).
- AR/ArRenderer.cs (v86: DrawHeadDiagnosticBars + A/B ProjectPoint + конфиги).
- ETS2_Assist_GUI.csproj (v86 AR2-PINHOLE-DIAG, 09.01 11:39) — ОПУБЛИКОВАНО.

# Что осталось:
- Тест v87 у пользователя:
  1) оси: наклон головы вниз → цели ВВЕРХ; голова вправо → цели ВЛЕВО;
  2) качество текста: гладкий/мягкий, без рваной обводки;
  3) если оси теперь ПРАВИЛЬНЫЕ, но смещение велико — калибровка FOV 55..80
     (ArRenderer.CabinFovDegrees, статик, без правок проекции);
  4) если инверсия стала ДВОЙНОЙ (метки ушли не туда) — вернуть знаки
     (в CabinArProjection убрать минусы у yaw/bp/hp).
- CameraModeDetector (CABIN/OTHER) — следующий этап копилки (screen capture).
- Чекбокс «Показать в AR» (PointData.ShowInAr) — из TODO v65.
- **ПЕРЕОЦЕНКА ЛИМИТОВ (задача пользователя, 01.09.2026) — ВЫПОЛНЕНО в v39.7:** анализ
  `MemoryAI\ollama_limits_usage.md` сделан, добавлен блок «ПЕРЕОЦЕНКА 01.09.2026» в РАЗДЕЛ 1
  (фактические оценки для экономной модели deepseek-v4-flash: лёгкая 0.5-1%/5м, средняя
  1-5%/10-15м, крупная 5-8%/35-50м). Инструкции дополнены правилом «ПРОГНОЗ И ФАКТ» в РАЗДЕЛ 2
  (формат записи с прогнозом и фактом). ВАЖНО: экономная модель тратит ~x0.3-0.5 от оценок
  РАЗДЕЛА 1 (не x0.5-0.7); прогнозы в TODO (10-60%) были сильно завышены — факт 0.3-7.2%.

# Известные проблемы:
- «Подёргивания»: подозрение на редкие пакеты телеметрии — прогрессбары v86
  дадут визуальный ответ (ступеньки vs плавно).
- «Плавание в сторону вращения» — v86 pinhole с FOV 65° должен уменьшить;
  если нет — калибровка FOV (§11) и проверка знаков/осей (§12).

# Следующий шаг:
1. Тест v86: прогрессбары (частота телеметрии) + pinhole-проекция (плавание).
2. По фидбеку: FOV-калибровка / возврат к v85-пути / CameraModeDetector.

# Что уже сделано:
- v60–v82: канал AR (C#): WS-телеметрия TruckTel (порт из web_data.json), подбор ближайшей точки на C#, событийные рассылки ar_target/ar_telemetry/ar_pin через WS 8084; страница AR v1 — dumb-receiver.
- v76: нативный рендер AR2 (AR.ArOverlayWindow + ArRenderer, D3D11 + Vortice, latest-wins буфер, отдельный рендер-поток).
- v81: цель попадает в ArBridge (ArTarget._arV2Target → PublishArV2Snapshot).
- v82 (JS AR v1): pin-зеркало горизонтали (u→W−u), текст под pin (имя+дист; питч/курс/голова), рамка отсечения, предиктивная интерполяция pin.
- v84: ТЕСТОВЫЙ ОБЪЕКТ в AR2 (ArRenderer): синий кружок, плавно движется влево-вправо в левом верхнем углу
  (x = 120 + 90·sin(2π·0.5·t), y = 90, радиус 24px), анимация по Environment.TickCount64, ни к чему не привязан.
  Цель — визуально убедиться в плавности/эффективности рендер-цикла AR2 (TestHlsl + CreateTestPipeline + DrawTestObject).
- v85 (ПОДТВЕРЖДЕНО ПОЛЬЗОВАТЕЛЕМ: кружок плавный — рендер эффективен; текст работает):
  корень бага горизонтали найден — в v83 YawHead применялся как РАДИАНЫ (получалось ×4π) И
  зеркала ArFlipHorizontal/ArFlipZ ломали знак целиком. В v85:
  1) ProjectPoint = ТОЧНАЯ КОПИЯ projectPoint ar_hud.js: yaw = YawBase*2π + YawHead*2π
     (YawHead — доля оборота!), fwd=(-sin,-cos), right=(cos,-sin), НИКАКИХ зеркал;
  2) КОМПОЗИТНЫЙ ПИТЧ (v75 JS): кузов вращает луч вокруг right, голова добавляется сверху;
  3) высота цели = displayYFor JS (город → переход 350..50 м → НЧ-фильтр 0.08, захват <50 м);
  4) цвет метки = цвет категории (CATEGORY_COLORS editor), размер 15..33 px по дистанции,
     затухание альфа 500→1500 м (и на тексте через TintW.w); экспоненциальное сглаживание 0.25/кадр;
  5) СИСТЕМНОЕ ИМЯ под названием точки (Segoe UI 9.5, если отличается от realName) — требование;
  6) PIN-МЕТКА — DUMB-ПРИЁМНИК: ArBridge.Pin ← ar_pin (+X / кнопка «Пометить в АР»);
     серый крестик «Новая точка · дист» независимо от цели (CreateBoxPipeline + DrawBox);
  7) ПРИЦЕЛЬНЫЙ ПИКСЕЛЬ по центру ВСЕГДА (DrawBox 1.5×1.5, белизна 0.45) — как render() JS.
  МаркерHLSL перекрашен: тёмный центр + чёрная обводка (как drawMarkerToward JS).

# Что обнаружено (фидбек 31.08, тест AR2):
1. Розовая точка = цель категории Company (#ff78c8) — она ОДНА И ТА ЖЕ в AR1 и AR2.
2. Движение головы ВВЕРХ/ВНИЗ — корректно (питч OK, метка держится в вертикальной плоскости).
3. Горизонталь СЛОМАНА И В AR1, И В AR2: взгляд по центру «отключает» точку; взгляд вправо — точка уходит левее; перемещение Company в редакторе двигает прицел (точка привязана к соседней Company).
4. Подозрение подтверждено анализом кода: в AR2 ProjectMarker right-вектор = (cos yaw, −sin yaw·wz)... фактически инверсна BOTH: right должен быть (+cos, −sin) как на миникарте, но Z-компонента в формуле rdot = wx*cos(yaw) − wz*sin(yaw) даёт зеркальную ось относительно мировой XZ-кинематики TruckTel при данном конвенционном yaw → горизонталь зеркальна и «X-стрелка» реверсивна.

# Текущая архитектура:
- MainForm.ArTarget.cs — единый канал: телеметрия (WS TruckTel), модель точек (копия конвейера overrides), событийные ar_* рассылки (WS 8084), pin (луч взгляда ∩ плоскость Y=truckY), AR v2 bridge (PublishArV2Snapshot).
- AR.ArBridge/LatestBuffer — latest-wins снимок ArGameState (камера/цель/pin/города) → рендер-поток.
- AR.ArRenderer (D3D11, Vortice) — v85: ProjectPoint = точная копия projectPoint ar_hud.js
  (без зеркал, композитный питч); маркер DrawCircle (цвет категории) + текст-конвейер GDI→BGRA
  (имя·дист + системное имя) + DrawBox (прицельный пиксель, линии креста pin) + тестовый кружок v84.
- AR v1 JS (data/js/ar_hud.js) — эталон проекции, продолжает работать поверх WebView.

# Изменённые файлы:
- data/js/ar_hud.js (v82: pin-зеркало, текст, рамка, предикция) — ЭТАЛОН, не трогаем.
- AR/ArRenderer.cs (v85: ProjectPoint = копия projectPoint JS без зеркал; УДАЛЕНЫ
  ArFlipHorizontal/ArFlipZ; композитный питч; displayYFor; круг = цвет категории с
  чёрной обводкой/тёмным центром; сглаживание 0.25; системное имя; pin-крестик через
  CreateBoxPipeline/DrawBox; прицельный пиксель всегда; альфа текста через TintW.w)
- ETS2_Assist_GUI.csproj (v85 AR2-JS-PROJ, 31.08.2026 22:18) — ОПУБЛИКОВАНО
  (exe = build.txt = manifest: 1.0.38.85-AR2-JS-PROJ-08.31-2218).

# Что осталось:
- Проверить v85 у пользователя:
  1) горизонталь: голова влево → метка уходит ПРАВЕЕ (мир стоит на месте, камера крутится) — НЕ «с ускорением» в сторону головы;
  2) системное имя под названием точки (мелким шрифтом);
  3) +X (Shift+Ctrl+X) — серый крестик «Новая точка» в AR2;
  4) прицельный пиксель по центру всегда;
  5) тестовый кружок v84 оставить/убрать по готовности (сейчас рисуется).
- Если горизонталь всё ещё не та — проверить знак YawHead в ApplyPlacementJson (сравнить с JS).
- Калибровка FOV (75°) по фидбеку точности.
- Чекбокс «Показать в AR» (PointData.ShowInAr) — из TODO v65.
- Краш g.DrawPath первого старта редактора (TODO с другого ПК).

# Известные проблемы:
- «Метка движется с ускорением при повороте головы» — ПРИЧИНА НАЙДЕНА И УСТРАНЕНА v85:
  в v83 YawHead умножался на 2π дважды (передавался как радианы) + зеркала ручек ломали знак.
  Теперь проекция — точная копия ar_hud.js (подтверждённой пользователем).
- «По центру отключается»: цель на луче → fdot≈0 → отсечение «за спиной» (depth<=0.5) — так и в JS.
- Прицел (плавность) второстепенна: сглаживание 0.25/кадр как в JS; если мало — поднять SmoothK.

# Следующий шаг:
1. Тест v85 (фидбек пользователя): горизонталь поворота головы, системное имя, +X pin, прицельный пиксель.
2. По результатам: убрать тестовый кружок v84 (одна строка — вызов DrawTestObject в RenderFrame).
3. После подтверждения осей: чекбокс «Показать в AR» в редакторе (PointData.ShowInAr).