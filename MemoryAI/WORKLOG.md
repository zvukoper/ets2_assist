# ETS2_Assist_GUI — WORKLOG (память между сессиями)

> Папка `MemoryAI` — память проекта между сессиями. Дополняется в конце каждой сессии.

---

## ⚠️ СЖАТИЕ WORKLOG — 17.09.2026

**Лог начат заново.** Весь предыдущий WORKLOG (01.09.2026 → 17.09.2026, включая архивные
записи 27–31.08.2026 и старт ветки 39) проанализирован, сжат и вынесен в отдельный файл:

### ➡️ `MemoryAI/WORKLOG_summary_17.09.26.md` — читать ПЕРВЫМ для истории проекта

| Параметр | Старый WORKLOG | Сводный файл |
|---|---|---|
| Строк | 7 097 | ~1 290 |
| Символов | 448 690 | ~117 100 |
| Байт (UTF-8) | 667 344 | 117 182 |
| Блоков `## ` | 207 | 10 разделов + ~40 подразделов |

**Что выявлено при сжатии:**

- **≈113 000 символов (25 % файла) — чистый дубликат.** Один блок
  («РЕДАКТОР: ГРУЗОВИК = ТРЕУГОЛЬНИК-СТРЕЛКА (61)») был записан **48 раз подряд**
  (строки 3091–5195, 1 927 строк); блок «AR: ПОДБОР БЛИЖАЙШЕЙ ТОЧКИ НА C# (60)» — 4 копии;
  «Сессия 40» = дословный дубль «Сессии 39»; «НОВАЯ СТРАТЕГИЯ» (03.09) — 2 копии подряд.
- Последняя (48-я) копия была **оборвана** на середине строки — дефект импорта.
- ⛔ **Урок гигиены:** не записывать повторно один и тот же блок; уникальные факты — в один раздел.

**Что сохранено в сводном файле (ничего важного не потеряно):**

1. Статистика сжатия и дублирования, распределение записей по датам, 107 версий `1.0.A.B`.
2. Полная хронология 27.08 → 17.09 по датам, версиям и сборкам.
3. **Реестр корневых причин** (КОРЕНЬ) с классификацией по 6 категориям.
4. Итоговая архитектура: данные/точки, телеметрия, AR (AR1/AR2), сборка, оверлеи.
5. Уроки и ⛔-правила: методология диагностики, логи, лимиты, сборка, работа с пользователем.
6. Хоткей-карта, настройки `AppSettings`, полный блок числовых данных (объёмы, тайминги,
   геометрия, координаты, экономика).
7. Открытые вопросы, бэклог и раздел «археология» (что устарело/опровергнуто/отменено).

**Исходный (несжатый) WORKLOG полностью сохранён в истории git** (последний коммит с ним —
`b34b75f`). Восстановить при необходимости:

```
git show b34b75f:MemoryAI/WORKLOG.md > WORKLOG_full_until_17.09.26.md
```

**Актуальная версия на момент сжатия:** `1.0.40.44-SMOOTH-CAM-09.16-2203`
(fallback в `BuildInfo.cs` = `1.0.40.44-SMOOTH-CAM`).

---

## Формат новых записей (с этого момента)

Каждая сессия/изменение — короткий блок, **без дублирования** соседних записей:

```markdown
## <ДД.ММ.ГГГГ> — <версия/сборка> — <краткая суть>

- **Задача:** что просил пользователь.
- **Симптом:** как проявлялось.
- **КОРЕНЬ:** истинная причина (не гипотеза).
- **ФИКС:** что именно изменено (файл → суть).
- **Урок/⛔ правило:** если применимо.
- **ПЕНДИНГ:** что должен проверить пользователь.
```

**Правила ведения:**

- ⛔ Никаких повторов одного и того же блока — уникальные факты записываются один раз.
- Значения и сырые данные — в `app_data.log`, не в WORKLOG; в WORKLOG — выводы и правила.
- Перед созданием нового блока проверить, нет ли уже такого в `WORKLOG_summary_17.09.26.md`
  (история) — не дублировать.

---

## Счётчик сжатий

| Дата | Файл-сводка | Объём до → после |
|---|---|---|
| 17.09.2026 | `WORKLOG_summary_17.09.26.md` | 667 344 Б → 117 179 Б |

---

# ЛОГ (новый, с 17.09.2026)

## 17.09.2026 — сжатие WORKLOG (без изменения кода)

- **Задача:** проанализировать разросшийся `WORKLOG.md`, выделить самое полезное, сделать выводы,
  свести количественные и фактические данные в таблицы/статистику, записать в
  `MemoryAI/WORKLOG_summary_17.09.26.md`; в основном WORKLOG отметить факт сжатия и начать лог заново.
- **Сделано:**
  - Проведён машинный анализ: 7 097 строк / 448 690 символов / 207 блоков `## `;
    обнаружено ≈113 000 символов (25 %) чистого дублирования (48 копий одного блока).
  - Создан `MemoryAI/WORKLOG_summary_17.09.26.md` (117 179 Б) — полный сжатый дайджест
    с 10 разделами: статистика, хронология, архитектура, реестр корневых причин, уроки/⛔-правила,
    хоткей-карта, настройки, числовые данные, открытые вопросы, выводы.
  - `WORKLOG.md` перезапущен: настоящее уведомление + формат новых записей + счётчик сжатий.
- **Изменённые файлы:** `MemoryAI/WORKLOG_summary_17.09.26.md` (новый), `MemoryAI/WORKLOG.md` (перезапуск).
- **Код не менялся**, версия сборки не поднималась (остаётся `1.0.40.44-SMOOTH-CAM-09.16-2203`).
- **ПЕНДИНГ:** продолжать вести лог в новом формате; при необходимости открыть
  `WORKLOG_summary_17.09.26.md` для исторического контекста.

## 18.09.2026 — v1.0.40.54 OVERLAY-TIMER-QUEST-SMOOTH

- **Задача:** «изучи логи последнего запуска, что-то не так с оверлеями» — при запуске игра уже
  на паузе в меню: сразу показало миникарту, минилого нет; при выходе из паузы не появился
  гибрид; при повторном входе в паузу миникарта не исчезла, минилого нет; квест-диалог при ESC
  не появился. Плюс: квестовые маркеры в AR дрожат при движении.
- **КОРЕНЬ №1 (оверлеи): ПОЛИТИКА ШЛА НА `System.Windows.Forms.Timer`, КОТОРЫЙ НЕ ТИКАЛ.**
  В сессии 13:34 за 22 минуты — НИ ОДНОЙ строки политики (`Игровые интерфейсы…`, `Фокус игры=`),
  тогда как `/status` через 2 с отвечал `paused:false` при живом `paused=true` в телеметрии.
  Проверено: UI-поток жив (`SendMessageTimeout` отвечает за 27 мс), политика просто не вызывалась.
  Симптом ровно «миникарта залипла на первой команде, потом ничего»: все прочие слои оставались
  скрытыми (`_lastGameUiVisible` инициализирован `false`), а миникарту показали разово при старте.
  Дополнительный вклад: ETS2 при старте забирает фокус → политика вызывала
  `ForceForegroundWindow` СВОЕГО окна (`_committedActive` уже `true`, так как ETS2 держит фокус)
  и возвращала фокус приложению; окно игры, лишившись фокуса, перестаёт отдавать WM_TIMER.
- **ФИКС:** `UI/WebUIManager.cs` — `System.Threading.Timer` (`QueuePauseCheck`,
  `Interlocked _pauseCheckBusy`, снятие флага в `finally`), HTTP-опрос паузы В ФОНЕ,
  решение — через `BeginInvoke`. Строка жизни `[UI] политика жива: …` в app_data раз в 30 с.
  Проверено: тиков=120 за 60 с (ровно 500 мс), раньше — 0.
- **КОРЕНЬ №2 (фокус из игры):** `MoveToMonitor` при АВТОпереносе на старте вызывал
  `ForceForegroundWindow` → после перезапуска телеметрия писала «placement нет (пауза/окно игры
  неактивно)», политика — «фокус вне игры». ФИКС: `MoveToMonitor(index, activate)` —
  автоперенос `activate:false`, захват фокуса только по явному действию из трея.
- **КОРЕНЬ №3 (мёртвый порт):** `MainForm.IsGamePaused()` жёстко дёргал 8080 (МЁРТВ) с таймаутом
  2 с — в UI-потоке. Переведён на динамический `TruckTelemetry.Port`; `IsGamePausedAsync` —
  тонкая обёртка через `Task.Run(IsGamePaused)` (единый путь определения порта).
- **КОРЕНЬ №4 (маркеры дрожат):** `data/js/ar_quests.js` применял позу ТОЛЬКО в момент пакета
  (~28–35 Гц) при 60+ кадрах — ровно дефект v1.0.40.44 у точек AR1. ФИКС: та же техника
  (экстраполяция ЦЕЛИ по скорости + `k = 1 − exp(−dt/tau)`); `ar_hud.js` публикует
  `window.__arSmooth = { cfg: SMOOTH, view: camSmoothView }`, маркеры берут ОБЩУЮ силу (tau)
  и готовую сглаженную позу. ⛔ Публикация — ПОСЛЕ объявления `const SMOOTH`/`camSmoothView`
  (в temporal dead zone — `ReferenceError`). Скорость камеры — из обработчика ПАКЕТА.
  Сила сглаживания = 80 мс (`Ar1SmoothTau`), как просил пользователь.
- **Изменённые файлы:** `UI/WebUIManager.cs`, `MainForm.cs`, `data/js/ar_hud.js`,
  `data/js/ar_quests.js`, `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`,
  `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **ПЕНДИНГ:** проверка пользователем (миникарта/гибрид/минилого при паузе; квестовый диалог
  по ESC при уведомлении о квесте; плавность квестовых маркеров в движении).

## 18.09.2026 — v1.0.40.55 LOG-SPAM-CLEANUP

- **Задача:** убрать спам данными из КОНСОЛИ логов (`[AR] TILT` / `[AR] HORIZON-GEOM` — пример в
  запросе); спамить значениями можно только в `app_data`. Также убрать устаревшие сообщения о
  регистрации хоткеев и подключении локалей.
- **КОРЕНЬ:** `AppendLog` пишет ДВУМЯ путями — в `app_workflow.log` И в консоль логов
  (`Logger.OnLogMessage` → `AppendLog(persistWorkflow:false)`); `AppendDataLog` / `Logger.Data`
  пишут ТОЛЬКО в `app_data.log`. `LogArTiltDiagnostics` вызывался раз в 5 с, но через
  `AppendLog` — за 2 дня в `app_workflow.log` накопилось **2592 строки** TILT/HORIZON-GEOM
  (1292 HORIZON-GEOM + 748 TILT(rest) + 540 TILT(ws)).
- **ФИКС:** `[AR] TILT` и `[AR] HORIZON-GEOM` переведены на `Logger.Current?.Data(...)` —
  они и есть ДАННЫЕ (углы/геометрия), значит их место в `app_data`.
- **УСТАРЕВШИЕ СООБЩЕНИЯ:** сводка «Hotkeys: Ctrl+X …» (дублировала успешные регистрации)
  и «Language saved to config.json» переведены в `Logger.Data`.
- **ПОПУТНО (найденный баг):** `InitializeLanguage()` идёт РАНЬШЕ `InitializeProcessManager()`,
  а логгер создавался только там → `Logger.Current` был null, и её data-сообщение молча
  терялось. Введён идемпотентный `EnsureLogger()`, вызывается ПЕРЕД `InitializeLanguage()`.
- **ПРОВЕРЕНО:** новая сессия (1.0.40.55, 40+ с): `app_workflow.log` = 57 строк, из них
  TILT/HORIZON = 0, «Hotkeys:» = 0, «Language saved» = 0; при этом `app_data.log` содержит TILT
  и HORIZON-GEOM (данные не потеряны). Ранее — тысячи строк спама.
- **Изменённые файлы:** `MainForm.ArTarget.cs`, `MainForm.cs`, `ETS2_Assist_GUI.csproj`,
  `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`, `data/ets2_assist_build.txt`,
  `data/web_runtime_manifest.json`.
- **ПЕНДИНГ:** проверка пользователем (чистота консоли логов).

## 18.09.2026 — v1.0.40.56 QUEST-ICONS

- **Задача:** квестовые иконки — новые растровые `data/quests/images/Pointer_*.png`
  (в масштабе): для АР 32x32 без изменений, для миникарты смасштабировать до 8x8 «для начала».
- **СООТВЕТСТВИЕ ИКОНОК (определено АНАЛИЗОМ, не угадано — силуэт + цвета):**
  `Pointer_quest_on/off` = «!» (yellow_exclamation); `Pointer_questdone_on/off` = «?»
  (yellow_question); `*_on` = жёлтый, `*_off` = серый ⇒ `gray_question` ВСЕГДА берёт `_off`.
  Силуэт `!` = 10x28 px (широкая шапка 10, разрыв на y=21); `?` = 15x26 (широкая шапка 15).
- **РЕАЛИЗАЦИЯ (4 потребителя):**
  - АР `js/ar_quests.js`: 32 px при `size`=10 с прежним дистанционным рампингом
    (`markerStyle`), указатель за краем — 26 px;
  - миникарта `js/map_draw.js`: **8x8** (`questPointerIconSizePx`, исходник 32x32),
    приоритет над SDO-иконкой категории; URL приходит в поле POI `__questIcon`
    (`js/quests_client.js` выставляет его из `q.Marker`);
  - окно квестов `js/quests_ui.js`: 28 px (как было у SVG);
  - сайдбар редактора 2 `js/quest_editor.js`: 16 px.
- ⛔ **У ВСЕХ РЕНДЕРЕРОВ ОСТАВЛЕН ЗАПАСНОЙ ВАРИАНТ** (старый SVG / текстовый глиф):
  рисуем битмап только при `img.complete && img.naturalWidth>0`, иначе на первых
  кадрах (пока картинка не декодирована) маркер исчез бы совсем.
- ⛔ **`js/quest_editor.js` РАБОТАЕТ ВНУТРИ `data/map_editor2/index.html`** — его пути
  к ассетам обязаны быть `../quests/images/...` (файл подключается как
  `s.src='../js/quest_editor.js'`). Корневой путь давал 404 — поймано проверкой
  `Invoke-WebRequest` ДО сборки.
- **ПРОВЕРЕНО:** `node --check` (5 файлов), `dotnet build` 0 ошибок, `compile.ps1`
  delivery OK; все 4 иконки отдаются по 200 из обоих корней (8082 из web-страниц и
  `/map_editor2/../quests/images` из редактора); сборка 1.0.40.56-QUEST-ICONS.
- **Изменённые файлы:** `data/js/ar_quests.js`, `data/js/quests_client.js`,
  `data/js/map_draw.js`, `data/js/quests_ui.js`, `data/js/quest_editor.js`,
  `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`,
  `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **ПЕНДИНГ:** проверка пользователем (АР 32 px, миникарта 8 px — «для начала»;
  если мелко, размер поднять одной константой `questPointerIconSizePx`).

## 18.09.2026 — v1.0.40.57 QUEST-UI-AR-FIX

- **Задачи пользователя (6 пунктов):** (1) иконки на миникарте 8 → 12; (2) окно квестов
  не полностью уходит за экран → фейдаут в финальных кадрах + обратный возврат;
  (3) закладку «Квесты» НЕВОЗМОЖНО нажать (курсор проходит сквозь, выглядит игровым);
  (4) в редакторе карт пропала категория «Квесты»; (5) в статусбар редактора нужна
  ПОЛНАЯ версия сборки; (6) в АР видна прицельная точка, но иконки квеста НЕТ.
- **КОРЕНЬ №1 (АР, пункт 6) — РАСХОЖДЕНИЕ ИМЁН ПОЛЕЙ ПОЗЫ КАМЕРЫ.**
  `window.__arSmooth.view` (сглаженная поза из `ar_hud.js`) хранит позицию в
  `camX/camY/camZ`, а СОБСТВЕННАЯ структура `ar_quests.js` — в `x/y/z`. Код читал только
  `camPos.x/y/z` ⇒ для общего view это `undefined` ⇒ NaN в проекции ⇒ условие
  `sp.x>-90` ложно ⇒ маркер МОЛЧА не рисовался (ошибок в консоли НЕТ). Подтверждено
  ЗАМЕРОМ в живом браузере: `viewKeys` содержит только `camX/camY/camZ`,
  `questCanvasNonEmpty=0` при `hudNonEmpty=819`.
  ФИКС: `viewPos(camPos)` — понимает ОБЕ формы + фолбэк на сырую камеру при NaN;
  `draw()` разрешает позу ОДИН раз в СКАЛЯРЫ `vpx/vpy/vpz` (вложенный вызов `viewPos`
  больше не перезаписывает общий буфер — это вторая, скрытая ловушка того же класса).
- **КОРЕНЬ №2 (закладка, пункт 3) — ПОРЯДОК КОМАНД.** В хосте кликабельность выводится
  как `NeedsClickThroughStyle = (!_clickable || _hotspot.IsEmpty)`, а `set_clickable`
  СБРАСЫВАЕТ `_hotspot`. Страница отправляла `set_clickable(false)` ПЕРВЫМ, поэтому
  окно оставалось прозрачным для мыши, хотя горячая область задавалась следом.
  ФИКС: СНАЧАЛА `set_clickable_hotspot`, ПОТОМ `set_clickable(true)`.
- **КОРЕНЬ №3 (категория квестов в редакторе, пункт 4) — ФЛАГ ИНЪЕКЦИИ НЕ СБРАСЫВАЛСЯ.**
  `map2-ready` приходит на КАЖДУЮ загрузку страницы (в логе ДВЕ записи подряд в одной
  сессии редактора), а `_questEditorOverlayInjected` оставался true ⇒ скрипт
  `js/quest_editor.js` не подключался заново ⇒ категория «Квестовые» исчезала целиком.
  ФИКС: сброс флага в обработчике `map2-ready` + идемпотентность НА СТОРОНЕ СТРАНИЦЫ
  (ранний выход, если элемент `#questEditorOverlayScript` уже есть) — насос 600 мс держит
  модуль живым. Плюс `decorateSidebar` возвращает секцию ПЕРЕД первым ВИДИМЫМ соседом и
  снимает `display:none` (у «Цели» он может быть скрыт).
- **ПУНКТ 5 (версия в статусбаре редактора):** `EditorStatusBar.SetBuildVersion`
  рисует полную строку `BuildInfo.Version` сразу после заголовков состояний
  (приглушённым цветом); заполняется из `MapEditorForm` при открытии.
- **ПУНКТ 1:** миникарта 8 → **12** px (`questPointerIconSizePx`).
- **ПУНКТ 2:** фейдаут в финальных кадрах сворачивания (`transform 220ms, opacity 160ms 220ms`)
  и ЗЕРКАЛЬНОЕ проявление при раскрытии (`opacity 160ms, transform 220ms 160ms`).
- **МЕТОДИКА (важно):** расхождение имён полей найдено только ЗАМЕРОМ в живом браузере
  (состав ключей объекта + подсчёт непрозрачных пикселей канваса). По коду дефект
  невидим: обе ветки выглядят корректно, исключений нет.
  ⚠️ ВНИМАНИЕ: превью-вкладка VS Code имела `visibilityState='hidden'` и **0 кадров rAF**
  за 1.5 с — в скрытой вкладке `requestAnimationFrame` НЕ идёт и канвас не обновляется;
  это артефакт стенда, а НЕ баг продукта (проверять только в видимой вкладке).
- **ПРОВЕРЕНО:** `node --check` (все правленые JS), `dotnet build` 0 ошибок,
  `compile.ps1` delivery OK; сборка 1.0.40.57-QUEST-UI-AR-FIX, версии синхронизированы.
- **Изменённые файлы:** `data/js/ar_quests.js`, `data/js/quests_ui.js`,
  `data/js/map_draw.js`, `data/js/quest_editor.js`, `data/web_quests.html`,
  `UI/EditorStatusBar.cs`, `MapEditorForm.cs`, `MapEditor2Form.cs`,
  `MapEditor2Form.QuestOverlay.cs`, `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`,
  `АРХИТЕКТУРА ПРОЕКТА.md`, `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **ПЕНДИНГ:** проверка пользователем (иконки АР/миникарты, клик по закладке,
  фейдаут окна, категория и версия в редакторе).

## 18.09.2026 — v1.0.40.58 DEBUGSHOW-FOCUS-ONLY

- **Задачи пользователя (3 пункта):** (1) debugShow должен игнорировать логику скрытия
  окон ВНЕ ФОКУСА — при включённой галочке веб-оверлеи без фокуса НЕ исчезают,
  но логика ПАУЗЫ / БЕЗ ПАУЗЫ отрабатывает как обычно; (2) при debugShow гибридное
  окно меняет масштабы и все блоки внутри искажаются; (3) рамку по периметру АР удалить.
- **КОРЕНЬ №1 (пункты 1 и 2 — ОДИН ДЕФЕКТ).** Прежний `debug_show.js` вешал на все
  найденные контейнеры И ИХ ПОТОМКОВ правило `.debug-show-force`
  (`display:block !important; transform:none !important; max-width:none !important;
  max-height:none !important`), снимал инлайн-стили и **замораживал** `classList`
  + `MutationObserver` + дозатор 500 мс.
  Последствия ровно те, что описал пользователь:
  1) у гибрида flex-раскладка и анимация `scale` — правило ломало и то, и другое
     («окно меняет масштабы, блоки искажаются»);
  2) заморозка блокировала саму логику показа: `show_ui`/`hide_ui`,
     `minimap_show`/`minimap_hide` переставали действовать ⇒ debugShow ОТКЛЮЧАЛ
     логику паузы вместо того, чтобы отключать только правило фокуса.
- **ФИКС (единый смысл):** debugShow отменяет РОВНО скрытие по фокусу и ничего больше.
  - `data/js/ui_category.js`: правило взаимоисключения категорий
    (`html.ets2-cat-game [data-category~="interactive"]` и обратное) теперь работает
    ВСЕГДА (убран `:not([data-debug-show="1"])`); под debugShow закрыт ТОЛЬКО селектор
    скрытия по фокусу (`html.ets2-hide-all`). `applyHideAll` при `__debugShow` не ставит
    ни класс, ни `pointer-events:none`. Появился `window.ets2ReapplyVisibility()`:
    запомненная команда `set_overlay_hidden` пересматривается при переключении режима
    (иначе после выключения debugShow окна остались бы видимыми без фокуса).
  - `data/js/debug_show.js`: удалены `forceShow/restore/installFreeze/uninstallFreeze`,
    `MutationObserver`, дозатор 500 мс и вся работа с инлайн-стилями. Осталась одна
    пассивная CSS-страховка `html[data-debug-show="1"].ets2-hide-all [data-category]`
    (на случай, если стиль категорий ещё не загрузился) + снятие уже применённого класса.
    Новый `window.ets2DebugShowBase` — базовая реализация для страниц со СВОИМ
    `window.debugShow`.
  - `data/js/ar_hud.js` и `data/web_heights.html` перекрывают `window.debugShow`
    своими отметками, поэтому ТЕРЯЛИ флаг общего режима: теперь оба ПЕРВЫМ делом
    вызывают `window.ets2DebugShowBase(on)`.
  - `UI/WebUIManager.cs`: `gameVisible = gameRunning && (debugShow || gameFocused)`,
    `UpdateOverlayLayerFocus(debugShow || _committedActive)` — обходится ТОЛЬКО
    слагаемое фокуса; пауза и взаимоисключение категорий считаются как прежде.
    `MainForm` при переключении чекбокса дополнительно зовёт `ResetOverlayVisibilityCache()`.
- **ПУНКТ 3 (рамка по периметру АР):** удалены `CFG.showClipFrame`, функция
  `drawClipFrame()` и оба её вызова в `data/js/ar_hud.js`. Прижим маркеров к краям
  сохранён (`CFG.edgeMargin` в `clampToScreen`) — убрана только отрисовка рамки.
  В AR2 (нативный) рамки никогда и не было.
- **МЕТОДИКА:** дефект видел только пользователь — это ПОВЕДЕНЧЕСКИЙ баг (CSS-приоритеты
  и заморозка DOM), а не исключение. Проверять такие правки в ЖИВОЙ странице:
  включить галочку, снять фокус (Alt+Tab) и убедиться, что окна остались, затем
  поставить/снять паузу и убедиться, что интерактивы/игровая категория переключаются.
- **ПРОВЕРЕНО:** `node --check` (debug_show.js, ui_category.js, ar_hud.js) + инлайн-скрипт
  web_heights.html — OK; `dotnet build` 0 ошибок; `compile.ps1` delivery OK (422 файла);
  версии синхронизированы (exe = build.txt = manifest = 1.0.40.58-DEBUGSHOW-FOCUS-ONLY-09.18-1603);
  MD5 пяти правленых web-файлов в publish совпадают.
- **Изменённые файлы:** `data/js/debug_show.js`, `data/js/ui_category.js`,
  `data/js/ar_hud.js`, `data/web_heights.html`, `UI/WebUIManager.cs`, `MainForm.cs`,
  `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`,
  `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **ПЕНДИНГ:** проверка пользователем (галочка debugShow: окна не исчезают без фокуса,
  пауза переключает категории; гибрид не искажается; рамки по периметру АР нет).

## 18.09.2026 — v1.0.40.59 QUEST-TAB-TOGGLE

- **Задача пользователя:** когда на экране видна закладка «Квесты», нажатие TAB
  должно сворачивать/разворачивать окно квестов. Окно выводится поверх игры и
  полностью забирает себе фокус и курсор. TAB в окне квестов — то же, что нажатие
  на стрелочку сворачивания: сворачивает окно и оставляет закладку.
- **КОРЕНЬ (почему нельзя просто `keydown` на странице):** хост оверлеев намеренно
  создаётся с `WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW`, `ShowWithoutActivation=true` и
  подавленным `OnActivated` (`Program.SuppressOverlayActivation`). Это ЗАЩИТА от
  регресса v1.0.40.54: захват фокуса оверлеем отбирал foreground у ETS2, телеметрия
  рапортовала «placement нет (пауза/окно игры неактивно)» и политика прятала ВСЁ.
  ⇒ Страница НИКОГДА не получает WM_KEYDOWN, и `document`-keydown там не сработает.
- **РЕШЕНИЕ:** TAB — ГЛОБАЛЬНЫЙ ХОТКЕЙ ПРИЛОЖЕНИЯ, активный РОВНО ПОКА видна
  интерактивная категория (закладка или окно).
  - `MainForm`: `HOTKEY_QUEST_TOGGLE = 9022`, регистрация `MOD_NOREPEAT` + `Keys.Tab`
    (без Ctrl/Shift) в `SetQuestToggleHotkeyActive(bool)`; в `WndProc` →
    `SendCommandToMap("quest_toggle_collapse")`.
  - ⛔ ХОТКЕЙ РЕГИСТРИРУЕТСЯ И СНИМАЕТСЯ ДИНАМИЧЕСКИ из
    `WebUIManager.ApplyOverlayVisibility` по `showInteractive` — иначе TAB был бы
    перехвачен глобально во ВСЕХ приложениях. При `StopSystem` снимается гарантированно.
  - `data/js/quests_ui.js`: команда `quest_toggle_collapse` идёт через ТЕ ЖЕ
    `collapseWindow`/`expandWindow`, что и стрелочка, поэтому защита от двойного
    жеста (250 мс `blockToggle()`) и отчёт `quest_window_state` работают как прежде.
- **ФОКУС И КУРСОР (частично ограничено осознанно):** окно реально принимает мышь
  целиком (`set_clickable(true)` + `set_clickable_hotspot` сброшен), а над окном
  показывается ОБЫЧНЫЙ КУРСОР — новый `applyCursorLayer()` в `syncInput` +
  `#questApp.paused{cursor:default}` (в остальных слоях курсор игры скрыт).
  При сворачивании прежний `return_focus` возвращает управление игре.
  ⛔ FOREGROUND оверлею НЕ отдаём: это сломало бы телеметрию (см. корень выше).
  ⛔ Настройка `WebOverlay` — ОТДЕЛЬНЫЙ РЕПО, правки GUI-репо его не меняют.
- **ПРОВЕРЕНО:** `node --check js/quests_ui.js`; `dotnet build` 0 ошибок;
  `compile.ps1` delivery OK (422 файла), кэш WebView2 очищен; версии синхронизированы
  (exe = build.txt = manifest = 1.0.40.59-QUEST-TAB-TOGGLE-09.18-1623); MD5 правленых
  web-файлов в publish совпадают.
- **Изменённые файлы:** `MainForm.cs`, `UI/WebUIManager.cs`, `data/js/quests_ui.js`,
  `data/web_quests.html`, `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`,
  `АРХИТЕКТУРА ПРОЕКТА.md`, `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **ПЕНДИНГ:** проверка пользователем (TAB сворачивает/разворачивает окно, когда видна
  закладка или окно; закладка остаётся при сворачивании).

## 18.09.2026 — v1.0.40.60 QUEST-CURSOR-FADE

- **Задачи пользователя (2 пункта):** (1) окно квестов не получает курсор — курсор
  остаётся в игре и двигается ПОД окном; (2) схема анимации сворачивания: кадр 0 —
  прозрачность 0% и старт ease-движения «с медленного с ускорением»; за 70% до
  последнего кадра — ключ прозрачности 0%; 99% кадров — прозрачность 100%
  (окно становится невидимым ЕЩЁ В ДВИЖЕНИИ, а не после остановки).
- **КОРЕНЬ №1 (курсор).** CSS `cursor:default` на странице бесполезен: ETS2 со своим
  HUD СКРЫВАЕТ системный курсор и рисует собственный. Он принадлежит игре и остаётся
  сверху как объект рабочего стола, двигаясь под нашим окном. Страница это исправить
  не может — курсор нужно ЗАБРАТЬ на уровне окна.
  ФИКС (в ОТДЕЛЬНОМ РЕПО WebOverlay):
  - новая команда страницы `set_cursor {value}` → `OverlayForm.SetCursorOwnership`
    (принимается только для web_quests/web_notifications/web_ar_hud);
  - `ShowCursor(TRUE)` до НЕОТРИЦАТЕЛЬНОГО счётчика + `SetCursor(IDC_ARROW)`;
  - `WM_SETCURSOR`/`HTCLIENT` на своей клиентской области отдаёт стрелку — сообщение
    приходит в окно ПОД КУРСОРОМ, поэтому игра свой курсор в этот кадр не рисует;
  - «пульс» 30 мс, пока курсором владеет слой: игра продолжает опускать счётчик
    каждый кадр, без поддержки стрелка гаснет. Пульс выравнивает счётчик, а не растит:
    если уровень уже >= 0 — лишний плюс сразу забирается назад.
  - ⛔ СЧЁТЧИК ОБЯЗАН БЫТЬ СБАЛАНСИРОВАН (`ReleaseCursor` снимает ровно добавленные
    плюсы + вызов в `OnFormClosing`) — иначе курсор исчезнет во ВСЕЙ системе.
  - ⛔ Это НЕ захват фокуса: foreground остаётся у игры (реальная передача ломает
    телеметрию — регресс v1.0.40.54).
  - Страница шлёт `set_cursor(true)` из `applyCursorLayer()` (в `syncInput`), то есть
    ровно при `pagePaused && !collapsed`.
- **КОРЕНЬ №2 (фейдаут завершался после остановки).** Прежние правила:
  развёрнутое — `transform 220ms, opacity 160ms 160ms`, свёрнутое — `transform 220ms,
  opacity 160ms 220ms`. Фейдаут не только ЗАДЕРЖАН на 220 мс, но и длится ещё 220 мс,
  то есть заканчивался ПОСЛЕ остановки окна — отсюда видимая «полоса» в конце.
  ФИКС — тайминги считаются от 320 мс движения ровно по процентам пользователя:
  - кадр 0: `opacity:1` → 0%, движение сразу `ease-in` («с медленного с ускорением»);
  - до 70% пути (224 мс): прозрачность ДЕРЖИТСЯ 0% — `opacity 92ms ease-in 224ms`;
  - 70%→99% (224→316 мс): прозрачность уходит в 100% ⇒ окно невидимо В ДВИЖЕНИИ.
  - Раскрытие зеркальное: `transform 320ms ease-out` (замедление к концу) и
    `opacity 96ms ease-out` БЕЗ задержки.
- **МЕТОДИКА:** «медленный старт с ускорением» = `ease-in`; «замедление к концу» =
  `ease-out`. Задержка перехода сдвигает НАЧАЛО, а не конец — именно из-за неё
  фейдаут оказался после остановки.
- **ПРОВЕРЕНО:** `node --check js/quests_ui.js`; сборка WebOverlay (отдельное репо)
  0 ошибок; `dotnet publish` 0 ошибок; `compile.ps1` delivery OK (422 файла; WebOverlay.exe
  пересобран и скопирован в data\bin); версии синхронизированы
  (exe = build.txt = manifest = 1.0.40.60-QUEST-CURSOR-FADE-09.18-1638);
  MD5 quests_ui.js / web_quests.html в publish совпадают.
- **Изменённые файлы:** `MainForm.cs`, `UI/WebUIManager.cs`, `data/js/quests_ui.js`,
  `data/web_quests.html`, `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`,
  `АРХИТЕКТУРА ПРОЕКТА.md`, `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`;
  ВНЕ РЕПО: `F:\repo\weboverlay\Program.cs` (+ собранный WebOverlay.exe).
- **ПЕНДИНГ:** проверка пользователем (курсор над развёрнутым окном не двигается под
  ним; сворачивание исчезает в движении; раскрытие).

## 18.09.2026 — v1.0.40.61 QUEST-CURSOR-TAB-HINT

- **Задачи пользователя (2 пункта):** (1) на закладке «Квесты» снизу в 3px от границы,
  внутри закладки, тёмно-серым НЕЖИРНЫМ мелким шрифтом и ГОРИЗОНТАЛЬНО (не как
  «Квесты») написать «TAB»; (2) курсор так и не появляется — при нажатии TAB виден
  только игровой курсор, перемещающийся ПОД окном.
- **КОРЕНЬ №1 (пункт 2) — КУРСОР ДВИЖЕТСЯ ПОД ОКНОМ, ЗНАЧИТ РИСУЕТ ЕГО ИГРА.**
  ETS2 прячет системный курсор и рисует СВОЙ прямо в ИГРОВОЙ КАДР. Игровой кадр
  лежит НИЖЕ слоя оверлея (слой — `WS_EX_TOPMOST`), поэтому стрелка игры видна и
  двигается «под окном». Ни `ShowCursor`, ни `SetCursor`, ни `WM_SETCURSOR` этого
  не изменят: наш слой ВСЕГДА выше. Дополнительно игра уводит счётчик `ShowCursor`
  глубоко в минус (замерено −7…−10).
  ⛔ ПРАВИЛО: отрисовка курсора в свой кадр (спрайт) не отбирается ни сменой
  курсора, ни стилем слоя. Положение курсора нужно менять ТОЛЬКО если игра
  использует системный курсор.
  ФИКС: курсор рисует САМА СТРАНИЦА. `#questApp.paused{cursor:none}` скрывает
  курсор внутри слоя, а `quests_ui.js` рисует СОБСТВЕННУЮ стрелку
  (`<svg id="cursorDot">` 15x21, белая с тёмной обводкой, `z-index:2147483000`),
  позиционируемую из `mousemove` (`startCursorTrack`/`placeCursor`,
  `applyCursorLayer` из `syncInput`, активно ровно при `pagePaused && !collapsed`).
  Отладочный вход: `window.__questCursor.place(x,y)`.
  Хостовая мера `set_cursor` (`SetCursorOwnership`) оставлена как вспомогательная.
- **ПОБОЧНО НАЙДЕН И ИСПРАВЛЕН OFF-BY-ONE В ПУЛЬСЕ КУРСОРА (хост).**
  `ShowCursor` возвращает счётчик ПОСЛЕ вызова, а курсор виден при `>= 0`. Прежнее
  условие отката `level >= 0` отменяло РОВНО ТОТ плюс, который делал курсор
  видимым: счётчик болтался между −1 и 0, курсор не показывался НИКОГДА.
  Верное условие отката — `level >= 1` (только лишний плюс).
- **КОРЕНЬ №2 (пункт 1) — ОТСТУП СЧИТАЛСЯ ОТ PADDING-БОКСА.** У закладки
  `padding-bottom:18px`, поэтому обычный `margin-bottom` дал бы 21px, а `bottom`
  отсчитывается от ВНУТРЕННЕЙ стороны рамки 1px. Ставим `bottom:2px` — на живой
  странице ЗАМЕРЕНО ровно **3px** до видимой границы.
  `writing-mode` НАСЛЕДУЕТСЯ, поэтому подсказке задан `horizontal-tb` (отменяет
  унаследованный `vertical-rl` от «Квесты»): цвет #5c646e, `font:400 9px`.
- **МЕТОДИКА (запомнить):** жалоба «курсор под окном» однозначно указывает, что
  курсор рисует игра в свой кадр, а не система. Проверять это ДО того, как менять
  ShowCursor/стили — иначе правится несуществующая причина.
- **ПРОВЕРЕНО:** `node --check js/quests_ui.js`; сборка WebOverlay 0 ошибок;
  `dotnet publish` 0 ошибок; `compile.ps1` delivery OK (422 файла, WebOverlay.exe
  пересобран и скопирован в `data\bin`); версии синхронизированы
  (exe = build.txt = manifest = 1.0.40.61-QUEST-CURSOR-TAB-HINT-09.18-1658);
  MD5 quests_ui.js / web_quests.html в publish совпадают.
  ВИЗУАЛЬНО (живая страница): отступ подсказки = 3px, `writing-mode` подсказки
  horizontal-tb при вертикальной подписи, цвет rgb(92,100,110), вес 400, 9px;
  собственная стрелка рисуется (скриншот закладки и стрелки приложены).
- **Изменённые файлы:** `data/web_quests.html`, `data/js/quests_ui.js`,
  `ETS2_Assist_GUI.csproj`, `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`,
  `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`;
  ВНЕ РЕПО: `F:\repo\weboverlay\Program.cs` (off-by-one пульса курсора).
- **ПЕНДИНГ:** проверка пользователем (видна ли собственная стрелка над окном;
  подсказка TAB на закладке).

---

## 19.09.2026 — 1.0.40.65-LOGS-COLLECT-BTN-09.19-1043 — ВРЕМЕННАЯ КНОПКА «Собрать логи»

- **Задача:** временная кнопка ПОД окном консоли логов в основной форме: собрать
  `publish\Logs\**` + `%APPDATA%\WebOverlay\quest-input-diagnostic.log` +
  `%APPDATA%\WebOverlay\debug.log` в `MemoryAI\LOGS`, с принудительной перезаписью
  и отчётом в консоль о выполненных операциях.
- **РЕАЛИЗАЦИЯ (`MainForm.cs`):** поле `btnCollectLogs` (120x26, `consoleLeft`,
  `Bottom|Left` anchor), создаётся сразу после `logConsole`, добавлено в
  `Controls.AddRange`. Обработчик `CollectDiagnosticLogs()` + хелперы
  `CopyLogFile()` (пишет строку отчёта) и `ResolveLogsDropFolder()`.
- **ИСТОЧНИК ЛОГОВ приложения:** `AppDomain.CurrentDomain.BaseDirectory\Logs`.
  Если exe запущен из `bin\Release\net10.0-windows\win-x64` (не из publish),
  подставляется `<...>\win-x64\publish\Logs` — это и есть затребованная папка.
  Обход `SearchOption.AllDirectories` с сохранением относительной структуры.
- **ПОИСК ПАПКИ-ПРИЁМНИКА:** `ResolveLogsDropFolder()` поднимается по родителям
  на 10 уровней от `BaseDirectory` и от `Environment.CurrentDirectory`, ища
  `MemoryAI\LOGS` — путь не хардкодится, работает и в рабочей копии, и в worktree.
- **ПОВЕДЕНИЕ:** `File.Copy(..., overwrite: true)` — перезапись принудительно.
  Отчёт: cyan/обычные строки `[LOGS] Скопирован: файл -> путь (N байт, изменён …)`,
  оранжевые `[LOGS] Не найден: …` для отсутствующих источников, зелёный итог
  `[LOGS] Готово: скопировано файлов — N`, красный при нуле копий. Ошибки доступа
  ловятся пофайлово и не роняют обработчик.
- **ФАЙЛЫ КОПИРУЮТСЯ ПЛОСКО** для `%APPDATA%`-источников (по именам) и с
  относительными подпутями для `Logs\**`.
- **ПРОВЕРЕНО:** `dotnet build` 0 ошибок; `compile.ps1` as-is — delivery check OK
  (422 файла hashed), WebView2 cache cleared, `MemoryAI\LOGS` чист (только
  README.md). Версии синхронизированы: exe `1.0.40.65-LOGS-COLLECT-BTN-09.19-1043`
  = `build.txt` = `manifest` (штамп `Hmm` в exe генерируется при сборке — 1043
  против 1041, это норма).
- **Изменённые файлы:** `MainForm.cs`, `ETS2_Assist_GUI.csproj` (`VersionIter` 65,
  `VersionDesc=LOGS-COLLECT-BTN`), `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`,
  `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **⛔ КНОПКА ВРЕМЕННАЯ:** после отладки кейса «quest input» её нужно УДАЛИТЬ
  (`btnCollectLogs`, `CollectDiagnosticLogs`, `CopyLogFile`, `ResolveLogsDropFolder`
  и позиция в `Controls.AddRange`). В `АРХИТЕКТУРА ПРОЕКТА.md` помечена как
  `temporary_logs_collect_button`.
- **ПЕНДИНГ:** проверка пользователем — что кнопка не перекрывает нижнюю часть
  консоли/`BUILD`-бейдж и что все три источника реально копируются.

---

## 19.09.2026 — 1.0.40.68-WEBOVERLAY-VERSION-09.19-1154 — Версия WebOverlay второй строкой + лог при старте

- **Задача:** в правом нижнем углу основной формы (там уже выводится `BUILD <версия
  приложения>`) добавить высоты на одну строку и во ВТОРОЙ строке, ПОД версией
  приложения, выводить версию WebOverlay. При запуске приложения проверять версию
  WebOverlay и писать её в лог и в окно формы.
- **РЕАЛИЗАЦИЯ (`MainForm.cs`):** новое поле `webOverlayVersionLabel` (Label,
  `Consolas 8.5 Bold`, `DarkGray`, `AutoSize`, anchor `Bottom|Right`), создаётся
  рядом с `buildVersionLabel` и добавлено в `Controls.AddRange`.
  `PositionBuildLabel()` переписана на укладку СТРОК ОТ НИЗА ВВЕРХ: сначала
  вычисляется верх WebOverlay-строки (`ClientSize.Height - h - 6`), затем строка
  версии приложения ставится ровно над ней (`webOverlayTop - h - 2`); обе
  вызывают `BringToFront()`. Так порядок строк не зависит от порядка добавления
  контролов и не даёт наложения.
- **`CheckWebOverlayVersion()`** (вызывается в конструкторе сразу после
  `InitDevModeFromConfig()`, обёрнута в try/catch): читает
  `<BaseDirectory>\data\bin\WebOverlay.exe` через `FileVersionInfo` — **процесс НЕ
  запускается**. В окно идёт `ProductVersion` (= `AssemblyInformationalVersion`)
  с отрезанным суффиксом `+<scm hash>`, т.к. `FileVersion` у SDK-сборки — только
  числовая часть `1.0.40.67` и теряет описательный суффикс.
- **ВЫВОД:** в окно — `WebOverlay: 1.0.40.67` (красный `WebOverlay: НЕ НАЙДЕН`,
  если файла нет); в `Logs\app_workflow.log` и консоль — две строки:
  `[WEBOVERLAY] Версия WebOverlay: 1.0.40.67` и
  `[WEBOVERLAY] path=…; fileVersion=1.0.40.67; informational=1.0.40.67+e1f0deb…; size=174130674 байт`.
  Если версия начинается с `1.0.0` — оранжевое предупреждение «сборка без версии
  в csproj» (именно так было в v1.0.40.66: `assemblyVersion=1.0.0.0`).
- **ПРОВЕРЕНО:** `compile.ps1` as-is, 0 ошибок; publish delivery OK (422 файла
  hashed); WebView2 cache cleared; `MemoryAI\LOGS` чист. Лог старта новой сборки
  подтверждён: `BUILD_VERSION=1.0.40.68-WEBOVERLAY-VERSION-09.19-1152` +
  обе строки `[WEBOVERLAY]` (WebOverlay = `1.0.40.67`). Версии приложения
  синхронизированы: exe = `build.txt` = `manifest` = `1.0.40.68-WEBOVERLAY-VERSION`.
- **Изменённые файлы:** `MainForm.cs`, `ETS2_Assist_GUI.csproj` (`VersionIter` 68,
  `VersionDesc=WEBOVERLAY-VERSION`), `BuildInfo.cs`, `АРХИТЕКТУРА ПРОЕКТА.md`,
  `data/ets2_assist_build.txt`, `data/web_runtime_manifest.json`.
- **ЗАМЕЧАНИЕ:** `compile.ps1` уже собирает соседний `..\weboverlay` и печатает
  `WebOverlay delivered: version=1.0.40.67+e1f0deb…` — теперь это же значение
  видно из приложения, сверять вручную не нужно. Строку версии задаёт
  `F:\repo\weboverlay\WebOverlay.csproj` (`AssemblyInformationalVersion` =
  `1.0.40.67-RAW-INPUT-TEST`) — она в СВОЁМ репозитории.
- **ПЕНДИНГ:** визуальная проверка пользователем — что две строки в правом нижнем
  углу не накладываются и не перекрываются нижним краем окна.

---

## 19.09.2026 — pull.ps1 — git pull для ОБОИХ репозиториев

- **Задача:** `pull.ps1` должен пуллить и `ets2_assist`, и соседний `weboverlay`.
- **РЕАЛИЗАЦИЯ:** `pull.ps1` переписан. Шаги: (1) `git -C $PSScriptRoot pull` для
  текущего репозитория; (2) поиск соседнего WebOverlay — сначала `..\weboverlay`,
  если нет `.git` — `..\..\weboverlay` (ровно та же логика, что уже была в
  `compile.ps1`, поэтому worktree-раскладка тоже работает), затем
  `git -C <resolved> pull`; (3) `dotnet clean` + `dotnet restore` +
  `& "$PSScriptRoot\compile.ps1"` (сборку оверлея делает сам compile.ps1 —
  отдельный publish не нужен).
- **ОБРАБОТКА ОШИБОК:** `$ErrorActionPreference='Stop'`; после каждого `git pull`
  проверяется `$LASTEXITCODE`, при провале — красное сообщение и `exit` с тем же
  кодом (не собираем частично обновлённое состояние). Отсутствие репозитория
  WebOverlay — НЕ фатально: жёлтое предупреждение, pull пропущен (compile.ps1
  оставит существующий `data\bin\WebOverlay.exe`).
- **⛔ НАЙДЕНА И ИСПРАВЛЕНА ЛОВУШКА КОДИРОВКИ.** Первая версия скрипта была с
  русскими комментариями, и `Parser::ParseFile` дал ошибки «В строке отсутствует
  завершающий символ "» / «Отсутствует закрывающий знак }». Причина: **PowerShell
  5.1 читает файлы БЕЗ BOM как ANSI** (Windows-1251), поэтому UTF-8 кириллица
  рассыпается и ломает парсер. `compile.ps1`/`kill.ps1`/`start_*.bat` в репозитории
  ASCII-only именно поэтому. Скрипт переписан полностью на ASCII (проверено:
  `non-ASCII=0`, `Parser::ParseFile` -> syntax OK).
- **ПРОВЕРЕНО:** `Parser::ParseFile` — syntax OK и для `pull.ps1`, и для
  `compile.ps1` (регресса нет); `pull.ps1` = 2295 байт, non-ASCII = 0. Discovery
  соседнего репо проверен вживую: `..\weboverlay` -> `F:\repo\weboverlay`,
  `hasGit=True`, origin `https://github.com/zvukoper/weboverlay.git`;
  `..\..\weboverlay` не существует (fallback не мешает). `git pull` в
  `F:\repo\weboverlay` (ветка `main`) выполнен вручную — `Already up to date.`
- **Изменённые файлы:** `pull.ps1`, `АРХИТЕКТУРА ПРОЕКТА.md` (правило
  `build_scripts`). Версия приложения НЕ менялась (изменение вне исходников C#).
- **ПРАВИЛО НА БУДУЩЕЕ:** любые `.ps1` в репозитории писать ТОЛЬКО ASCII
  (кириллица допустима лишь с BOM, но безопаснее ASCII).


## 19.09.2026 — v1.0.40.79 CURSOR-CAL-TAB-QUEST-R9

- **Курсор:** Raw Input и обычный Windows relative mouse input — разные уровни. `SendInput` использует relative `MOUSEINPUT.dx/dy`, причём Windows может применять mouse speed и thresholds; это не доказательство, что конкретная игра увидит те же данные как свой физический Raw Input. Поэтому host-side калибровка ведёт физический курсор к `(0,0)`, а web-курсор сразу стартует из `(0,0)`. Источник: Microsoft MOUSEINPUT/Raw Input docs.
- **Курсор fade grace:** сохранён расчёт затухания тени и добавлен буфер **50 px** перед началом quadratic fade.
- **TAB:** старый `RegisterHotKey(TAB)` был воспроизведён по v1.0.40.59, но в runtime TAB больше не реагировал. В v1.0.40.79 оставлен один источник: `WH_KEYBOARD_LL` в `MainForm`, активный только при видимом интерактивном UI; key-down переключает квестовое окно, autorepeat блокируется, TAB проглатывается на время жеста.
- **Клик по квесту:** введён локальный `questDetailPinned`. Пока карточка квеста открыта из списка, устаревшие `selectedInteraction/selectedQuest/dialogue` от backend не могут через следующий секундный `quest_state` сбросить выбор. Выбор интерактива явно снимает pin.
- **Анимация:** исходные пользовательские фазы НЕ изменены: движение 320 ms `ease-in`, opacity держится 0 до 224 ms, fade 224→316 ms, expand зеркальный `ease-out` + 96 ms fade с начала.
- **Версия:** приложение и quest web UI подняты с 1.0.40.78 до **1.0.40.79-CURSOR-CAL-TAB-QUEST-R9**; build.txt и web manifest синхронизированы.

---

## 19.09.2026 — compile.ps1 Stage 3e — удаление логов WebOverlay при сборке

- **Задача:** при компиляции удалять также логи хоста оверлея
  `%APPDATA%\WebOverlay\debug.log` и
  `%APPDATA%\WebOverlay\quest-input-diagnostic.log`.
- **РЕАЛИЗАЦИЯ (`compile.ps1`, новый **Stage 3e** сразу после Stage 3d):** список
  `$overlayLogs = @('debug.log','quest-input-diagnostic.log')` в
  `Join-Path $env:APPDATA 'WebOverlay'`. Для каждого файла: если есть — пишется
  размер, делается `Remove-Item -Force`, затем **проверяется факт удаления**
  (`Test-Path` после удаления). Не удалилось (файл залочен) — жёлтое
  предупреждение `WebOverlay log NOT removed (locked?)`, а не молчаливый провал.
  Итог: `WebOverlay logs cleaned: N file(s) removed` / `already clean`.
- **ПОРЯДОК ГАРАНТИРУЕТ ОТСУТСТВИЕ ЛОКОВ:** хост WebOverlay убивается в Stage 1,
  wipe идёт ПОСЛЕ `dotnet publish` и ДО запуска в Stage 4 — держать файлы в этот
  момент некому.
- **⛔ ГЕЙТ $deliveryOk:** блок пропускается при провале delivery check
  (`WebOverlay log cleanup SKIPPED … keep the logs for diagnosis`) — сломанная
  сборка не должна уничтожать логи, которые нужны для её диагностики. Та же
  защита, что у Stage 3d.
- **ПОЧЕМУ УДАЛЯЕМ:** устаревший лог от прошлой сборки хуже отсутствующего — его
  отдают на анализ, и записи ошибочно приписывают НОВОЙ сборке.
- **ИЗМЕРЕНО:** `quest-input-diagnostic.log` разросся до **141 066 606 байт
  (~141 МБ)**, `debug.log` — 519 653 байта. За одну сборку освободилось ~141 МБ.
- **ПРОВЕРЕНО:** dry-run блока (без удаления) — оба файла найдены и помечены к
  удалению; затем реальный `.\compile.ps1`: `Removed WebOverlay log:
  …debug.log (519653 bytes)`, `Removed WebOverlay log:
  …quest-input-diagnostic.log (141066606 bytes)`, `WebOverlay logs cleaned:
  2 file(s) removed`. После сборки в `%APPDATA%\WebOverlay` остался только
  `config.json`. `MemoryAI\LOGS cleaned: 5 temporary file(s)`. Publish delivery OK
  (423 файла hashed), WebView2 cache cleared, WebOverlay delivered
  `1.0.40.78+60e6872d…`. Версия приложения не менялась (правка только сборочного
  скрипта): exe = `1.0.40.81-CURSOR-CAL-TAB-QUEST-R11`.
- **Изменённые файлы:** `compile.ps1`, `АРХИТЕКТУРА ПРОЕКТА.md` (`publish_cache`).
- **ЗАМЕЧАНИЕ:** в `compile.ps1` уже были 4 не-ASCII символа (`—`, тире в старых
  комментариях строк 27/84/130/145) — парсер их терпит, т.к. они не ломают
  лексику; НОВЫЕ комментарии писать ASCII-only (правило из записи про `pull.ps1`).
